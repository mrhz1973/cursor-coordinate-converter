# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-04-29 — **Range Rings 5D (Cursor):** label SVG nome (`<text class="rr-label">`) vicino al centro in `renderRangeRingsOverlay` se `labelMode === "name"`; nuovi set da UI con `labelMode: "name"`; costante `RANGE_RING_LABEL_MODES` include `"name"`. Dettaglio: `docs/orchestrator/inbox/2026-04-29_2405_riepilogo_range-rings-5D-label-svg.md`. Precedente: **5C** tabella+batch (`…2345_…`).

## Ultimo intervento Cursor

Monolite: solo **5D** (overlay + CSS + create default `labelMode`). Memoria orchestratore: inbox + `latest.md`. **Nessun** `finito`.

## File modificati (sintesi)

- `coordinate_converter Claude.html` (tipicamente non nel commit autosync)
- `docs/orchestrator/latest.md`, `docs/orchestrator/inbox/2026-04-29_2405_riepilogo_range-rings-5D-label-svg.md`

**Non toccati:** `docs/checkpoint.md`, `docs/session-geolocalizzazione-e-mappa.md`, `docs/roadmap.md`.

## Prossimo passo consigliato

1. **5E** — «Punta e crea» + flag pick dedicato.
2. **5F** — maniglia centro + drag.
3. QA browser: label su pan/zoom, tema chiaro/scuro, set `labelMode: "off"` senza testo.

## Dettagli (inbox)

- **5D:** `docs/orchestrator/inbox/2026-04-29_2405_riepilogo_range-rings-5D-label-svg.md`
- **5C:** `docs/orchestrator/inbox/2026-04-29_2345_riepilogo_range-rings-5C-lista-tabella-batch.md`
- **Piano 5C–5F:** `docs/orchestrator/inbox/2026-04-29_2130_plan_range-rings-next-ui-label-autocreate-drag.md`
