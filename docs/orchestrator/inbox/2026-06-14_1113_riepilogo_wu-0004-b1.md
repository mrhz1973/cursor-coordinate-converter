# Riepilogo — WU-0004 B1 mutua esclusione rimossa

**Data:** 2026-06-14  
**Tipo:** riepilogo  
**WU:** WU-0004 — Toggle basemap / SonarChart indipendenti  
**Blocco:** B1  
**Stato blocco:** PASS  
**Stato WU:** OPEN (B2/B3 pending)

---

## Commit runtime

`0cd3c8c` — `feat(gis): remove Navionics/SonarChart mutual exclusion (WU-0004 B1)`

File: `coordinate_converter Claude.html` (−15 righe)

---

## Punti mutua esclusione rimossi

1. **`setMapLayer()`** — non spegne più `showSonarChart` quando si seleziona basemap `nav`.
2. **Handler toggle SonarChart** — non cambia più `mapLayer` da `nav` a `_lastBaseLayerNonNav`/`osm`.
3. **Init post-load** — rimosso seed `_lastBaseLayerNonNav`.

---

## `_lastBaseLayerNonNav`

Rimosso completamente: grep confermava zero lettori reali dopo B1.

---

## Persistenza

Invariata (`saveStore` / `loadStore` — `mapLayer` e `showSonarChart` indipendenti).  
Reload conserva combo **`nav` + SonarChart** entrambi attivi.

---

## Validazione statica

- `git diff --check`: pulito
- `node --check`: exit 0 su entrambi i blocchi script inline

---

## Test manuale locale

**PASS**

- Combo Navionics basemap `nav` + SonarChart overlay attivi insieme
- Reload conserva entrambi
- Seamarks indipendente

---

## Non implementato in B1

- B2 toggle mostra/nascondi basemap
- B3 i18n + QA VPS

---

## Rischio noto

Doppio-fetch by-design quando basemap Navionics (`/tiles/`) e SonarChart overlay (`/sonar/`) sono entrambi attivi.

---

## Prossimo passo

**B2** — toggle mostra/nascondi basemap + wiring render/hydrate.
