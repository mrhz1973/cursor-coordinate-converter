# P-UI-UNIFORM — layout pannello Poligoni (runtime HTML/CSS)

**Data:** 2026-06-25  
**Tipo:** runtime UI + finito sessione + autosync orchestratore

## Commit task (runtime)

- **SHA:** `e0e9578d317cdf7e0662400fb4a64425c33efa47`
- **Subject:** `refactor(gis): align polygon panel with standard modal layout (P-UI-UNIFORM)`
- **File:** `coordinate_converter Claude.html` (+121/−5)
- **Blob monolite:** `edd4b973bd18719ef5b55a517b2e04f79490d679`

## Cosa è stato fatto

- Allineamento `#polygonPanel` al pattern modal GIS flottanti (famiglia Range & Bearing / Misura).
- Corpo scrollabile `.polygon-panel-scroll`; footer stabile `.app-modal-foot.polygon-panel-footer`.
- Flex column su `#polygonPanelBody`; overflow interno; footer con bordo superiore.
- GIS mode floating CSS + anchor resize handle `#polygonPanel`.
- Gerarchia pulsanti: `Nuovo poligono` e `Chiudi poligono` → `btn-primary`; export secondari; `:has()` demote primary footer in edit/rename/delete.
- **JavaScript byte-invariato**; sanitizer/CRUD/storage invariati; `APP_BUILD_ID` `B5.5Z` invariato.

## Verifiche tecniche

- `git diff --check`: PASS
- `node --check` su JS estratto: PASS
- Smoke visivo browser: **non disponibile** in sessione Cursor

## Non eseguito

- Deploy VPS
- QA operatore
- Review Claude (NON RICHIESTA)

## Stato blocco

- **P-UI-UNIFORM:** runtime pubblicato; **non CLOSED end-to-end** (deploy/QA pending)
- **P-UNITS / P-VERTEX-MODAL / P-STYLE:** non iniziati
- **P5 / batch:** non chiusi

## Prossimo passo

Deploy GIS-only + QA operatore minima su layout pannello Poligoni (stati lista/drawing/edit/mobile).
