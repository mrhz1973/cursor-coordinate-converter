> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-07 — **`finito` Regola H** dopo **`QA CARTO-ARCHIVE-MATCH-A-FIX2 PASS operatore`**. Commit task docs **`1bd20f6`** (`docs: finito CARTO-ARCHIVE-MATCH-A after Regola H QA PASS`). Blocco **CARTO-ARCHIVE-MATCH-A (+ FIX1–FIX2) CLOSED / PASS end-to-end**. Runtime tip **`c4d7db5`** / build **135** (già su `main` e VPS). Working tree post-task pre-autosync: **pulito**. Monolite **nel tip runtime** (`c4d7db5`), **non** nel commit docs finito. Inbox: `docs/orchestrator/inbox/2026-08-07_0035_riepilogo_finito-sessione.md`.

2026-08-06 — **`DOCS-CARTO-IGM-CRS-AUDIT-A-CLOSE` CLOSED / PASS docs-only** (storico; task `db2d430`; tip allora `51e0f5b`).
