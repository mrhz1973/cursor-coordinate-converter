# Riepilogo — WIKI-LLM-LEAN-CONSOLIDATION-D

**Data:** 2026-08-16 · **Commit task:** `cb0bd07` — push OK pre-autosync.

## Cosa

1. Creato `docs/FRONTIER.md` = unica LIVE STATE (tabella da OM §7.1 invariata).
2. OM §7.1 → pointer soltanto.
3. CORE BOOT AI-BOOT → FRONTIER; registry `omitted`/`plain`/`thin`; discovery 0 se fetch_file già disponibile.
4. CBG + CONNECTOR-DISCOVERY-HARD-GUARD + TOOL-PAYLOAD fail-closed; `agg` refresh minimo.
5. Pointer minimi rule 00 / 30.

## Static cold boot (documentale)

```text
schema discovery: 0 ideale / max 1 (omitted→fetch_file)
GitHub tools: fetch_file only
payloads: AI-BOOT range (~54) + FRONTIER full (14) + WU hot-header (~10) ≈ 75–80 righe
search/dir/OM§4/roadmap/WU body/§7.2–7.3/report/monolite: 0
fetch_file: 3 · discovery: 0–1
```

## Non toccato

Monolite · WU · roadmap · QA-CHECKLIST · HANDOFF · gate/NEXT APP GIS.
