> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-07 — **`finito` Regola H** dopo **`QA CARTO-IGM-AREA-ESC-RESTORE-A PASS operatore`**. Commit task docs **`b0a60e0`** (`docs: finito CARTO-IGM-AREA-ESC-RESTORE-A after Regola H QA PASS`). Blocco **CARTO-IGM-AREA-ESC-RESTORE-A CLOSED / PASS end-to-end**. Runtime tip **`764e661`** / build **136** (già su `main` e VPS). Working tree post-task pre-autosync: **pulito**. Monolite **nel tip runtime** (`764e661`), **non** nel commit docs finito. Inbox: `docs/orchestrator/inbox/2026-08-07_0120_riepilogo_finito-sessione.md`.

2026-08-07 — **`finito` ARCHIVE-MATCH** (storico; task `1bd20f6`; tip allora `c4d7db5`).
