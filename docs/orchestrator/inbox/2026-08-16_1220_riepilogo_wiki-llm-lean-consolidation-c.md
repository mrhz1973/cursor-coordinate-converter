# Riepilogo — WIKI-LLM-LEAN-CONSOLIDATION-C (TURN-GROWTH)

**Data:** 2026-08-16 · **Task:** governance/docs-only · **Commit task:** `1f2e8b0` (`docs: reduce per-turn context with prompt delta`) — push riuscito pre-autosync.

## Cosa è stato fatto

1. **METHOD-CURSOR-PROMPT-DELTA** in OM §4: prompt GPT→Cursor = TASK DELTA + profilo `CLOSURE: STANDARD_RUNTIME_BUNDLE`; metodo stabile applicato da Cursor/repo.
2. **Coda runtime** preservata come comportamento normativo unico; GPT **non** la reinietta; Cursor la applica da OM §4.
3. **TOOL-PAYLOAD-GUARD** estende CBG (con CONNECTOR-SCHEMA-GUARD).
4. Alias **`agg`** (≠ `aggio`) formalizzato in OM §4/§6 + 1 riga README AI-BOOT.
5. **rule 30**: ricezione TASK DELTA default; RIEPILOGO/autosync/session guard/remote PASS preservati.

## Non toccato

Monolite · OM §7 · WU · roadmap · gate/NEXT APP GIS · deploy/QA.

## Finding residuo (fuori scope)

`docs/QA-CHECKLIST.md` § Template coda ancora dice «GPT incolla questa coda» — allineare in follow-up docs.

## Prossimo passo

Nessuno per governance. APP GIS resta sul gate corrente (non preso in carico).
