> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-12 — **`AUTOMATED BROWSER QA D-FLIGHT-F PASS`**. ACL `:8010` OK. Runtime GIS **`ddce434` / D-FLIGHT-F-FIX1 / 162**; helper **0.1.2**. Carica zone + headers X-GOI + OPSEC + force-offline + Rivaluta + READY_CHANGED/Apply PASS. **No** QA operatore. **No** `finito`. Dettaglio: [`docs/orchestrator/inbox/2026-08-12_1340_riepilogo_d-flight-f-automated-browser-qa-pass.md`](inbox/2026-08-12_1340_riepilogo_d-flight-f-automated-browser-qa-pass.md). NEXT: QA umana operatore → riga `QA D-FLIGHT-F PASS operatore` → coda `finito` Regola H.
