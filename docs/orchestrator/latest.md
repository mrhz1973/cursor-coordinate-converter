> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-05 — **docs-only backlog** `MAP-BOX-ZOOM-A` + `CARTO-INDEX-FEDERATED-A` registrati (NON APERTO). Commit task **`b737d5c`** (`docs(backlog): add map box zoom and federated chart index`). Fonte primaria: roadmap WU-0005-0009. Puntatori sintetici OM §7 + HANDOFF. Runtime tip live **invariato** **`f718582`** / build **115**. Monolite **non** modificato. Nessuna WU runtime aperta. Nessun deploy/QA. Working tree post-task/pre-autosync: **pulito**. Inbox: `docs/orchestrator/inbox/2026-08-05_1210_backlog_map-box-zoom-carto-index.md`.

2026-08-03 — **`finito` Regola H** ROUTING-ANELLO-A (+ FIX1). Commit task docs **`30063e0`**. Runtime tip **`f718582`** / build **115**.

2026-08-03 — **`finito` Regola H** ROUTING-ACTION-ROW-UX-A. Commit task docs **`4d27463`**. Runtime tip storico **`dde5156`** / build **113** (superseded live).
