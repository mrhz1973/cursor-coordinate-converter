> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-12 — **`D-FLIGHT-A PLAN COMPLETE — NO PRODUCT DECISION REQUIRED`** (piano read-only pubblicato). Raccomandazione tecnica **A3-light** (core comune + adapter WFS + adapter ED-269/318). Categoria futura **ROUTINE**. Zero stringhe UI; expose previsto `window.GOIDflight` per Automated Browser QA; confine A vs B netto (intermediate form); contratto helper `/dataset` documentato. Monolite **`ac3a0ea` / build 157** invariato. Helper H2 **CLOSED** (`bc80604`). WU-0013 NEXT: implementazione runtime **`D-FLIGHT-A`** (prompt esplicito). Inbox: [`docs/orchestrator/inbox/2026-08-12_0028_plan_d-flight-a.md`](inbox/2026-08-12_0028_plan_d-flight-a.md).
