# GIS Tool — OPERATING_MEMORY

> Gli agenti devono leggere questo file prima di modificare il GIS Tool.  
> Read-set operativo: `README.md` (bootloader) → `docs/OPERATING_MEMORY.md` (§7 stato vivo) → `docs/work-units/WU-0005-0009-roadmap.md` (piano/backlog).  
> Questo file riguarda il **GIS monolite**, non il control-plane e non Planet-Clone.

---

## 1. Identità progetto

- **Repo:** `mrhz1973/cursor-coordinate-converter`
- **File operativo:** `coordinate_converter Claude.html`
- **Tipo:** app GIS tattica leggera, offline-first, OPSEC-aware

---

## 2. Vincoli architetturali

- Singolo file HTML standalone
- HTML / CSS / JS nello stesso file
- Vanilla JS
- No framework
- No TypeScript
- No npm
- No bundler
- No ES modules
- No split operativo
- Nessuna dipendenza runtime esterna salvo tile/geocoding opt-in o cache offline

---

## 3. Read-set operativo

**Read-set corrente (wiki-LLM lean):** gli agenti devono leggere, in ordine:

1. `README.md` — bootloader e regole di lettura; **non** stato operativo vivo.
2. `docs/OPERATING_MEMORY.md` — stato operativo vivo, soprattutto **§7**.
3. `docs/work-units/WU-0005-0009-roadmap.md` — piano, backlog e workstream WU-0005→0009.

**Precedenza:** se README, OPERATING_MEMORY e roadmap confliggono, segnalare il conflitto e usare il documento più specifico e più recente.

**Legacy (non memoria corrente):** `docs/checkpoint.md`, `docs/session-geolocalizzazione-e-mappa.md`, `docs/orchestrator/latest.md`, `docs/orchestrator/chatgpt-checkpoint.md` e WU chiuse (WU-0001→0004 salvo richiamo esplicito dalla roadmap viva) — consultabili per audit, **non** come current-state primario. Il **`finito`** può continuare ad appendere checkpoint/session come storico/audit. In conflitto con OM §7 o roadmap → segnalare e dare precedenza ai documenti vivi; **non** riscrivere log storici già pushati salvo richiesta esplicita.

**Verifica remoto / cache RAW:** dopo un push, `raw.githubusercontent.com` può servire contenuto cache per alcuni minuti. Per verifiche immediate usare `git fetch && git log origin/main`, oppure URL RAW con query cache-bust, oppure attendere propagazione. Non considerare una singola lettura RAW immediata come prova negativa definitiva se `git log origin/main` mostra il commit atteso.

---

## 4. Protocollo orchestratore minimo

- ChatGPT e Cursor usano lo stesso read-set target: README (bootloader) → OPERATING_MEMORY §7 → WU-0005-0009-roadmap.
- Prompt Cursor: istruzioni esterne fuori dal prompt; blocco operativo pulito dentro il prompt.
- Procedere per **bundle** coerenti (default METHOD-BUNDLING-DEFAULT); non frammentare il lavoro routine in micro-blocchi separati salvo categorie delicate (OM §4 Regola G).
- Non toccare aree non correlate.
- `finito` è un workflow interno a Cursor, **non** un comando PowerShell che l'operatore esegue a mano; nei prompt **bundle** runtime la coda `finito` è **pre-autorizzata** e si **innesca automaticamente** dalla riga `QA <BLOCK-ID> PASS operatore` (Regola H); resta manuale/non automatico per eccezioni in Regola A.
- Nessun GPS silenzioso.
- Nessun live tracking GPS senza decisione esplicita.
- Modifiche runtime: commit separati — codice / docs operative / autosync.
- Blocchi docs-only: non toccare il monolite.
- **Session / repo guard:** prima di patch non read-only, verificare repo root, branch e `git status --short`; se workspace atteso pulito risulta sporco all’avvio o repo/cartella non coerenti, fermarsi e riportare lo stato. Cursor non decide autonomamente se procedere; la decisione spetta alla review.
- **Remote hash / PASS tecnico:** dopo push, il PASS remoto richiede output verbatim coerente di `HEAD`, tracking locale `origin/main` e `git ls-remote origin main`; l’autorità finale è `git ls-remote`, mentre RAW GitHub è secondario/best-effort e può essere stale. Se `origin/main` locale diverge da `ls-remote`, non è PASS. Se gli output mancano o sono ambigui, prima prompt Cursor verify-only; shell manuale utente solo fallback finale. Distinto da PASS operatore / QA runtime.
- **QA evidence / PASS operatore:** il PASS operatore è distinto dal PASS tecnico remoto e richiede attestazione esplicita nel flusso da utente/operatore o orchestratore. Cursor non può inferirlo da PASS tecnico, diff pulito o `node --check`. In assenza di attestazione, default fail-closed: QA operatore non eseguita/non attestata. Se la QA è attestata, RIEPILOGO e docs devono registrarne provenienza, esiti concreti, ambiente essenziale e limiti.
- **LAST_CURSOR_REPORT (Fase F3):** da Fase F3 `docs/runtime/LAST_CURSOR_REPORT.md` è **obbligatorio** post-push per task reale GIS-only; non per read-only/plan/review diff senza commit; evidenza rolling post-push, non fonte viva primaria — OM §7 e roadmap restano primari; mapping: commit principale = task, autosync = report, nessun terzo commit/finalize-hash. **Home canonica dettagliata:** `.cursor/rules/30-output-workflow.mdc` e `docs/runtime/LAST_CURSOR_REPORT.template.md`. **`real_task_commit`** = anchor stabile del blocco; il container corrente resta **`PENDING_SELF_REFERENCE`** (non sostituirlo nel commit corrente); il report **non** attesta il proprio HEAD finale — HEAD e PASS remoto si provano **esternamente** con `git ls-remote origin main` e seed Regola F; **vietati** amend self-reference, terzo commit e finalize-hash; backfill dei container precedenti **solo** in HISTORY di un report successivo. **Ambito esteso:** la stessa disciplina F3 (container corrente, HEAD finale esterno, anti-ricorsione, anti-terzo-commit, backfill differito) vale anche per `docs/orchestrator/inbox/**` e `docs/orchestrator/latest.md` contenuti nel **commit autosync corrente**: lo SHA dell’autosync corrente, l’esito del suo push e l’HEAD finale sono **`EXTERNAL_ONLY`**; **non** autorare campi post-push «da creare/da verificare» destinati a un terzo commit; **vietati** i commit «completa inbox» e «finalize autosync»; *published = immutable* per l’intervento corrente. Home canonica dettagliata: `.cursor/rules/30-output-workflow.mdc`.

### Handoff & Close Discipline — minimizzazione copia-incolla

Disciplina di handoff e chiusura blocco orientata a ridurre il copia-incolla manuale tra Cursor, GPT e Claude. Sostituisce integralmente ogni precedente catena fissa di revisione tra GPT, Claude e Cursor.

**Regola A — `finito` condizionale nel prompt (bundle: pre-autorizzato).** Ogni prompt Cursor **bundle runtime** già approvato include in coda la clausola `finito` **pre-autorizzata** (vedi **Regola H** e *Template coda prompt bundle* sotto). Per blocchi non-bundle o senza deploy/QA bundle, la clausola classica resta:

> Se tutti i controlli statici risultano PASS e il diff resta nello scope dichiarato, esegui il workflow `finito`. Se un controllo fallisce o il diff esce dallo scope, NON eseguire `finito`: fermati e riporta il problema.

Il workflow `finito` resta **manuale o non automatico** per: diagnosi; attività read-only; blocchi delicati in attesa di review byte Claude (se richiesta e non ancora completata); review sostitutiva GPT non ancora loggata (bundle delicato); QA visiva pre-registrazione; errori; scope drift; workspace sporco; repository o branch incoerenti; **deploy non eseguito**; **smoke fallito**; prompt che **non** ha autorizzato esplicitamente la coda `finito`. `finito` è un workflow interno a Cursor, **non** un comando PowerShell da far eseguire all'operatore — e **non** un secondo giro separato dopo QA PASS di un bundle autorizzato.

**Regola B — Review tiered (a livello BUNDLE).** La review graduata sostituisce integralmente la disciplina precedente. Il gate (review, deploy, QA) vale per **intero bundle**, mai per singolo item. Vedi anche **Regola G — Bundling di default**.

- **Bundle ROUTINE** (mega-bundle: CSS, HTML, attributi, i18n, UI, cosmetico, Ramo A, JS a basso rischio che **non** tocca categorie delicate): flusso `GPT prepara il prompt completo → Cursor esegue → controlli statici → deploy → QA bundle → attestazione QA PASS → finito automatico` (Regola H). **Nessun hop Claude in nessun caso** — vai sempre, zero attese.
- **Bundle DELICATO** (sanitizer/whitelist, OPSEC, rete/tile/proxy, cache/storage, nuovo campo persistito, nuovo create-path, lifecycle modale/dialog area −/× — possono stare insieme tra loro, **mai** nel bundle routine): Claude **upstream** (sostanza, rischi, gate) → GPT redige prompt → Cursor implementa → Claude **downstream** verifica diff **intero bundle** da `raw@FULL_SHA` (**una** review) **prima** del deploy, se Claude **disponibile**.
- **Bundle DELICATO, Claude NON disponibile** (limite token / attesa inaccettabile): il deploy **non** si blocca. Procedere con **review sostitutiva GPT** — valida **solo** se esegue esplicitamente la checklist per-categoria da `raw@FULL_SHA` (non un «PASS» a occhio) + QA operatore della categoria + review byte Claude **post-hoc** come backstop (rollback/fix-forward se finding; build bump + git rendono il rollback pulito). Etichettare «review sostitutiva GPT», **mai** «Claude», e loggarla nel report. Una sostitutiva dichiarata senza eseguire i check è errore di gate documentato (es. Help/QR).

In entrambi i tier: Claude **non** scrive il prompt Cursor; il prompt Cursor resta responsabilità di GPT.

**Regola G — Bundling di default (METHOD-BUNDLING-DEFAULT).** Sostituisce ogni default precedente di separazione per-blocco/micro-blocco.

1. **Default operativo = BUNDLE:** raggruppare il lavoro in **un** blocco / **un** commit / **una** QA. Target **≥5 item** per bundle; 5–10+ è normale; nessun limite superiore rigido se il bundle resta coerente. **Un solo gate per bundle:** una review, un deploy, una QA — mai per singolo item. L'operatore **accetta** esplicitamente rollback/debug più grezzo sui bundle routine; **non** sollevare obiezioni di granularità sul routine. Scopo: ridurre cerimonia per-microblocco, aumentare velocità operatore.

2. **Mega-bundle ROUTINE** (libero, 5–10+ item): CSS, HTML, attributi, i18n, UI, cosmetico, Ramo A, JS a basso rischio che **non** tocca le categorie delicate sotto.

3. **Categorie delicate** — isolate in bundle proprio (mai mischiate nel routine; possono stare insieme tra loro): sanitizer/whitelist, OPSEC, rete/tile/proxy, cache/storage, nuovo campo persistito, nuovo create-path, lifecycle modale/dialog (area −/×). Motivo: non è fissazione di granularità — è velocità operatore. Un bug delicato sepolto in un mega-bundle blocca l'**intero** bundle dal deploy (più lento, non più veloce). Isolare le delicate è l'unica granularità che fa risparmiare tempo.

4. **Precedenza:** questa regola sostituisce ogni default precedente di separazione per-blocco. Separare resta consigliato **solo** per le categorie delicate elencate. Per routine UI/CSS/HTML/i18n/cosmetica/JS basso rischio, default = **bundling**.

**Checklist sostitutiva GPT obbligatoria** (da `raw@FULL_SHA`, bundle delicato, Claude non disponibile):

- **Lifecycle modale/dialog (−/×):** apertura context-aware per **ogni** dialog toccato `[if(isGis)dlg.show();else dlg.showModal();` + `aria-modal=isGis?"false":"true"`]; close per-dialog con id specifici, **nessun** `querySelectorAll` globale; markup close = `.app-modal-close` esistente (`type="button"`, glifo via `::before`, niente SVG/formmethod); CSS legacy non rimossa se condivisa; QA: ogni modale in GIS (mappa/pannelli interattivi, niente inert, −/×/minimize/modal vertice ok) + fuori GIS (backdrop).
- **Sanitizer/whitelist, nuovo campo persistito, nuovo create-path, storage:** estensione whitelist scoped (quali kind); nessun type-check allentato (`typeof x==="number"&&isFinite`, mai coercion lasca); il dato passa **sempre** dal sanitizer esistente, nessuna scrittura diretta; regressione round-trip **obbligatoria** save→reload→export→import su Tracce **e** poligoni. Bug **silenti** (non visibili in QA, corrompono dati/export) → categoria più rischiosa da sostituire: se grosso/dubbio preferire attesa Claude; se piccolo e checklist pulita, procedere.
- **Rete/tile/proxy/OPSEC:** nessun endpoint/chiamata esterna nuova; offline ancora funzionante. OPSEC = massima cautela, preferire attesa se non banale.

**Regola H — QA-PASS AUTO-INNESCA FINITO (METHOD-QA-PASS-AUTO-FINITO).** Elimina il giro separato «QA PASS → ChatGPT dice ora lancia finito».

1. **Nei prompt bundle runtime**, la coda `finito` è **pre-autorizzata** nel prompt stesso (template *Coda prompt bundle* sotto).
2. **Trigger:** la riga di attestazione operatore esatta `QA <BLOCK-ID> PASS operatore` (stesso `<BLOCK-ID>` del bundle).
3. **Quando Cursor riceve quella riga**, se il prompt bundle prevedeva la coda, il **deploy tecnico è PASS**, nessuna eccezione attiva (Regola A) e la review richiesta (se bundle delicato) è già completata e loggata, Cursor **esegue automaticamente** senza chiedere un comando separato:
   - chiusura docs `OPERATING_MEMORY.md` §7;
   - aggiornamento roadmap/work-unit se previsto;
   - aggiornamento `docs/QA-CHECKLIST.md` / `docs/HANDOFF.md` se previsto;
   - autosync orchestratore (`latest.md` + `inbox` + `LAST_CURSOR_REPORT.md` se task reale);
   - commit/push selettivi;
   - verifica `HEAD` = `origin/main` = `git ls-remote origin main`;
   - workspace pulito;
   - conferma monolite invariato se la chiusura è docs-only.
4. **Non significa saltare la chiusura.** La chiusura docs resta **obbligatoria**. OM §7 deve restare fresco per la chat successiva. Saltare la chiusura dopo QA PASS = OM §7 stale = **errore di metodo**.
5. **GPT / orchestratore:** **non** emettere messaggi separati del tipo «ora esegui finito», «ora fai la chiusura docs», «ora lancia finito» dopo QA PASS di un bundle con coda pre-autorizzata.
6. **Bundle ROUTINE:** regola applicata normalmente; un solo gate; nessun hop Claude.
7. **Bundle DELICATO:** **non** auto-innescare `finito` prima della review byte Claude se richiesta; se Claude non disponibile e il metodo consente review sostitutiva GPT → applicare solo **dopo** review sostitutiva completata e loggata, deploy PASS e QA operatore PASS della categoria.

**Regola C — Report a un solo destinatario.** Blocco delicato → report Cursor destinato a **Claude**; blocco di routine → report nel flusso Cursor/GPT. Il destinatario va **dichiarato nel prompt**. Non duplicare lo stesso report verso più destinatari; l'operatore non ricopia lo stesso riepilogo tra GPT, Claude e Cursor salvo escalation reale.

**Regola D — QA operatore unica (minima narrativa di default).** La QA operatore è fornita come **un'unica** emissione copiabile, **già compilata** con i dati noti, e **restituita una sola volta** dall'operatore. **Formato predefinito** (blocchi di routine, bundle ROUTINE): **QA minima narrativa** — breve riepilogo PASS tecnico; frase «Ora serve solo la QA operatore minima, senza Cursor»; URL VPS con runtime short SHA; pochi passaggi concreti; pochi esiti specifici; risposta attesa `QA <BLOCK-ID> PASS operatore` oppure errore esatto e punto. **Quella riga PASS** (in un prompt bundle con coda pre-autorizzata) è il **trigger automatico** del workflow `finito` (Regola H) — Cursor **non** deve attendere un secondo messaggio «finito». **Checklist estesa** (caselle, sette categorie, campi strutturati): solo per OPSEC, rete/tile/proxy, cache/storage, migrazioni, architettura, diff multi-area, alto rischio o richiesta esplicita — vedi [`docs/QA-CHECKLIST.md`](QA-CHECKLIST.md). Non usare come modalità ordinaria: domande QA singole successive; round multipli PASS/FAIL; checklist frammentate; audit generale non pertinente. Fonte canonica: [`docs/QA-CHECKLIST.md`](QA-CHECKLIST.md). Formato URL: `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=<short-sha-runtime-reale>` (short SHA del commit **runtime** reale; mai docs/autosync; mai etichette `*-local` sul VPS). Cursor prepara la QA ma **non** attesta la QA visiva; senza risposta esplicita dell'operatore resta **QA pending** (fail-closed).

**Regola E — Tutto copiabile e fenced.** Questi artefatti vanno forniti ciascuno dentro **un unico fenced code block** contiguo: prompt Cursor; workflow/comando `finito` quando fornito separatamente; URL QA; checklist QA; seed handoff; sostanza Claude → GPT. Ogni blocco: completo; selezionabile in un'unica operazione; senza testo estraneo all'interno; non frammentato inutilmente. I prompt Cursor usano i delimitatori `=== INIZIO PROMPT CURSOR ===` / `=== FINE PROMPT CURSOR ===`. Le indicazioni per l'operatore (modalità Cursor, AI consigliata, documenti da allegare, azioni successive) restano **fuori** dal prompt.

**Regola F — Seed handoff minimo e freschezza.** Dopo la pubblicazione, `finito` emette in un fenced code block:

> `HEAD verificato (ls-remote) @ <timestamp> = <full-sha-post-finito> · frontiera = <block-id> (<data>)`

con anche un pointer sintetico al read-set del README. `git ls-remote origin main` è **autorità finale**; RAW main, blob/main e snapshot connector sono secondari e possono essere stale; il blob SHA di un singolo file **non** prova HEAD. Il lettore successivo legge il read-set pinnato allo SHA del seed; mismatch tra frontiera dichiarata e frontiera letta → **STOP fail-closed**. Un handoff prodotto da un attore non capace di verificare `ls-remote` è provvisorio e non azionabile. Le procedure complete vivono nel repository e non vanno reincollate integralmente tra chat; il seed post-push usa il **nuovo** SHA verificato sul remoto, mai automaticamente lo SHA iniziale del task.

### Chiusura blocco (dopo l'esecuzione Cursor)

- Verifica esito: diff, controlli automatici pertinenti e gate OPSEC
  mirato se il blocco tocca rete, tile, proxy, cache, storage o fetch.
- Commit e autosync chiusi nello stesso intervento operativo, ma con
  commit separati e selettivi:
  - commit codice/runtime se il monolite o altri file operativi sono
    stati modificati;
  - commit docs operative se OPERATING_MEMORY §7 o roadmap cambiano stato/piano;
  - commit README **solo** se cambia read-set/boot procedure (non a ogni blocco runtime);
  - commit autosync memoria orchestratore per latest.md + inbox/.
- Aggiornare OPERATING_MEMORY §7 quando cambia lo stato operativo; roadmap quando cambia piano/backlog; README solo se cambia boot/read-set.
- Nessun blocco operativo è chiuso finché non risulta pubblicato
  l'autosync orchestratore pertinente.
- "Pubblicato" significa pushato su `origin` e verificato sul remoto,
  non solo committato in locale.
- Verifica del push: `git log origin/main` dopo `git fetch`, oppure
  lettura del file su GitHub a prova di cache. Una sola lettura raw
  immediata non è prova: il CDN può servire contenuto cachato per
  alcuni minuti.

### Sequenza blocco runtime GIS

1. implementazione in Cursor;
2. QA pre-commit a schermo sul file locale (`coordinate_converter Claude.html`), opzionale;
3. `finito` in Cursor → commit selettivo + push `origin`;
4. **deploy VPS** necessario per QA/uso su tailnet — **modalità deploy:**
   - eseguito da **Cursor via SSH**, alias `ionos-n8n`, in **un unico prompt** (non far digitare comandi manuali all'operatore per deploy ordinario);
   - il prompt deploy deve coprire: `git pull origin main`; `systemctl restart goi-gis-app`; smoke HTTP (status, Content-Length, build label);
   - clone GIS VPS: `/root/local-files/handoff-runtime/cursor-coordinate-converter`;
   - **GIS-only** per blocchi GIS-only; Planet-Clone/proxy **solo** se cambia il proxy;
   - riferimento: [`docs/runtime/VPS_DEPLOY_RUNTIME.md`](runtime/VPS_DEPLOY_RUNTIME.md);
5. **QA operatore** sull'app deployata — **condotta QA:**
   - visiva, nel browser; include: caricare l'app; guardare layout/overlay; scaricare/aprire il JPG; cliccare i dialog; provare finestra stretta;
   - la esegue l'**operatore**; Cursor **non** sostituisce la QA visiva (non apre l'app per attestare, non carica tile come operatore, non scarica immagini al posto del giudizio operatore); l'advisor AI non raggiunge il VPS e non inventa esiti — instradare la QA operatore su Cursor è errore noto;
   - l'orchestratore (o Cursor a chiusura blocco) fornisce **un'unica** **QA minima narrativa** di default — già compilata con i dati noti (vedi [`docs/QA-CHECKLIST.md`](QA-CHECKLIST.md) e *Handoff & Close Discipline*, Regola D); checklist estesa solo per blocchi delicati/complessi; l'operatore risponde **una sola volta** con `QA <BLOCK-ID> PASS operatore` oppure errore esatto e punto; niente domande singole successive, round multipli o checklist frammentate come modalità ordinaria;
   - tailnet `:8000`; cache-buster con hash runtime reale: `?v=<hash runtime>`; **non** usare etichette `*-local` per QA su VPS;
   - attestazione onesta: PASS copre solo ciò che l'operatore ha guardato; sotto-check non eseguiti = limite dichiarato; esito mai inventato da AI;
6. **registrazione in OM §7:** hash runtime, HEAD deploy, smoke, link QA, esito PASS/FAIL operatore — **verifica pubblicazione / published = verified:**
   - dopo ogni `finito`/push e dopo ogni deploy, la chiusura **non** basa solo su self-report Cursor; Cursor dichiara, la prova è su **origin**, indipendente;
   - autorità: `git ls-remote origin main` (arbitro del ref); lettura SHA-pinned / raw vincolato al commit (non `main` mutevole); per docs delicati, confronto blob SHA del file vs commit precedente per provare byte-identità delle parti non-target (es. mega-bullet §7, B5.5A);
   - post-deploy VPS: byte-match Content-Length servito su `:8000` vs `wc -c` del file allo stesso commit su origin — conferma che il VPS serve quel commit, non clone stale;
   - motivo: in sessione Cursor ha riportato hash/repo errati; ref/blob/byte su origin è l'arbitro.

**Nota chiave:** push su GitHub ≠ app aggiornata. `:8000` mostra solo ciò che il clone VPS ha pullato.

### Ruolo Claude (consigliere) — limiti

- Claude NON scrive prompt per Cursor. Mai. Nemmeno comandi git, nemmeno "una riga".
- Claude lavora solo a monte (imposta i task per GPT) e a valle (legge gli esiti su origin e dà verdetti/critiche).
- I prompt per Cursor li scrive sempre GPT.
- Se Claude sta per produrre testo destinato a Cursor, deve fermarsi e passare la sostanza a GPT, non il prompt.

### Comandi all'operatore — uno alla volta

- I comandi che l'operatore esegue a mano — PowerShell, git, shell, terminale — vanno dati **uno alla volta**: un singolo comando, si attende l'output, poi il successivo.
- Non dare mai blocchi di più comandi in sequenza da copiare insieme quando l'esecuzione è manuale.
- Motivo: l'operatore li esegue manualmente; sequenze multiple causano errori pratici, per esempio copia parziale, cartella sbagliata o comando saltato.
- Prima di comandi git/percorso, indicare sempre la cartella corretta:
  `C:\Users\mrhz\Documents\AI\GitHub\cursor-coordinate-converter`
- **Eccezione:** i prompt per Cursor Agent restano **completi**. Cursor esegue il blocco intero; non spezzettare i prompt-task di Cursor.
- La regola “uno alla volta” vale solo per i comandi eseguiti a mano dall'operatore, non per i prompt-task destinati a Cursor.

### Formato prompt Cursor e blocchi sostanza — copiabili in un colpo solo

Quando ChatGPT prepara un prompt destinato a Cursor, il prompt deve essere un unico blocco contiguo, dentro un fence di codice, con delimitatori espliciti:

````text
=== INIZIO PROMPT CURSOR ===
...
=== FINE PROMPT CURSOR ===
````

Tutto ciò che Cursor deve eseguire deve stare dentro il blocco delimitato. Niente che l'operatore non deve incollare in Cursor deve stare dentro il blocco.

Le meta-istruzioni per l'operatore devono restare fuori dal blocco, sopra o sotto. Esempi:
- modalità Cursor consigliata;
- AI consigliata;
- documenti da allegare;
- cosa riportare dopo l'esecuzione;
- note operative rivolte all'utente.

L'operatore deve poter selezionare l'intero blocco e incollarlo in Cursor senza tagliare parti utili e senza includere testo estraneo.

Lo stesso formato vale per la "sostanza" che Claude passa a GPT: blocco unico, delimitato, copiabile, senza testo estraneo dentro il blocco.

### Template coda prompt bundle runtime (canonico)

**Home:** questa sezione + [`docs/QA-CHECKLIST.md`](QA-CHECKLIST.md) § *Template coda prompt bundle runtime*. GPT incolla la coda in ogni prompt bundle runtime.

````text
GATE / CHIUSURA (coda finito pre-autorizzata):
Dopo deploy tecnico PASS, fermati per QA operatore.
Quando l'operatore attesta esattamente:
QA <BLOCK-ID> PASS operatore
esegui automaticamente la coda finito già autorizzata:
chiusura docs OM §7 + roadmap/checklist/HANDOFF se previsti + autosync orchestratore + commit/push + verifica HEAD = origin/main = ls-remote + workspace pulito + conferma monolite invariato se docs-only.
Non chiedere un comando separato «finito» né attendere un secondo messaggio.
Se QA fallisce o deploy/smoke non PASS, NON eseguire finito.
Eccezioni: diagnosi/read-only; review Claude pendente (bundle delicato); review sostitutiva GPT non loggata; workspace sporco; scope drift.
````

Sostituire `<BLOCK-ID>` con l'ID reale del bundle (es. `ROUTINE-CLEANUP-BUNDLE`).

---

## 5. Modalità Cursor consigliata

- Default per blocchi docs/governance: **Agent + Auto**.
- Per blocchi runtime, architettura, OPSEC, storage, offline/cache, import/export o modifiche multi-area, la modalità viene fissata dal prompt approvato volta per volta.
- **GPT-5.5** è escalation: usarlo solo se Auto si incarta, propone scope troppo largo o il rischio è alto.

---

## 6. Alias scoped memoria GIS

- Sul repo **GIS**, **`aggio`** e **`aggio gis`** sono **equivalenti**: entrambi aggiornano la memoria operativa del repo GIS.
- Sul **control-plane** si usa **`aggio control`**.
- **Trade-off:** `aggio` secco non identifica il repo; l’operatore deve lanciarlo nel contesto/chat corretto.
- **`aggio` scoped GIS-only:** in questo repo `aggio`/`aggio gis` non significano «tutti i repo» (semantica dev-method storica); coerente con control-plane scoped `aggio control`.
- Read-set: `README.md` → `docs/OPERATING_MEMORY.md` §7 → `docs/work-units/WU-0005-0009-roadmap.md`.

**Flusso `aggio` / `aggio gis` (attivo da Fase 3):** legge/aggiorna, quando necessario:

- `README.md` solo se cambia read-set, boot procedure o regole di precedenza;
- `docs/OPERATING_MEMORY.md` §7 quando cambia lo stato operativo;
- `docs/work-units/WU-0005-0009-roadmap.md` quando cambia piano/backlog/workstream;
- eventuale autosync/inbox se il workflow lo richiede.

**Non** puntare a `docs/orchestrator/chatgpt-checkpoint.md` come fonte primaria.

---

## 7. Stato corrente

1. **PASS** — OPSEC strict cycle chiuso. Dettaglio: [`docs/work-units/WU-0001-opsec-strict-cycle.md`](work-units/WU-0001-opsec-strict-cycle.md)
2. **PASS** — Standardizzazione memoria wiki-LLM completata (Fasi 1–5). Dettaglio: [`docs/work-units/WU-0002-memory-standardization.md`](work-units/WU-0002-memory-standardization.md)
3. **CLOSED** — SonarChart overlay nel monolite. Dettaglio: [`docs/work-units/WU-0003-sonarchart-overlay.md`](work-units/WU-0003-sonarchart-overlay.md)
4. **CLOSED** — Basemap / SonarChart indipendenti. Dettaglio: [`docs/work-units/WU-0004-navionics-basemap-sonarchart-independence.md`](work-units/WU-0004-navionics-basemap-sonarchart-independence.md). B1 PASS (`0cd3c8c`); B2 rimosso per decisione (`5201ff8`); B3 decaduto con B2. Piano/backlog attivo: [`docs/work-units/WU-0005-0009-roadmap.md`](work-units/WU-0005-0009-roadmap.md).

**Backlog GIS-monolite ammesso (non infrastruttura control-plane):**

- **README bootloader rules front-load (2026-06-27, docs-only):** commit **`c409819`** — tre regole AI in cerca (`git ls-remote`/read-set pinnato; `finito` condizionale in coda prompt; Cursor write+commit = Agent+Auto); dettaglio OM §4 Handoff & Close Discipline; monolite invariato blob **`8d13e41a36fe7cc0605dc8f315eff551725340ed`**; indipendente da chiusura P-STYLE: al momento di quel commit P-STYLE era deploy **`0a51379`** PASS tecnico / QA operatore **pending**; chiusura end-to-end P-STYLE registrata in commit docs successivo dopo attestazione «**QA P-STYLE PASS operatore**».
- UX GIS / mappe offline / import-export / waypoint / tracce / poligoni
- OPSEC, cache/tile/geocoding lato app

**Backlog esplicito (prossimi candidati, non WU aperte):**

- **Default operativo runtime-first:** per i prossimi blocchi, implementare direttamente nel monolite `coordinate_converter Claude.html` e saltare i blocchi docs-only, salvo lavori davvero delicati. Restano giustificati blocchi docs/governance minimi per OPSEC, rete/tile/proxy, cache/storage, migrazioni dati o cambi architetturali. Obiettivo pratico: ridurre overhead documentale e procedere per patch runtime piccole, testate a schermo, poi commit/push.
- Piano dettagliato WU-0005→0009: vedi docs/work-units/WU-0005-0009-roadmap.md.
- **Poligoni:** fix base PASS (`72a194e`) + UX leggera PASS (`f7260d9`) — auto-arm draw mode all’apertura pannello, cancellazione con `✕` in lista, pannello minimizzato durante il disegno e ripristino dopo finish/cancel. **Backlog WU-0006 (parziale):** modifica in-place su mappa — vedi sotto POLY-EDIT-B2/B3+ e roadmap §WU-0006. Restano anche: standardizzazione modal trasversale (vedi sotto). Dettaglio: `docs/work-units/WU-0005-0009-roadmap.md` §WU-0006.
- **WU-0006 POLY-EDIT-B2 — fondazione edit poligoni (transiente) — review byte PASS; nessun deploy:** `_polyEdit` transiente; helper `polygonIsEditing`, `polygonEnterEdit`, `polygonCancelEdit`, `polygonSaveEdit`; validità/cap delegati al sanitizer via `gisFeatureUpdate` (micro-fix `0e23b42`); **nessuna UI** in B2; runtime base `9bd2e4c`/`0e23b42`; **`APP_BUILD_ID` `B5.5Z` invariato**; **non** CLOSED end-to-end B2 da solo (assorbito in catena POLY-EDIT).
- **WU-0006 POLY-EDIT-B3 — UI Modifica + overlay edit + barra Salva/Annulla — review byte PASS; QA operatore FAIL; nessun deploy:** pulsante **Modifica** in lista; barra `#polygonPanelEditBar` Salva/Annulla; overlay edit vertici visibili non interattivi; **QA operatore FAIL** — editor insufficiente vs funzione Poligono Tracce (assenza info live, nome non integrato, nessun drag); **`APP_BUILD_ID` `B5.5Z` invariato**; **non** CLOSED end-to-end.
- **POLY-PARITY-DIAG — completata read-only (2026-06-23):** mappa riuso Tracce/Misura vs poligoni GIS; piano blocchi P1–P8; nessuna modifica runtime.
- **POLY-PARITY-P1 — scheda informazioni live + nome salvabile — runtime `7a668d7`; review byte Claude PASS; nessun deploy; non CLOSED end-to-end:** `#polygonPanelEditBar` con input nome transiente, riepilogo (vertici, area, perimetro), dettaglio lati/bearing da `_polyEdit.working`; helper `gisRingToLatLonPts`, `renderPolygonEditInfo`, `polygonRecomputeEditDirty`; `polygonSaveEdit` verificato: una sola `gisFeatureUpdate(id,{geometry,properties})`, proprietà canoniche preservate, nessun timestamp/sanitizer toccato, contratto transiente B2 intatto, nessuna persistenza fuori CRUD; **`node --check` OK**; **`APP_BUILD_ID` `B5.5Z` invariato**. **QA operatore P1: FAIL** (storico pre-FIX) — FAIL UI/i18n documentati in handoff `2026-06-23_qa-fail-poly-parity-p1-handoff.md`. **Chiarimento:** poligoni visibili assenti da lista Poligoni non erano bug P1 — erano poligoni **Tracce**, non `state.gisPolygons`; separazione corretta, non fondere.
- **POLY-PARITY-P1-FIX — correzioni UI/i18n emerse dalla QA P1 — CLOSED / PASS end-to-end (scope P1-FIX):** runtime **`d30a3a8`**; deploy tecnico GIS-only PASS (HEAD VPS allineato `d52ce79`, runtime servito `d30a3a8`, byte/SHA match); fix cinque FAIL UI/i18n (i18n `defaultName`, formatter distanze Misura, barra errore vuota, hint utente, sblocco Modifica draw auto-arm); **`APP_BUILD_ID` `B5.5Z` invariato**. **QA operatore P1-FIX: PASS** — attestazione «QA POLY-PARITY-P1-FIX PASS operatore»: Modifica al primo click; nome default tradotto; fascia errore vuota assente; distanze senza `k m`; hint senza blocchi futuri; Salva/Annulla e riapertura Modifica OK. Limiti fuori scope PASS: nessun selettore unità locale; nessun drag vertici (P2); nessuna cancellazione vertice/traslazione intero.
- **WU-0006 POLY-PARITY-P2 — drag vertici poligono GIS in modalità Modifica — CLOSED / PASS end-to-end:** catena runtime P2 **`e22e40b`** (review byte Claude **FIX REQUIRED**: handle assenti da `CTRL_SEL`; `setPointerCapture` asimmetrica) → P2-FIX **`f35e4d9`** (review byte Claude **PASS** — GO DEPLOY; `.poly-edit-handle`/`.poly-edit-handle-hit` in `CTRL_SEL`; rimossa capture dal pointerdown poligono; invarianti drag/Salva confermati) → deploy VPS GIS-only **PASS tecnico** (HEAD VPS `c6bd491`, runtime servito `f35e4d9`, `goi-gis-app` active/enabled, HTTP **200**, byte **2274553**, SHA-256 **`96082c0934073e5bae9959ef46290a8173e160002e5fe4c85ac5910395e80701** match; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=f35e4d9`; solo `goi-gis-app` riavviato; warning test-harness `exit 31` non bloccante — grep globale `setPointerCapture` matcha handler legacy resize pannelli, non modulo poligono) → **QA operatore P2: PASS** — attestazione «**QA WU-0006 POLY-PARITY-P2 PASS operatore**»: drag vertice; nessun pan concorrente; area/perimetro/lati live; Annulla ripristina; Salva persiste; canonico invariato senza Salva; drag interrotto senza puntatore bloccato; Modifica riutilizzabile. Scope P2: `mapPolyEditDocDrag*`; live `_polyEdit.working`; dirty geometrico reversibile; nessun CRUD durante drag; una `gisFeatureUpdate` in Salva; **`APP_BUILD_ID` `B5.5Z` invariato**. **Nessun fix aperto su P2/P2-FIX.**
- **POLY-PARITY-P3 — cancellazione vertice in modalità Modifica — CLOSED / PASS end-to-end:** runtime base **`fc38247`** — `polygonDeleteEditVertex` su `_polyEdit.working`; deploy VPS **PASS tecnico** (byte **2281525**, SHA **`d12c9e3…`**); **QA operatore PARTIAL FAIL** storico — solo redraw post-Annulla; **P3-FIX** runtime **`6083abe`** — `polygonInvalidateEditOverlayRefresh` + `polygonSyncRenderMapAfterEditEnd` in `polygonEditCancelHandler` (pattern A2-B2-FIX); deploy VPS GIS-only **PASS tecnico** (HEAD documentale clone **`671cf30`**, runtime servito **`6083abe`**, byte **2281832**, SHA-256 **`46d265c615f31e3e685d3f72a5948f78839bacdb5a15af0895467b0efdf642f7** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled); **QA operatore P3-FIX PASS** — attestazione «**QA POLY-PARITY-P3-FIX PASS operatore**»: Modifica → eliminazione o drag vertice → Annulla — ripristino immediato geometria originale, handle assenti senza pan/zoom; Salva OK; chiusura pannello OK; min 3 vertici e drag invariati; **`APP_BUILD_ID` `B5.5Z` invariato**. P2/P7/A2 invariati.
- **POLY-PARITY-P3-ADD — inserimento vertice su un lato in Modifica — CLOSED / PASS end-to-end:** runtime **`5df925f`** — `polygonInsertEditVertex` + `polygonGeodesicMidpointLonLat` (`vincentyInverse` + `vincentyDirect` metà arco; `normalizeLon`; niente media aritmetica lon/lat); pulsante `+` su ogni riga lati in `#polygonPanelEditLegs` (cella `.poly-edit-leg-actions` con `✕` P3); midpoint geodetico; ultimo lato → append ring aperto; mutazione solo `_polyEdit.working`; gate cap **`POLYGON_RING_VERT_CAP` 500**; i18n IT/EN/FR; dirty + overlay A1 via `polygonRefreshEditUi`; una `gisFeatureUpdate` su Salva via `polygonSaveEdit`; deploy VPS GIS-only **PASS tecnico** (HEAD documentale clone **`75229c9`**, runtime servito **`5df925f`**, blob **`87aa0927282fdbfdafd3681ddb98dff35793f513`**, byte **2285990**, SHA-256 **`760772d266f7f4954c32cf583742baf50fa49b137fb6098b0464a9b58ecb0c8c** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled); **QA operatore P3-ADD PASS** — attestazione «**QA POLY-PARITY-P3-ADD PASS operatore**»: `+` lato interno e ultimo→primo; handle immediato; drag P2; info/overlay live; Annulla ripristino immediato; Salva + riapertura; insert + `✕`; IT/EN/FR; **`polygonSaveEdit`** / **`polygonDeleteEditVertex`** byte-invariati; P2/P3/P3-FIX/P7/A1/A2 invariati; **`APP_BUILD_ID` `B5.5Z` invariato**. **Nessun pending P3-ADD.**
- **POLY-PARITY-P4-B1 — traslazione intero poligono in Modifica — CLOSED / PASS end-to-end:** runtime **`505e7d0`** — modalità esplicita «Sposta poligono» (`_polyEdit.moveMode` transiente); toggle ghost/subtle con `aria-pressed` + hint i18n IT/EN/FR; pipeline dedicata `mapPolyMoveDocDrag*` separata da P2 (`mapPolyEditDocDrag*` byte-invariati); `snapshotGeo`/`snapshotPx` immutabili; ogni pointermove deriva dalla snapshot iniziale; offset totale pointer-based da anchor; traslazione in proiezione mappa (`tileMapLatLonToPx`/`tileMapPxToLatLon`); zero-salto; world wrap breve; frame invalidi fail-closed; fill interattivo solo in move mode; handle P2 visibili ma non interattivi; mutua esclusione P4/P2; pan fuori dal fill; working-copy-only; canonico invariato fino a Salva; nessun CRUD durante drag; una `gisFeatureUpdate` su Salva; cleanup su pointerup/cancel/toggle/Salva/Annulla/close; review byte Claude **PASS** — GO DEPLOY; deploy VPS GIS-only **PASS tecnico** (HEAD VPS **`505e7d0`**, runtime servito **`505e7d0`**, blob **`7dbe96ef5b68d670d647556a8883039a054d9ad4`**, byte **2294595**, SHA-256 **`2ae34929432600ac4ee75cb065b4690e3d59c408d237118dd4ee1bb4bfc6619e** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled); **QA operatore P4-B1 PASS** — attestazione «**QA POLY-PARITY-P4-B1 PASS operatore**»: toggle/hint; handle P2 non interattivi in move mode; traslazione intero poligono; assenza salto iniziale; numero/ordine vertici invariati; move mode persistente post-pointerup; pan fuori fill; P2 ripristinato post-toggle; Annulla/chiusura X/Salva/riapertura; P3 insert/delete + drag vertice; i18n IT/EN/FR; **`APP_BUILD_ID` `B5.5Z` invariato**. **Finding non bloccante (backlog tecnico separato, non urgente):** ramo pointerdown P2 in `renderPolygonEditOverlay` — guardia originale `if (mapPolyEditDocDrag) return` non più presente nella forma originale; secondo pointer touch su altro handle durante drag P2 può sostituire il primo; nessun listener duplicato né corruzione geometria/persistenza; non raggiungibile con mouse singolo; possibile micro-fix futuro: `if (mapPolyEditDocDrag || mapPolyMoveDocDrag) return`. P2/P3/P3-ADD/P7/A1/A2 invariati. **Nessun pending P4-B1.**
- **POLY-PARITY-P5-B1 / P5-B1-FIX — CLOSED / PASS end-to-end:** analisi P5 read-only completata; **Opzione A** approvata (informazioni e controllo base in creazione; geometria avanzata post-salvataggio via Modifica). **Percorso canonico invariato:** `polygonFinishDraw` → una sola `gisFeatureAdd("polygon", feature)` → `gisSanitizeFeature` → `gisSanitizeGeometry` → persistenza interna CRUD; sanitizer unico backstop; P7 `created_at === updated_at` alla creazione; **`APP_BUILD_ID` `B5.5Z` invariato**. **P5-B1** runtime **`8bc7804`** — su rifiuto sanitizer preserva draft e draw mode; errore localizzato `#polygonPanelDrawErr` (`gis.polygonPanel.drawInvalidGeometry` IT/EN/FR); retry e Annulla; una sola `gisFeatureAdd`; nessuna `gisFeatureUpdate`; nessun `saveStore` diretto; blob **`820d4d78f6d0617ba886fd228ee093ef9a0b8e64`**. **Review Claude P5-B1:** NO-GO / FIX REQUIRED (storico) — drawing auto-minimizza pannello; su rifiuto errore nel body minimizzato (draft recuperabile ma feedback invisibile). **P5-B1-FIX** runtime **`59f2bd1`** — nel ramo `if (!added)` di `polygonFinishDraw`: `polygonDrawRestoreIfAutoMinimized()` prima di `polygonShowDrawErr(...)`; solo `polygonFinishDraw` modificata; ramo successo invariato; blob **`c289f65579c450f39bd8971831ed0d8978f055ed`**. **Review byte Claude P5-B1-FIX: PASS** — GO DEPLOY; contratti verificati: rifiuto canonico senza perdita draft; draw mode preservata; pannello ripristinato prima dell’errore; errore visibile; retry e Annulla; nessuna feature fantasma; creazione valida senza regressioni; una sola `gisFeatureAdd`; nessuna `gisFeatureUpdate` post-add; nessun `saveStore` diretto; sanitizer e persistenza canonica invariati; P7 timestamp invariato. **Deploy VPS GIS-only PASS tecnico** (HEAD documentale clone **`1b0924f`**, runtime servito **`59f2bd1`**, byte **2295978**, SHA-256 **`a99654a8b23337f7f00436bee0cc110fe01fae837d2001839020574998436119** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=59f2bd1`). **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B1/P5-B1-FIX PASS operatore**»: draft preservato su rifiuto; pannello ripristinato; messaggio visibile; retry funzionante; Annulla disponibile; nessuna feature fantasma; creazione valida funzionante; nessuna regressione osservata. **Backlog P5-B2 residuo (non avviato):** `verts.length < 3` → `polygonCancelDraw()` preesistente; errore drawing stale riapertura pannello; correzione nome automatico duplicato F2; nome editabile durante disegno; info live (vertici, area, perimetro); rimozione ultimo punto. P1–P4/P7/A1/A2 invariati. **Nessun pending P5-B1/P5-B1-FIX.**
- **POLY-PARITY-P5-B2-A — toggle etichetta spostamento in Modifica — CLOSED / PASS end-to-end:** runtime **`5cc2e1b`** — `#polygonPanelEditMove` sincronizzato da `state._polyEdit.moveMode` in `renderPolygonEditBar()`; inattivo `↔ Sposta` / attivo `■ Termina spostamento` (IT); EN `↔ Move` / `■ Stop moving`; FR `↔ Déplacer` / `■ Terminer le déplacement`; chiavi `gis.polygonPanel.editMovePolygonIdle` / `editMovePolygonActive`; `textContent`, `title`, `aria-label` e `aria-pressed` coerenti; hook minimo `applyLanguage()` durante edit attivo; **`polygonToggleMoveMode` byte-invariata**; review Claude **NON RICHIESTA** (solo UI/i18n; sanitizer/create-path/CRUD invariati; nessun nuovo campo persistito). **Deploy VPS GIS-only PASS tecnico** (pull FF `1b0924f`→`5cc2e1b`, HEAD VPS **`5cc2e1b`**, blob **`306765aa06d55ebfd03928290c5702ba8b661204`**, byte **2297265**, SHA-256 **`da0c8c200b92455b0fbff253006cfd52ae1af6474fbbb85a3020cf0f453301fa** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled; 6 stringhe i18n nel body). **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B2-A PASS operatore**»: stato inattivo/attivo; secondo click termina modalità; hint visibile in move mode; reset dopo Salva/Annulla/chiusura pannello; cambio lingua IT/EN/FR con move mode attiva; nessuna regressione modifica poligono; P5-B1/P5-B1-FIX invariati; **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P5-B2-B — nomi automatici progressivi e non duplicati — CLOSED / PASS end-to-end:** runtime **`b68c774`** — helper `polygonParseAutoNameSuffix` + `polygonBuildNextDefaultName()`; base da `gis.polygonPanel.defaultName`; prossimo numero `max(suffisso numerico valido per base localizzata)+1` con loop anti-collisione; nessun `state.gisPolygons.length+1`; sequenze indipendenti per base IT/EN/FR; nome salvato in `properties.name` esistente; create-path invariato (`polygonFinishDraw` → una sola `gisFeatureAdd`); sanitizer invariato; nessun nuovo campo persistito; blob **`1d585c4fe337a5a16e8f6be8820405fefd1c276e`**. **Review Claude NON RICHIESTA.** **Deploy VPS GIS-only PASS tecnico** (pull FF `5cc2e1b`→`b68c774`, HEAD VPS **`b68c774`**, byte **2298437**, SHA-256 **`a87322edc95b142fe1164c30b6cd51eefa9201dd524a3cccf5cd9ba80614684d** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled; helper presenti nel body; vecchia espressione F2 assente). **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B2-B PASS operatore**»: nomi progressivi non duplicati; buco post-cancellazione senza collisione; sequenze per base localizzata indipendenti; nomi salvati invariati al cambio lingua; persistenza dopo reload; P5-B2-A e creazione/modifica poligoni senza regressioni; **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P5-B2-C — nome editabile durante il drawing — CLOSED / PASS end-to-end:** runtime **`d893775`** — `#polygonPanelDraftNameWrap` visibile solo in drawing; stato transiente `state._polygonDraftName`; `polygonSyncDraftNameUi()`; chiavi IT/EN `gis.polygonPanel.drawNameLabel` / `drawNamePlaceholder` (nessuna nuova stringa FR); nome manuale → `properties.name` esistente via `polygonNormalizeEditName`; vuoto/spazi → `polygonBuildNextDefaultName()`; reset draft solo su `polygonStartDraw`, `polygonCancelDraw` e successo post-`gisFeatureAdd`; ramo `!added` P5-B1: `_polygonDraftName` e vertici preservati, drawing attivo, pannello ripristinato, errore visibile, retry possibile; create-path invariato (una sola `gisFeatureAdd`); sanitizer invariato; nessun nuovo campo persistito; blob **`cc759b80f2cd691bd386066bf34429a36e82b451`**. **Review Claude NON RICHIESTA.** **Deploy VPS GIS-only PASS tecnico** (pull FF `b68c774`→`d893775`, HEAD VPS **`d893775`**, byte **2300677**, SHA-256 **`d5138ab6e2f39cfd0874f149656c1fe2be2028de61b0692fb306b419eac90e8e** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled; controllo draft e stringhe IT/EN presenti; ramo `!added` senza reset nome). **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B2-C PASS operatore**»: campo Nome solo in drawing; nome manuale salvato; vuoto/spazi → fallback P5-B2-B; reset nuovo drawing/Annulla/successo; cambio IT↔EN label/placeholder aggiornati valore preservato; nessuna stringa FR nuova; P5-B2-A/P5-B2-B invariati; nomi persistenti dopo reload; nessuna regressione creazione/modifica; **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P5-B2-D — metriche live durante il drawing — CLOSED / PASS end-to-end:** runtime **`c2c4836`** — `#polygonPanelDraftInfo` + `renderPolygonDraftInfo()`; metriche read-only da `state._polygonDraftVertices` (Vertici = `length`; Area `—` fino a 2 punti, poi `sphericalPolygonArea`+`fmtAreaPlain`; Perimetro segmento aperto a 2 punti, chiuso da ≥3 via `computePolylinePerimeterMeters`+`polygonEditFormatDistance`); chiavi metriche riusate `editVertices`/`editArea`/`editPerimeter`/`editNoData` (nessuna nuova i18n; nessuna modifica FR); `polygonFinishDraw` e ramo `!added` byte-invariati; draft non mutato nel helper; nessuna persistenza metriche; nessun timer/polling; blob **`7919e6ebce2f9671987a03c11eaa173abedc7b6b`**. **Review Claude NON RICHIESTA.** **Deploy VPS GIS-only PASS tecnico** (pull FF `d893775`→`c2c4836`, HEAD VPS **`c2c4836`**, byte **2302987**, SHA-256 **`2df56ba3f7760a750fc919907c12391cd32768665245984e7f90bfb84310746c** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled; contenitore e helper metriche presenti). **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B2-D PASS operatore**»: riepilogo solo in drawing; 0/1→`—`; 2→perimetro segmento; ≥3→area+perimetro chiuso; aggiornamento live; nascosto Annulla/successo; nuovo drawing da zero; IT↔EN preserva draft; nome P5-B2-C invariato; P5-B2-A/B/C senza regressioni; **cambio unità di misura non disponibile nel flusso generale — NON verificato in QA P5-B2-D** (intervento separato; non blocca chiusura). **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P5-B2-E — rimuovi ultimo punto durante il drawing — CLOSED / PASS end-to-end:** runtime **`aea7434`** — `#polygonPanelRemoveLastBtn` + `polygonRemoveLastDraftPoint()` + `polygonSyncRemoveLastPointUi()`; unica mutazione `state._polygonDraftVertices.pop()`; `_polygonDraftLastClick` azzerato a `null` post-rimozione (stato transiente `{ts,x,y}` per doppio click); refresh `refreshTileMapForTrackUi()` + `renderPolygonPanelList()` (overlay, metriche P5-B2-D, disabled a zero vertici); nome draft invariato; chiave IT/EN `gis.polygonPanel.removeLastPoint` (nessuna FR); `polygonFinishDraw` e ramo `!added` byte-invariati; nessuno stack undo; nessun nuovo campo persistito; blob **`8d17497a556a9be9ab8fa30c27120083e3f2ad06`**. **Review Claude NON RICHIESTA.** **Deploy VPS GIS-only PASS tecnico** (pull FF `c2c4836`→`aea7434`, HEAD VPS **`aea7434`**, byte **2304409**, SHA-256 **`6bd474632b5ddca79ace586200042d720d755fd20923f9ab02fd1d35fd9cb484** match, cmp PASS, HTTP **200**, `goi-gis-app` active/enabled; singola `.pop()` confermata). **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B2-E PASS operatore**»: pulsante solo in drawing; disabled a 0; enabled da ≥1; rimozione ultimo vertice; overlay/metriche aggiornati; nome preservato; Annulla/successo invariati; P5-B2-A/B/C/D senza regressioni; errore non nascosto da rimozione (storico pre-P5-B2-F); **cambio unità non verificato** (limitazione nota). **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P5-B2-F — pulizia errore stale dopo modifica geometrica draft — CLOSED / PASS end-to-end:** runtime **`739bf49`** — `polygonHideDrawErr()` dopo push vertice valido in handler polygon draw (`attachPanHandlers`) e dopo `.pop()` riuscita in `polygonRemoveLastDraftPoint()`; helper esistente riusato; no-op (click invalidi, rimozione a zero vertici, solo nome draft) non nascondono errore; `polygonFinishDraw` e ramo `!added` byte-invariati; sanitizer/create-path/CRUD/storage invariati; nessuna nuova i18n/HTML/CSS; diff **+2**; blob storico commit **`ba8a7f0a8edfee07dff4eb762d0a0309939db43d`**, già incluso nel monolite live **`8d13e41a36fe7cc0605dc8f315eff551725340ed`**. **Review Claude NON RICHIESTA.** **Deploy VPS già coperto** indirettamente da deploy P-STYLE-E (HEAD VPS **`0a51379`**, stesso monolite); **nessun nuovo deploy** in questo blocco docs-only. Nota OM precedente «Deploy VPS NON ESEGUITO» era **stale** — corretta qui. **QA operatore PASS** — attestazione «**QA POLY-PARITY-P5-B2-F PASS operatore**». **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P5 — creazione poligono (Opzione A) — CLOSED / PASS end-to-end:** catena **P5-B1/P5-B1-FIX** (`8bc7804`+`59f2bd1`, review Claude PASS) + **P5-B2-A…E** (runtime fino a **`aea7434`**, tutti CLOSED deploy+QA) + **P5-B2-F** (`739bf49`, CLOSED con QA operatore PASS; deploy indiretto via P-STYLE-E/`0a51379`) + **P5-B2-G covered** — ramo `verts.length < 3` → `polygonCancelDraw()` **preesistente**, irraggiungibile da UI ordinaria, documentato senza runtime aggiuntivo. **P5 complessivo CLOSED / PASS end-to-end.** **Micro-fix multi-touch P2** (`if (mapPolyEditDocDrag || mapPolyMoveDocDrag) return`) — **NON landed**; backlog tecnico separato non bloccante (finding review P4-B1); futuro blocco runtime Ramo B + review byte Claude prima deploy. Monolite autorevole **`8d13e41a36fe7cc0605dc8f315eff551725340ed`**. **Nessun pending P5.**
- **P-UI-UNIFORM — layout pannello Poligoni allineato ai modal GIS flottanti — CLOSED / PASS end-to-end:** runtime **`e0e9578`** — solo HTML/CSS `#polygonPanel` (`.polygon-panel-scroll`, footer stabile `.app-modal-foot`, flex GIS come Range & Bearing; gerarchia primary unica per stato; `:has()` demote primary footer in edit/rename/delete; anchor resize handle); body scrollabile e footer stabile; desktop/mobile e resize verificati operatore; **JavaScript byte-invariato**; sanitizer/CRUD/storage/i18n/`APP_BUILD_ID` `B5.5Z` invariati; blob **`edd4b973bd18719ef5b55a517b2e04f79490d679`**. **Review Claude NON RICHIESTA.** **Deploy VPS GIS-only PASS tecnico** (pull FF `5791e0d`→`eb7edba`, HEAD VPS **`eb7edba`**, byte **2308588**, SHA-256 **`39e05c4605f608c3cd48216ea40ba7ea0eec0f262a7c59ce398cbfddd0b4a8b1`**, blob match repo/servito/tailnet, cmp PASS exit 0, HTTP **200**, `goi-gis-app` active/enabled; solo clone GIS + restart servizio). **QA operatore PASS** — attestazione «**QA P-UI-UNIFORM PASS operatore**»: layout header/body/footer; un solo primary per stato; corpo scrollabile; footer accessibile; viewport normale e stretta; minimizzazione/chiusura/resize OK; nessuna regressione evidente su drawing, modifica, salvataggio ed export.
- **P-UNITS — unità di misura selezionabili metriche poligoni — CLOSED / PASS end-to-end:** catena runtime **`8c266ae`** (base selettori lunghezza/area, stato session-only, metriche drawing/Modifica/lati live, distanze canoniche m / aree m², nessuna persistenza feature) + **`8be2845`** (P-UNITS-FIX — rimossa `polygonSyncLengthUnitToMeasure`; unità Poligoni indipendenti da `state.mapMeasureUnit`; Range & Bearing indipendente via `rrUnit`; nessun render Misura GIS; a11y select) + **`11838a1`** (P-UNITS-RB-PARITY — lunghezza **auto/m/km/NM/mi/ft** via `formatMapMeasureDistance`; area **auto/m²/ha/km²**; Auto m/km sotto/sopra 1000 m; NM 1852 m; mi 1609.344 m; ft 0.3048 m); blob finale **`10f5f1e90a7cc9fcc4c63ea40627841878fbb378`**. **Review Claude NON RICHIESTA.** **Deploy VPS GIS-only PASS tecnico** (pull FF `8044356`→`26f73f7`, HEAD VPS **`26f73f7`**, byte **2314390**, SHA-256 **`9b9aca0c591faea2f889905eef9c8ef170a5880defae7c94153736c0bb9ff5de`**, blob match repo/servito/tailnet, cmp PASS exit 0, HTTP **200**, `goi-gis-app` active/enabled, bind `100.114.7.53:8000`; solo clone GIS + restart servizio). **QA operatore PASS** — attestazione «**QA P-UNITS PASS operatore**»: selettori Lunghezza/Area; valori auto/m/km/NM/mi/ft e auto/m²/ha/km²; metriche drawing e Modifica (area/perimetro/lati) aggiornate live; nessuna mutazione geometria/draft/dirty da cambio unità; Misura GIS e Range & Bearing indipendenti; IT↔EN; P-UI-UNIFORM preservato; nessuna regressione evidente su drawing/Modifica/salvataggio/export. sanitizer/CRUD/storage/import/export/timestamp/`APP_BUILD_ID` `B5.5Z` invariati.
- **P-VERTEX-MODAL — modifica numerica coordinate vertice in Modifica — CLOSED / PASS end-to-end:** catena runtime **`a4fa8e7`** (runtime principale — modifica coordinate vertici via modal; integrazione pipeline P2 click-vs-drag; **review byte Claude eseguita retroattivamente dopo escalation correttamente gestita = PASS**) + **`5f8f73d`** (P-VERTEX-MODAL-FIX — `ReferenceError: vtxNum is not defined` in `renderPolygonEditInfo` interrompeva loop righe «Lati»; fix scope `const vtxNum = i + 1` prima del pulsante `⌖`; ripristino righe «Lati» e controlli `⌖`/`+`/`✕`; fix locale rendering; **nessuna nuova review Claude richiesta**) + **`5449cb9`** (P-VERTEX-MODAL-FIX2 — **RAMO A** solo HTML/CSS/attributi, **zero righe JavaScript**; un hunk, +3 righe CSS `body.gis-mode dialog#polygonPanel.polygon-panel:not([open]){ display:none }` perché `body.gis-mode dialog#polygonPanel.polygon-panel { display:flex }` applicava flex incondizionato senza `[open]` e sovrascriveva `dialog:not([open]){ display:none }` — conseguenza: `×` chiudeva logicamente (`panelOpen=false`) ma pannello chiuso restava visibile e intercettava click; `−` instabile nei cicli successivi; **non** causa: listener persi, drag header, resize NE, `inert`, lifecycle `showModal()`, wiring JS `−`/`×`; deploy GIS-only diretto autorizzato; byte-check leggero; **review Claude non richiesta**, correttamente, gate RAMO A CSS-only); blob autorevole finale **`acafd51982ace54524e6dd1ef7cc694a76389568`**. **Deploy VPS GIS-only PASS tecnico** (pull FF `5f8f73d`→`5449cb9`, HEAD VPS **`5449cb9`**, byte **2325624**, SHA-256 **`23d0ba1f9d8bc4331d05ade6900f1d0a792a1de5612b15f51833cc8bc8b6e458`**, blob match repo/servito/tailnet, cmp PASS exit 0, HTTP **200**, `goi-gis-app` active/enabled). **Sequenza QA:** QA FAIL operatore su `a4fa8e7` — lista Lati vuota → fix `5f8f73d`; QA FAIL operatore su `5f8f73d` — controlli header `×`/`−` non affidabili → fix CSS `5449cb9` → deploy GIS-only PASS tecnico → **QA operatore PASS** — attestazione «**QA P-VERTEX-MODAL PASS operatore**». Batch P5 separato non modificato.
- **P-VERTEX-FORMAT — selettore formato coordinate vertice — CLOSED / PASS end-to-end:** catena runtime **`b9db963`** (base — selettore «Formato coordinate» in `#polygonVertexCoordDialog`; formati **`dd`**, **`signed`**, **`ddm`**, **`dms`**, **`utm`**, **`mgrs`**; working copy transiente `polygonVertexCoordModalCanon`; geometria aggiornata solo su Salva nel modal; canonico geometrico **`[lon, lat]`** invariato; accettazione coordinate via helper già usati dal coordinate converter dove applicabile; nessun nuovo store persistente; nessun export/import/sanitizer/P-STYLE/P5/OPSEC/tile/geocoding/`state.mapWaypoints[]`; blob **`0cae293bb3b91fd3ed549531e477649f4b37a769`**) + **`6ef714a`** (P-VERTEX-FORMAT-UX2 — secondo selettore `#polygonPanelVertexCoordFormatSel` in `#polygonPanelUnits` vicino «Unità di misura»; entrambi i select con `data-role="poly-vertex-coord-format"`; stato transiente condiviso `polygonVertexCoordFormat`; sincronizzazione bidirezionale pannello↔modal; modal aperto: riformatta working copy senza salvare geometria; modal chiuso: aggiorna solo preferenza sessione; nessun salvataggio geometria automatico; nessun nuovo campo GIS/properties/export/import; blob finale/live **`ed62117316c4e6ad04fc67f1f484c46a3f5aa76b`**). **Review Claude NON RICHIESTA** in chiusura docs (zero delta runtime). **Deploy base GIS-only PASS tecnico** (runtime VPS **`b9db963`**, blob **`0cae293…`**, `goi-gis-app.service` active/enabled, HTTP **200**, byte/SHA match, CMP_PASS). **QA operatore base PASS** — attestazione «**QA P-VERTEX-FORMAT PASS operatore**». **Deploy UX2 GIS-only PASS tecnico** (runtime VPS **`6ef714a`**, blob **`ed621173…`**, byte repo/servito **2352764**, SHA-256 **`7f8799058382c6e2e01ef1f298a215515198131e75f8009f670d6c0659bf40cb** match, CMP_PASS, HTTP **200**; Planet-Clone/Navionics proxy/Docker/n8n/Tailscale firewall **non toccati**). **QA operatore UX2 PASS** — attestazione «**QA P-VERTEX-FORMAT-UX2 PASS operatore**». **Runtime live finale VPS = `6ef714a`**. Selettore formato disponibile in **due punti**: modal vertice e sezione Unità del pannello Modifica poligono. Formato **transiente/sessione** — nessun nuovo store/campo persistente. **`APP_BUILD_ID` `B5.5Z` invariato** (non bumpato in questi blocchi; nessuna nuova build label dichiarata). **Nessun deploy in chiusura docs-only**. Backlog separato ancora pending: micro-fix multi-touch P2 (`if (mapPolyEditDocDrag || mapPolyMoveDocDrag) return`).
- **P-POLYGON-LIST-ENRICHMENT — lista Poligoni arricchita + tabella ordinabile + layout FIX2 — CLOSED / PASS end-to-end:** catena runtime **base `0409ad4`** (metadati lista — vertici, area, perimetro, timestamp; azioni Mostra/Centra/Rinomina/Elimina; blob **`70f790e0448b2bed436c790e6f69928722720c3b`**; deploy GIS-only **PASS tecnico**; **QA operatore PASS** — «**QA P-POLYGON-LIST-ENRICHMENT PASS operatore**») + **FIX1 `d65410f`** (tabella ordinabile coerente Tracce/Waypoint/Offline — `state._polygonPanelListSort` transiente; sort numerico raw `areaM2`/`perimeterM`; header click ▲/▼; `gisPanelBringToFront` include `polygonPanel`; blob **`701fc3ed063d1faa786918491478f7820acad16c`**; **review byte Claude PASS con osservazioni**; deploy GIS-only **PASS tecnico**; **QA operatore PASS** — «**QA P-POLYGON-LIST-ENRICHMENT-FIX1 PASS operatore**») + **FIX2 `28cc2d2`** (scroll orizzontale+verticale tabella; `#polygonPanelList` `overflow:auto`; tabella `min-width:720px`, `width:max-content`, `table-layout:auto`; colonna Nome `min-width` + `title` completo; **Unità di misura** in cima con selettore P-VERTEX-FORMAT; lista Poligoni in fondo; sorting FIX1 e stacking FIX1 invariati; regressione statica/harness su base **`5449cb9`** **PASS**; controlli Cursor `git diff --check` OK, `node --check` OK, harness **17/17 PASS**; blob **`f3c979170c89b879bae2bd3aa0fc927330a8959c`**; deploy GIS-only **PASS tecnico** — HEAD VPS **`28cc2d2`**, blob servito match, byte repo/servito **2365251**, SHA-256 **`58a53e20eed0567dccb5ce0e36212e5ffc137fda919012c45ed839d134eb14da** match, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; Planet-Clone/Navionics proxy/Docker/n8n/Tailscale/firewall **non toccati**; **QA operatore PASS** — «**QA P-POLYGON-LIST-ENRICHMENT-FIX2 PASS operatore**»). **Runtime autorevole live VPS = `28cc2d2`**. Chiusura docs-only — monolite già versionato; **`APP_BUILD_ID` `B5.5Z` invariato**. **Nessun pending P-POLYGON-LIST-ENRICHMENT.** Desiderata UX post-QA FIX2 classificate **backlog separato**, non FAIL: vedi **P-POLYGON-LIST-UX-NEXT**.
- **P-POLYGON-LIST-UX-NEXT — backlog candidato (A chiuso; B runtime landed; B-FIX2 chiuso):** **(A) rinomina inline** — **CLOSED** (vedi voce UX-NEXT-A sotto); **(B) colonne** tabella Poligoni ridimensionabili — runtime landed (`0c5fe42`/`e1d7718`, build 4, QA PASS operatore storico); chiusura docs dedicata B non in questo blocco; **(B-FIX2) indicatore Vis.** — **CLOSED** (vedi voce B-FIX2 sotto). Richieste emerse da QA FIX2 — **non** regressioni. **Non** WU aperta.
- **TRACK-BRUSH-A (+ FIX1 + FIX2 + FIX3) — pennello freehand saved-track — CLOSED / PASS end-to-end:** catena runtime **`15f9640`** (build 39 feature) → **`75a1d5c`** (FIX1 build 40) → **`db10408`** (FIX2 build 41) → tip **`d4f877a`** (FIX3 build 42 — `fix(gis): serialize brush imports and preserve review lifecycle`); blob **`6e6760890b40eed1f62a24893e815edc69140489`**; byte **2728773**; SHA-256 **`3660ce5002023ac419d575b960f5a8c366191a7a72245fc650ca09c5bb2df4e3`**; display **`B5.5Z · build 42`**; **`APP_BUILD_ID` `B5.5Z` invariato**. Bundle **DELICATO** (nuovo create-path + Pointer Events + storage read-back/rollback + mutual exclusion import). Review GPT-sostitutiva pre-deploy **PASS** (FIX3 + bundle cumulativo). **Deploy VPS GIS-only PASS tecnico** (Cursor SSH `ionos-n8n`, pull FF `40c97b6`→`d4f877a`, HEAD/origin/ls-remote **`d4f877a`**, blob/byte/SHA match, HTTP **200**, cmp PASS, `goi-gis-app` active/enabled; Planet-Clone/proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=d4f877a`). **QA operatore PASS** — attestazione «**QA TRACK-BRUSH-A PASS operatore**» (2026-07-23). Chiusi **TRACK-BRUSH-A** / **FIX1** / **FIX2** / **FIX3**. Superseded per runtime live da **TRACK-BRUSH-ANTIMERIDIAN** tip **`9cc7937`**.
- **TRACK-BRUSH-ANTIMERIDIAN (+ FIX1) — supporto dateline brush/render/fit — CLOSED / PASS end-to-end:** catena **`bebf517`** (build 43 — shortest-wrap render + lift reject brush; `fix(gis): support antimeridian track brush rendering and fit`) → tip **`9cc7937`** (FIX1 build 44 — fit allineato a unwrap ordinato; `fix(gis): align antimeridian track fit with rendered path`); blob **`6f22b7e9a197c1a5b2efd4d116c325247a07eee1`**; byte **2733148**; SHA-256 **`91272498442ec999121b7c2a87b8c649bd71770d9dd43b97c74e000e3686b89d`**; display **`B5.5Z · build 44`**; **`APP_BUILD_ID` `B5.5Z` invariato**. Bundle **DELICATO**. Review downstream pre-deploy **PASS** (feature + FIX1). **Deploy VPS GIS-only PASS tecnico** (Cursor SSH `ionos-n8n`, pull FF `d4f877a`→`9cc7937`, HEAD/origin/ls-remote **`9cc7937`**, blob/byte/SHA match, HTTP **200**, cmp PASS, `goi-gis-app` active/enabled; Planet-Clone/proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=9cc7937`). **QA operatore PASS** — attestazione «**QA TRACK-BRUSH-ANTIMERIDIAN PASS operatore**» (2026-07-23). Lon persistite canoniche; nessun auto-fly al save brush; lifecycle FIX1–FIX3 brush invariato. Chiusi **TRACK-BRUSH-ANTIMERIDIAN** / **FIX1**. Superseded per runtime live da **TRACK-CREATE-EDIT-UX-A** tip **`793f4cb`**.
- **TRACK-CREATE-EDIT-UX-A (+ FIX1) — CTA Nuova traccia + stile review/edit unificato + verify persistenza — CLOSED / PASS end-to-end:** catena **`33dc33d`** (build 45 — `feat(gis): unify track create review and edit UX`) → tip **`793f4cb`** (FIX1 build 46 — `fix(gis): verify track create and edit persistence`); blob Git **`0afb9c91177facc4fdca1a468144df870ddbcd8b`**; byte Git/LF **2765139**; SHA-256 Git/LF **`61c8b386dbda92f8f270eed26fa43aee02608cec644378f4bdef2ff06849209b`**; display **`B5.5Z · build 46`**; **`APP_BUILD_ID` `B5.5Z` invariato**. Bundle **DELICATO**. Review evidence + review downstream FIX1 pre-deploy **PASS**. **Deploy VPS GIS-only PASS tecnico** (Cursor SSH `ionos-n8n`, pull FF `9cc7937`→`793f4cb`, HEAD/origin/ls-remote **`793f4cb`**, blob/byte/SHA LF match, HTTP **200**, cmp PASS vs `git show`, `goi-gis-app` active/enabled; Planet-Clone/proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=793f4cb`). **QA operatore PASS** — attestazione «**QA TRACK-CREATE-EDIT-UX-A + FIX1 PASS operatore**» (2026-07-24); auto-`finito` Regola H. Menu Nuova traccia (punti/pennello); `workingStyle` brush review; Modifica unificata geo+stile; read-back canonico completo + rollback; batch stile preservato. Chiusi **TRACK-CREATE-EDIT-UX-A** / **FIX1**.
- **Prossimo ordine operativo candidato (backlog, non WU aperte):** **da scegliere da roadmap/backlog**; **OFFLINE-DOWNLOAD-CONTROLS** backlog (Pausa/Stop/Riprendi — non ora); estensioni **MAJOR-3** (import unificato) backlog basso; **MAJOR-4** import/restore mission package backlog basso; titolo statico `<title>` e micro-UX **non ora** (salvo bug reale). **Metodo vivo:** METHOD-BUNDLING-DEFAULT + METHOD-QA-PASS-AUTO-FINITO. **TRACK-CREATE-EDIT-UX-A** (+ FIX1) **CLOSED** (`793f4cb`, build 46). **TRACK-BRUSH-ANTIMERIDIAN** (+ FIX1) **CLOSED** (`9cc7937`, build 44). **TRACK-BRUSH-A** (+ FIX1–FIX3) **CLOSED** (`d4f877a`, build 42). **TRACK-STYLE-A** (+ FIX1/FIX2) **CLOSED** (`40c97b6`, build 38). **IMPORT-DROP-B + TRACK-MODAL-UX-A** **CLOSED** (`1d28163`, build 35). **IMPORT-DROP-A** **CLOSED** (`5f57a75`, build 34). **MAJOR-4-a** **CLOSED** (`73269a0`, build 33). **Programma pick Workbench MAJOR-5A2 completo.**
- **APP-BUILD-NUM-B1 — build number monotono runtime — CLOSED / PASS tecnico end-to-end:** runtime **`bd588a8`** — `const APP_BUILD_NUM = 1` accanto a `APP_BUILD_ID` / `APP_BUILD_DETAIL`; display runtime **`B5.5Z · build 1`** in title/footer/about via `applyAppBuildLabel()`; `#appBuildAboutDetail` / `APP_BUILD_DETAIL` intatto (*Quick geographic JPG export and segmented high-zoom tiles*); nessuna persistenza; nessun impatto su sanitizer/storage/rete/geometrie/poligoni/rename/import-export; blob **`afddf87a6f05929b540f768a0193872057fe24cb`**. **Review byte Claude PASS** — GO DEPLOY GIS-only. **Deploy GIS-only PASS tecnico** — VPS HEAD **`bd588a8`**, byte **2365479**, SHA-256 **`23907b809bb47ed52befe36058b6e8a1f01148d40ec54104a71dc019da3b0614** match, CMP_PASS. **Verifica runtime minima display PASS**; **nessuna QA operatore estesa**. **Cleanup span statici footer/about** foldato in **UX-NEXT-A** (`6892890`). **`APP_BUILD_ID` `B5.5Z` invariato**.
- **P-POLYGON-LIST-UX-NEXT-A — rinomina inline cella Nome + build bump 2 — CLOSED / PASS end-to-end:** runtime **`6892890`** — rinomina inline in cella Nome tabella Poligoni; click **Rinomina** → `<input>` in riga; Enter → `polygonCommitInlineRename` → **`polygonRenameExecute(id, value)`**; Esc/blur annulla; **nessuna** scrittura diretta `properties.name`; **nessun** `gisFeatureUpdate`/`saveStore` diretto nel path inline; `state._polygonPanelInlineRenameId` transiente; sorting/stacking FIX1 preservati; **`APP_BUILD_NUM = 2`**; display **`B5.5Z · build 2`** via `applyAppBuildLabel()`; cleanup span statici `#appBuildFooter`/`#appBuildAbout` → solo `B5.5Z`; `#appBuildAboutDetail` / `APP_BUILD_DETAIL` intatto; blob **`30358cd3aafa9879d76400e23ce103ff5372b081`**. **Review byte Claude PASS** — GO DEPLOY GIS-only. **Deploy GIS-only PASS tecnico** — VPS HEAD **`6892890`**, VPS blob match, byte repo/servito **2368796**, SHA-256 **`96f9468ed8ea6d1e39acd8186af0ffbe295747ac684848131ff4da9dfb7c893e** match, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; Planet-Clone/Navionics proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=6892890`. **QA operatore PASS** — «**QA P-POLYGON-LIST-UX-NEXT-A PASS operatore**» (Enter/Esc/blur; nome lungo; click input non triggera sort/azioni; rename altre righe disabilitato; sort durante editing OK; footer/about `B5.5Z · build 2`; regressione pannello `−`/`×`/minimize/modal vertice OK). **Nota backlog:** `polygonShowRenameBar` non più chiamata dalla lista — possibile dead code cleanup futuro se nessun altro path la usa. **`APP_BUILD_ID` `B5.5Z` invariato**. **Runtime autorevole live VPS = `6892890`**. Chiusura docs-only post-deploy+QA.
- **UI-MODAL-PARITY-HELP-QR — migrazione Help/QR a dialog build 5 — CLOSED / PASS tecnico, QA operatore FAIL (storico):** runtime **`dcea02f`** — blob **`cf23cc9ca4392fc489c8ccf4a7cda11b67f7f673`**; migrazione `#helpOverlay` e `#qrModal` da legacy `div.modal-overlay` a `<dialog class="app-modal">`; pattern GIS `show()` + `aria-modal="false"` / non-GIS `showModal()`; close per-dialog `#helpClose`/`#qrClose`; **`APP_BUILD_NUM = 5`**; display **`B5.5Z · build 5`**; **Review Claude NON RICHIESTA** (blocco routine Ramo B con review tiered su diff); **Deploy GIS-only PASS tecnico** (runtime VPS **`dcea02f`**, smoke HTTP **200**, CMP_PASS); **QA operatore FAIL** — Help GIS tagliata in basso/non floating/non movibile/manca `−`; QR dal flusso Converti non si apre → **FIX1**.
- **UI-MODAL-PARITY-HELP-QR-FIX1 — Help floating + QR ripristinato build 6 — CLOSED / PASS end-to-end:** runtime **`e8e8ff1`** — blob **`6eee6872d47dd8a0ed4e04c34dd990e661ced153`**; subject `fix(ui): restore GIS help and QR dialog behavior`; Help GIS: pannello floating (`wireHelpPanelFloatingGis`, drag/resize, scroll solo `.app-modal-body`, default centrato sotto topbar, `−` minimizza + dock, `×` chiude); fuori GIS dialog modale nativo; QR: `openContextAwareAppDialog` (GIS `show()` + bring-to-front; non-GIS `showModal`/fallback sicuro con Converti aperto); generazione/copy/download/i18n QR invariati; **`APP_BUILD_NUM = 6`**; display **`B5.5Z · build 6`**; **Review GPT sostitutiva PASS** (Claude fuori uso — **non** review byte Claude ordinaria); **Deploy GIS-only PASS tecnico** — VPS HEAD **`e8e8ff1`**, blob match, byte repo/servito **2404202**, SHA-256 **`3fe2ac2e39c2a92cc8b282eede1e937036440f7cc4acfb672003eb0290899775`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; **QA operatore PASS** — «**QA UI-MODAL-PARITY-HELP-QR-FIX1 PASS operatore**». Superseded per runtime live da **FIX2** (`14605e9`). **`APP_BUILD_ID` `B5.5Z` invariato**.
- **UI-MODAL-PARITY-HELP-QR-FIX2 — QR ridimensionabile build 7 — CLOSED / PASS end-to-end:** runtime **`14605e9`** — blob **`0886b6bb4ab4b2cd13e060b1c6f34eafe6953259`**; subject `fix(ui): make QR dialog resizable`; QR modal in GIS mode ridimensionabile con handle angoli (`gisPanelAttachResize`, pattern coerente Help FIX1); drag header QR preservato; layout body flex — SVG centrato, URL e azioni non tagliate al resize; resize transiente UI (sessione `gPanelLayouts`, non localStorage); invariati: `openQrModal`, `openContextAwareAppDialog`, `#qrSvg`, `#qrUrl`, copy/download, generazione QR, flusso Converti, Help (logica), storage/GIS/OPSEC/rete; **`APP_BUILD_NUM = 7`**; display **`B5.5Z · build 7`**; **Review GPT sostitutiva PASS** (Claude non disponibile — **non** review byte Claude ordinaria); **Deploy GIS-only PASS tecnico** — VPS HEAD **`14605e9`**, VPS blob match, byte repo/servito **2407357**, SHA-256 **`1447722424f5d8c180b4b89fb2c5dff7fb6d1e9b173d542f5b30484990e832b5`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; **QA operatore PASS** — «**QA UI-MODAL-PARITY-HELP-QR-FIX2 PASS operatore**». Superseded per runtime live da **CONVERT-SOURCE-PICKER** (`b294140`). **`APP_BUILD_ID` `B5.5Z` invariato**. **Catena UI-MODAL-PARITY-HELP-QR (build 5 → FIX1 → FIX2) CLOSED / PASS end-to-end**.
- **CONVERT-SOURCE-PICKER — sorgente coordinate Convertitore build 8 — CLOSED / PASS end-to-end:** runtime **`b294140`** — blob **`6feba1c9e0b192c1655ba052314e7d8cae87df98`**; subject `feat(convert): add waypoint favorite and map source picker`; sezione **Sorgente coordinate** in `#manualInputSection` / Converti — waypoint (`state.mapWaypoints[]` via select + **Usa** → DD + `doConvert()`), preferito (`state.favorites[]`, solo lat/lon validi), punto mappa **click one-shot** (`convertSourcePickMode`, pattern Astro/Range Rings; Esc/Annulla/chiusura Converti; nessun GPS/tracking), centro mappa (`state.viewCenter` \|\| `state.lastPoint`); feedback `#convertSourceFeedback`; input manuale DD/DDM/DMS/free/UTM/MGRS invariato; QR da Converti e Help/QR build 7 non regressi; nessuna nuova persistenza; OPSEC/rete/tile/proxy/cache/import-export/sanitizer/storage schema invariati; **`APP_BUILD_NUM = 8`**; display **`B5.5Z · build 8`**; **Review GPT sostitutiva PASS** (Claude non disponibile — **non** review byte Claude ordinaria); **Deploy GIS-only PASS tecnico** — VPS HEAD **`b294140`**, VPS blob match, byte repo/servito **2423291**, SHA-256 **`1a954ca989e436bb1dadb319d7fc84701ed760a845d3127d6d963f4b1ae6b4ab`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; Planet-Clone/proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=b294140`; **QA operatore PASS** — «**QA CONVERT-SOURCE-PICKER PASS operatore**» (input manuale OK; waypoint/preferito/punto mappa/centro mappa → output aggiornato; Annulla/ESC disattivano picker; QR da Converti; mappa interattiva; Help/QR non regressi; footer/about **`B5.5Z · build 8`**). **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **P-POLYGON-LIST-UX-NEXT-B-FIX2** (`b7b98c2`).
- **P-POLYGON-LIST-UX-NEXT-B-FIX2 — indicatore Vis. poligoni build 9 — CLOSED / PASS end-to-end:** runtime **`b7b98c2`** — blob **`dc8067d960a0ae0901f4a6f59d7ee19fb0e9586b`**; subject `fix(gis): add polygon visibility indicator`; colonna **Vis.** tabella Poligoni — indicatore non interattivo `.poly-vis-indicator` (pallino verde `#16a34a` visibile, grigio/spento nascosto; correzione da classe `saved-track-vis-indicator` scoped solo su Tracce); stato via **`polygonMapVisible(f)`**; `aria-label`/`title` i18n esistente (`gis.polygonPanel.visible`/`hidden`); toolbar batch Mostra/Nascondi selezionate/tutte invariata; checkbox, rename inline, resize colonne, delete, sorting invariati; nessuna nuova persistenza; storage/dati GIS/sanitizer/OPSEC/rete/tile/proxy/cache/import-export/geometrie invariati; **`APP_BUILD_NUM = 9`**; display **`B5.5Z · build 9`**; **Review Claude NON RICHIESTA** (micro-fix UX Ramo B); **Deploy GIS-only PASS tecnico** — VPS HEAD **`b7b98c2`**, VPS blob match, byte repo/servito **2423809**, SHA-256 **`87746763adf80441c9c952a0572972cffa199dc62dcdb66cc5f9326a9b77b844`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; Planet-Clone/proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=b7b98c2`; **QA operatore PASS** — «**QA P-POLYGON-LIST-UX-NEXT-B-FIX2 PASS operatore**» (pallino verde/grigio coerente; Mostra/Nascondi selezionate/tutte aggiornano indicatori; colonna Vis. non cliccabile; checkbox/toolbar/rename/resize invariati; footer/about **`B5.5Z · build 9`**). **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **MODAL-STD-SEARCH-B1** (`33c95ad`).
- **MODAL-STD-SEARCH-B1 — standardizzazione pannello Cerca build 10 — CLOSED / PASS end-to-end:** runtime **`33c95ad`** — blob **`d048ee2ff92bf956b31a74aa8ecde21ae49a4540`**; subject `fix(ui): improve search panel viewport layout`; primo micro-blocco standardizzazione modal/layout — solo `#searchPanel` / tab Cerca; `_searchPanelLayoutOpts`: `defaultHeightFraction` **0.58 → 0.78**, cap **520 → 940**, `topbarReserve` 100, `partialMinVisible` 72, `bodyMinH` 120; `clampSearchPanelRect` → `gisPanelClampRectPartialVisible` (coerente Favoriti); CSS `#searchPanelBody`: summary `#geocodeCard` nascosto nel floating, `.geo-body` sempre visibile, scroll interno body, sfondo map-first; nessun tocco geocoding/rete/OPSEC/storage/dati GIS/sanitizer/import-export/geometrie; nessun tocco altri modal, ESC globali, `show()`/`showModal()`; **`APP_BUILD_NUM = 10`**; display **`B5.5Z · build 10`**; **Review Claude NON RICHIESTA** (layout/CSS + opts Ramo B micro-blocco); **Deploy GIS-only PASS tecnico** — VPS HEAD **`33c95ad`**, VPS blob match, byte repo/servito **2424747**, SHA-256 **`fd6203f61e7f1b7fe14936664e20d280d0e32276988c769fe582178dd593b731`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; Planet-Clone/proxy/Docker/n8n/Tailscale/firewall **non toccati**; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=33c95ad`; **QA operatore PASS** — «**QA MODAL-STD-SEARCH-B1 PASS operatore**» (pannello più alto ~75–80% viewport; scroll interno risultati; header/× fissi; resize angoli OK; drag header OK; mappa interattiva; ESC e × OK; nessun summary duplicato; footer/about **`B5.5Z · build 10`**; nessuna regressione Help/QR/Converti/Poligoni). **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **MODAL-STD-B2-FIX2** (`266b116`).
- **MODAL-STD-B2 — Preferiti layout/scroll + Poligoni ESC build 11 — CLOSED / PASS tecnico, QA parziale:** runtime **`06ed2a0`** — blob **`431a9c88e6e5093cc2d0944b81750b71650eb238`**; subject `fix(ui): standardize favorites layout and polygon escape handling`; combinazione audit **MODAL-STD-FAVORITES-B1** + **MODAL-STD-POLYGON-ESC-B1**; Preferiti: `_favoritesPanelLayoutOpts` `bodyMinH` 120, CSS flex column + scroll lista interna (MODAL-STD-B2); Poligoni: catena ESC GIS (vertex modal → delete bar → rename bar → inline rename → `polygonEditCancelHandler()` → `closePolygonPanel()`; draw via `bindHotkeys`); nessun tocco dati/store/import-export preferiti; **`APP_BUILD_NUM = 11`**; display **`B5.5Z · build 11`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico**; **QA operatore parziale** — Poligoni ESC **PASS**; Preferiti **FAIL** (lista non scrollava su resize basso; ESC non chiudeva; × lasciava guscio vuoto). Superseded da FIX1/FIX2.
- **MODAL-STD-B2-FIX1 — Preferiti close/ESC build 12 — CLOSED / PASS tecnico, QA parziale:** runtime **`f53e2d8`** — blob **`4ac10423c893241b88fc3613c5f5033538644925`**; subject `fix(ui): repair favorites panel close and scroll`; fix `:not([open]){ display:none }`; `closeFavoritesPanel()` `close()` + `removeAttribute("open")`; ESC Preferiti con `preventDefault`; `openFavoritesPanel` forza `sec-favorites.open = true`; **`APP_BUILD_NUM = 12`**; display **`B5.5Z · build 12`**; **Deploy GIS-only PASS tecnico** — byte **2427546**, SHA **`4531f47e…`**, CMP_PASS; **QA operatore** — ×/ESC/riapertura/Poligoni ESC **PASS**; scroll Preferiti su resize basso **FAIL** residuo. Superseded da FIX2.
- **MODAL-STD-B2-FIX2 — Preferiti scroll body build 13 — CLOSED / PASS end-to-end:** runtime **`266b116`** — blob **`0f4d275ea86b5b78690421405ffa5909add5783e`**; subject `fix(ui): restore favorites panel inner scroll`; scroll intero `#favoritesPanelBody` (schema Search B1); rimossa catena flex interna lista; `syncFavoritesPanelBodyLayout` delega a `gisPanelSyncBodySize`; close/ESC/× FIX1 preservati; Poligoni ESC invariato; nessun tocco dati/store/import-export preferiti; **`APP_BUILD_NUM = 13`**; display **`B5.5Z · build 13`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`266b116`**, VPS blob match, byte repo/servito **2427039**, SHA-256 **`c8b39050e456511ea64ea4eaf60df88784ede46b0f490cf77efd587f9a227dc3`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=266b116`; **QA operatore PASS** — «**QA MODAL-STD-B2-FIX2 PASS operatore**» (Preferiti ridimensionato in basso: scrollbar body, contenuti raggiungibili; header/×/− accessibili; ×/ESC/riapertura OK; Poligoni ESC OK; footer/about **`B5.5Z · build 13`**). **Catena MODAL-STD-B2 (B2 + FIX1 + FIX2) CLOSED / PASS end-to-end.** **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **BUNDLE-BACKLOG-B3** (`709079c`).
- **BUNDLE-BACKLOG-B3 — audit backlog bundle + micro-fix sicuri build 14 — CLOSED / PASS end-to-end:** runtime **`709079c`** — blob **`da27be4363e878f97f1f1b8d4dbc9df34f9c7ed3`**; subject `chore(ui): apply safe backlog micro-fixes`; audit interno 4 candidati (dead code, resize laterale, HUD, multi-touch P2); **implementato:** rimossa `polygonShowRenameBar()` (dead certo, 0 call site); guard P2 multi-touch su vertex `pointerdown` in `renderPolygonEditOverlay` (`mapPolyEditDocDrag || mapPolyMoveDocDrag`); **non implementato (audit):** `polygonHideRenameBar`/barra rename (ancora wired); CSS legacy `.modal-overlay`/`.modal`; `renderAllMaps` undefined (8 call site); resize laterale (solo angoli nw/ne/sw/se); HUD-VIS (nessun setting pronto); **`APP_BUILD_NUM = 14`**; display **`B5.5Z · build 14`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`709079c`**, VPS blob match, byte repo/servito **2426501**, SHA-256 **`ca0d74a61395d02fc3a3281a29851721c4425e24e5073b68fe5d3d3ba95a0902`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=709079c`; **QA operatore PASS** — «**QA BUNDLE-BACKLOG-B3 PASS operatore**» (drag vertice invariato; secondo pointer non sostituisce drag; rename inline OK; lista Poligoni OK; footer/about **`B5.5Z · build 14`**). **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **ROUTINE-CLEANUP-BUNDLE** (`7b8cf04`).
- **ROUTINE-CLEANUP-BUNDLE — primo bundle bundle-first cleanup routine build 15 — CLOSED / PASS end-to-end:** runtime **`7b8cf04`** — blob **`71e353ee85c15bf2713bc7998c72582f81723ec5`**; subject `chore(ui): remove dead modal CSS and renderAllMaps calls (build 15)`; **bundle ROUTINE** (METHOD-BUNDLING-DEFAULT): un commit / un deploy / una QA; **nessun hop Claude**; **7 item:** (1) rimozione CSS legacy `.modal-overlay`/`.modal`/`.modal .modal-close` desktop+mobile+print token; (2) selettori `.modal` duplicati tipografia Help; (3) `.modal .modal-close` da chip close unificati; (4) ridondante `.qr-modal{max-width}`; (5) 7× `try { renderAllMaps(); }` no-op (funzione indefinita); (6) commento chip senza `.modal-close` legacy; nessuna categoria delicata toccata; sanitizer/OPSEC/rete/storage/lifecycle modale invariati; **`APP_BUILD_NUM = 15`**; display **`B5.5Z · build 15`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`7b8cf04`**, VPS blob match, byte repo/servito **2423860**, SHA-256 **`0caa70651a4fca7b04112abddc1af50a44059c5539a9407ed5702ddb646146ba`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=7b8cf04`; **QA operatore PASS** — «**QA ROUTINE-CLEANUP-BUNDLE PASS operatore**» (Help e QR OK GIS+non-GIS; poligoni Modifica/handle/drag vertice OK; nessun errore console nuovo; footer/about **`B5.5Z · build 15`**). **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **UX-NEXT-RUNTIME-BUNDLE-A** (`61bcda5`).
- **MAJOR-5A2c — GIS Object Workbench pick mappa poligoni build 29 — CLOSED / PASS end-to-end:** runtime **`eb1451b`** — build **`B5.5Z · build 29`**; blob git **`5ed0d4c5cb37d60fe8ce4a683f3bd172a7e060b2`**; subject `feat(gis): add workbench polygon map pick (build 29)`; estensione pick mode 5A2b — poligoni GIS salvati visibili via hit-test geometrico (point-in-polygon + bordo ~14px) in coda a waypoint/traccia in `workbenchMapHitTestAtClient`; ordine priorità WP→traccia→poligono; skip `skipEditId` come overlay; `workbenchMapPickSelect` accetta `kind: polygon`; guardia `workbenchMapInteractionBlocked()` (+ `mapPolyEditDocDrag`/`mapPolyMoveDocDrag` typeof-safe); overlay poligoni resta `pointer-events:none`; **esclusi** persistenza, delete/rename/edit, fix UX backlog 5A2; **`APP_BUILD_NUM = 29`**; **Review Claude PASS pre-deploy** (diff `4f598ed..eb1451b`, GO DEPLOY); **Deploy GIS-only PASS tecnico** — VPS HEAD **`eb1451b`**, byte **2574712**, SHA-256 file **`3f3adb173b04dc5edcf2270f6e8304c8c30a3a05ddb0e308a20ee4e6c8f0618c`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=eb1451b`; **QA operatore PASS** — «**QA MAJOR-5A2c PASS operatore**» (regressione 5A2a/5A2b, pick poligono, priorità hit-test, edit/draw/Esc/cleanup, build 29; nota donut ring esterno non bloccante); trigger auto-`finito` Regola H. **Runtime autorevole live VPS = `eb1451b`**. **Programma MAJOR-5A2 pick completo.** OPSEC/fetch/proxy/sanitizer/import-export/tile/cache invariati per scope.
- **MAJOR-5A2b — GIS Object Workbench pick mappa WP+traccia build 28 — CLOSED / PASS end-to-end:** runtime **`cef7d42`** — build **`B5.5Z · build 28`**; blob git **`638978935fcbead38a7c885b725976417a71c628`**; subject `feat(gis): add workbench map pick mode (build 28)`; toggle «Seleziona da mappa» in Workbench; `state._workbench.pickMode` session-only; pick waypoint (`.wpt-handle`) e traccia salvata (`data-saved-map-hit` + hit-test geometrico); selezione via `workbenchSetSelection` + highlight 5A2a; guardia `workbenchMapInteractionBlocked()`; uscita pick Esc/toggle/chiusura/post-selezione; **esclusi** pick poligoni, persistenza, delete/rename/edit; **`APP_BUILD_NUM = 28`**; **Review Claude PASS pre-deploy** (diff `41f4740..cef7d42`, GO DEPLOY); **Deploy GIS-only PASS tecnico** — VPS HEAD **`cef7d42`**, byte **2571484**, SHA-256 file **`8786d08290051f6fca8a71f484982871a00e0cffaab71ff62c1d235ddf4d4466`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=cef7d42`; **QA operatore PASS** — «**QA MAJOR-5A2b PASS operatore**» (funzionale: selezione riga, pick WP/traccia, esclusione poligoni, Esc/cleanup, build 28 OK; **non FAIL** del blocco); **backlog UX non bloccante post-QA** → vedi **MAJOR-5A2-UX-BACKLOG**; trigger auto-`finito` Regola H. **Runtime autorevole live VPS = `cef7d42`**. OPSEC/fetch/proxy/sanitizer/import-export/tile/cache invariati per scope.
- **TRACK-STYLE-A (+ FIX1 + FIX2) — stile saved tracks singolo/batch build 38 — CLOSED / PASS end-to-end:** catena runtime **`ab5455d`** (build 36 — campi flat `color`/`lineWidth`/`lineOpacity`/`lineStyle`, sanitizer + whitelist `saveStore`, editor singolo+batch, preview, hover relativo, default legacy runtime-only HSL/2.5/0.9/`6 4`) → **FIX1 `1146e59`** (build 37 — cleanup preview su edit geometrica/chiusura/minimize; invalidazione batch su selezione; guard set-based Applica) → **FIX2 `40c97b6`** (build 38 — sync checkbox «Includi» DOM+stato prima di PreviewRefresh); subject finale `fix(gis): sync batch style inclusion before preview (build 38)`; blob git **`2acf77113a73f1d76d130dc2b2947c8310080f6e`**; byte **2655368**; SHA-256 **`952550efe6eff48d2e6894fdb32fc41cbec7c22734c0bb69b78b9614f5827d49`**; **`APP_BUILD_NUM = 38`**; display **`B5.5Z · build 38`**; bundle **DELICATO** (nuovi campi persistiti / sanitizer / whitelist / batch); review GPT-sostitutiva pre-deploy con FAIL→FIX1→FIX2; **Deploy GIS-only PASS** — attestato operatore fuori sessione Cursor (servizio `goi-gis-app.service` active/enabled, HTTP **200**, byte **2655368**, SHA-256 match, contenuto coincidente; Cursor **non** ha ripetuto il deploy in chiusura); **QA operatore PASS** — attestazione «**QA TRACK-STYLE-A PASS operatore**» + autorizzazione esplicita `finito`; chiusi **TRACK-STYLE-A**, **TRACK-STYLE-A-FIX1**, **TRACK-STYLE-A-FIX2**. **Runtime Git / VPS atteso = `40c97b6`**. Resta non aperto: **TRACK-BRUSH-A**. Mission Package / export GPX-KML stile / poligoni / Range Rings invariati per scope.
- **IMPORT-DROP-B-TRACK-MODAL-UX-A — KMZ drop + fit tracce + Centra/Unità modal Tracce build 35 — CLOSED / PASS end-to-end:** runtime **`1d28163`** — build **`B5.5Z · build 35`**; blob git **`ee599bde006d7f1cf9fe835f9e9e437187330a0b`**; byte repo **2610149**; SHA-256 file **`21617a766ec606ddcce6a8480db16a69df9c6bbfe3aa60bf855431a37b345562`**; subject `feat(gis): add KMZ drop and track map controls (build 35)`; push diretto `origin/main` (+ branch/PR #3); drop globale `.gpx|.kml|.kmz` (case-insensitive); KMZ via riuso byte-invariato `spatialExtractKmlTextFromKmz` + `spatialKmlToFeatureCollection` (FC diretta); `fitAcc { trackPts, otherPts }` — fit prioritario su `res.track.points` delle sole tracce create nel batch; Centra card Punti su `state.track.points` (`flyMapToTrackPoints`); Unità reparent `#trackUnitRow` / `#trackDisplayUnit` (nodo unico) tra `#trackUnitHome` e mount inline; i18n IT/EN `tip.trackCenterCurrent` + `common.center` (FR intatto); **`APP_BUILD_NUM = 35`**; bundle **ROUTINE** JS basso rischio; **Deploy GIS-only:** non eseguito da Cursor (SSH `ionos-n8n` irrisolvibile in cloud agent) — **deploy manuale lato operatore** presupposto dall’attestazione QA; CMP/byte VPS **non** ri-verificati da Cursor in chiusura; **QA operatore PASS** — attestazione «**QA IMPORT-DROP-B-TRACK-MODAL-UX-A PASS operatore**» (trigger auto-`finito` Regola H); **Runtime Git autorevole = `1d28163`**. Chiusi: **IMPORT-DROP-B**, **TRACK-MODAL-UX-A**. **Runtime Git autorevole superseded da TRACK-STYLE-A `40c97b6`**. Resta non aperto: **TRACK-BRUSH-A**; finding F1–F7 note-only.
- **IMPORT-DROP-A — multi-file GPX/KML drag & drop build 34 — CLOSED / PASS end-to-end:** runtime **`5f57a75`** — build **`B5.5Z · build 34`**; blob git **`0d7137024c4b1de0496967bb5e3548256ea37e72`**; subject `feat(gis): add multi-file GPX/KML drag and drop import (build 34)`; PR **#2** squash-merged su `main`; drop globale Files-only → parser esistenti; Point→`state.mapWaypoints[]`; LineString/MultiLineString→**una** saved track per file via `savedTrackAddFromPoints` (no saveStore/render; fail-closed su cap); Polygon→`gisFeatureAdd("polygon")`; limiti 20 file/drop e 25 MB/file; KMZ escluso (poi aggiunto in IMPORT-DROP-B); draft `state.track` intatto; i18n IT/EN `dropImport.*` (FR intatto); **`APP_BUILD_NUM = 34`**; **Review pre-deploy PASS** — «**REVIEW PRE-DEPLOY IMPORT-DROP-A — PASS — GO MERGE**» (sostitutiva); **Deploy GIS-only PASS tecnico** — VPS HEAD **`5f57a75`**, byte **2606270**, SHA-256 file **`849bf44f9b3eac9de547ebfeb040fb2d812968df43785eb722a757293b504b14`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled (bind `100.114.7.53:8000`); **restart servizio non necessario** (`python3 -m http.server` serve direttamente WorkingDirectory); **QA operatore PASS** — attestazione «**QA IMPORT-DROP-A PASS operatore**» (ChatGPT post-deploy); **Runtime autorevole live VPS superseded da `1d28163`**. Bundle **DELICATO**.
- **MAJOR-4-a — Mission Package export build 33 — CLOSED / PASS end-to-end:** runtime **`73269a0`** — build **`B5.5Z · build 33`**; blob git **`0da1faa5a95410947340fadc9def320acb199566`**; subject `feat(gis): add mission package export (build 33)`; export **Mission Package JSON** nel Workbench (`#wbExportHub`, pulsante `data-role="wb-export-mission"`) — snapshot read-only `GOI_GIS_MISSION_PACKAGE` v1 con metadata app/build, `contents`/`counts`, GeoJSON embedded via `gisExportHubBuildFeatureCollection`; helper `gisMissionPackageBuild`, `gisMissionPackageExportRun`; riuso selezione categorie MAJOR-3-a; file-first locale `mission-package-*.json`; i18n IT/EN `workbench.export.missionPackage` / `missionOk`; **esclusi** import/restore mission package, nuovi campi persistiti, sanitizer/schema; **`APP_BUILD_NUM = 33`**; **Review pre-deploy PASS** — **GPT/GLM sostitutiva PASS** (Claude offline; **non** review byte Claude); **Deploy GIS-only PASS tecnico** — VPS HEAD **`73269a0`**, byte **2650883**, SHA-256 file **`5bc551d952f03aa02565e4cefe844624fc63f52c64283afcbbe308a3cf35ee76d`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=73269a0`; **QA operatore PASS** — «**QA MAJOR-4-a PASS operatore**» (trigger `finito`); **Runtime autorevole live VPS = `73269a0`**. OPSEC/fetch/proxy/sanitizer/storage/tile/cache invariati per scope.
- **MAJOR-3-a — Unified GIS export hub build 32 — CLOSED / PASS end-to-end:** runtime **`c0fb4da`** — build **`B5.5Z · build 32`**; blob git **`92d8ff692bc5ea7d7eb45ca1fae449d75e38b640`**; subject `feat(gis): add unified GIS export hub (build 32)`; hub export unificato in Workbench (`#wbExportHub`) — selezione categorie via checkbox DOM (waypoint / tracce salvate / poligoni); export **GeoJSON** + riuso path **GPX/KML** esistenti; helper `gisExportHubReadSelFromDom`, `gisExportHubBuildFeatureCollection`, `gisExportHubRun`; `savedTracks` via `savedTrackToFeatureCollection`; read-only/export locale file-first; **esclusi** import unificato, nuovi campi persistiti, sanitizer/import schema; **`APP_BUILD_NUM = 32`**; **Review pre-deploy PASS** — «**REVIEW GPT-SOSTITUTIVA MAJOR-3-a PASS — GO DEPLOY**» (**GPT-sostitutiva** — Claude offline; **non** review byte Claude); **Deploy GIS-only PASS tecnico** — VPS HEAD **`c0fb4da`**, byte **2591276**, SHA-256 file **`09d8ca97af546a1de1a81d44ee65bf8afcd70ae9642adf258e39f9418eff4cee`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=c0fb4da`; **QA operatore PASS** — «**QA MAJOR-3-a PASS operatore**» (trigger `finito`); **Runtime autorevole live VPS superseded da `73269a0`**. OPSEC/fetch/proxy/sanitizer/storage/tile/cache invariati per scope.
- **MAJOR-2E-a — Persist offline area audit status build 31 — CLOSED / PASS end-to-end:** runtime **`d9238b6`** — build **`B5.5Z · build 31`**; blob git **`73a6936a850ca1c65edd82ceb235593117aca946`**; subject `fix(gis): persist offline area audit status (build 31)`; bundle **DELICATO** (cache/storage metadata); helper `applyOfflineAreaAuditToNamedArea` + `offlineAreaAuditMapToNamedStatus`; dopo «Verifica» esplicita (`runOfflineAreaCoverageAudit`) persiste `status`/`tileCountEstimated`/`tileCountDownloaded` su `namedAreas[]` da ground truth IDB; mapping audit `empty`→`partial` se `expected>0`; `saveStore()` solo su mutazione; batch «Verifica selezionate» incluso; **esclusi** auto-scan boot, write/delete tile IDB, precache/download; **`APP_BUILD_NUM = 31`**; **Review pre-deploy PASS** — «**REVIEW MAJOR-2E-a PASS — GO DEPLOY**»; **Deploy GIS-only PASS tecnico** — VPS HEAD **`d9238b6`**, byte **2581430**, SHA-256 file **`cd61d8a76f4f3c1ac4bd4b344c3488c97961f7609cd3d73506094c9a1e53b03f`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=d9238b6`; **QA operatore PASS** — «**QA MAJOR-2E-a PASS operatore**» (trigger `finito`); **Runtime autorevole live VPS superseded da `c0fb4da`**. **Backlog emerso (non implementato):** **OFFLINE-DOWNLOAD-CONTROLS** — Pausa / Stop-Annulla / Riprendi su download tile Mappe Offline.
- **MAJOR-5A2-UX-BACKLOG — Workbench visual polish build 30 — CLOSED / PASS end-to-end:** runtime **`d9c8f7b`** — build **`B5.5Z · build 30`**; blob git **`2b2bb3beac1575bba946c3170c3a2d8d48bd72f1`**; subject `fix(gis): polish workbench panel styling (build 30)`; gate resize persistence **PASS** (ROUTINE — chiave `gisWorkbenchPanel` **non** in `UI_PANEL_KEYS`, nessuna nuova persistenza localStorage); fix **(1)** toolbar `.twb-btn` tema scuro GIS (normal/hover/active/open); **(2)** resize `#gisWorkbenchPanel` angoli — CSS posizionamento handle nw/ne/sw/se + z-order allowlist (handle DOM/`gisPanelAttachResize` già presenti); **(3)** chip `.wb-filter-chip` dark scoped (attivo/inattivo/hover/focus/disabled); **`APP_BUILD_NUM = 30`**; **Review Claude NON RICHIESTA** (bundle ROUTINE CSS/HTML); **Deploy GIS-only PASS tecnico** — VPS HEAD **`d9c8f7b`**, byte **2578728**, SHA-256 file **`e22bbe34b92a1ec1c5a631ebb40c237eac6f1fdadffe813500e48d8ba474fc9e`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=d9c8f7b`; **QA operatore PASS** — «**QA MAJOR-5A2-UX-BACKLOG PASS operatore**» (toolbar dark, resize angoli, chip filtri, regressione pick Workbench, build 30); trigger auto-`finito` Regola H. **Runtime autorevole live VPS superseded da `d9238b6`**. **Esclusi** logica pick/selezione Workbench, storage/sanitizer, i18n, lifecycle dialog.
- **MAJOR-5A2a — GIS Object Workbench selezione lista + highlight build 27 — CLOSED / PASS end-to-end:** runtime **`d2f7856`** — build **`B5.5Z · build 27`**; blob git **`895e1b9bc8fad8e12374ebe10f1d9962639bdfb4`**; subject `feat(gis): add workbench selection highlight (build 27)`; click riga Workbench → `state._workbench.selected` session-only `{ kind, id }`; riga `.is-selected` + `aria-selected`; overlay highlight mappa `.workbench-selection-overlay` (`pointer-events:none`) per waypoint/traccia/poligono; ⌖ fly-to separato; deselect toggle/Esc/chiusura/prune; helper `workbenchSetSelection`, `workbenchClearSelection`, `workbenchPruneSelection`, `renderWorkbenchSelectionHighlight`; **esclusi** pick mappa→lista, pick mode, delete/rename/edit, persistenza; **`APP_BUILD_NUM = 27`**; **Review Claude NON RICHIESTA** (bundle ROUTINE); **Deploy GIS-only PASS tecnico** — VPS HEAD **`d2f7856`**, byte **2559724**, SHA-256 file **`328377add5d78cb1799709c19d0b4c25d7e9935269a761329e96839e147bb559`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=d2f7856`; **QA operatore PASS** — «**QA MAJOR-5A2a PASS operatore**» (trigger auto-`finito` Regola H). **Runtime autorevole live VPS = `d2f7856`**. OPSEC/fetch/proxy/sanitizer/import-export/tile/cache invariati per scope.
- **MAJOR-5A1 — GIS Object Workbench catalogo read-only build 26 — CLOSED / PASS end-to-end:** runtime **`d74cbb7`** — build **`B5.5Z · build 26`**; blob git **`335257d4e1e02a467af149572613105a23c2bc5f`**; subject `feat(gis): add object workbench catalog`; pannello floating `#gisWorkbenchPanel`; adapter read-only `workbenchCollectRows` (waypoint `mapWaypoints[]` + tracce salvate `savedTracks[]` + poligoni `gisPolygons[]`); filtri tipo/testo; ⌖ fly-to delegato (`waypointsZoomTo`, `flyToSavedTrackById`, `polygonShowOnMapFromList`); azione Apri pannello nativo; `state._workbench` session-only (non in `saveStore`); **esclusi** pick mappa, delete, rename, edit, `gisTracks[]`, range rings, import/export, schema persistito; **`APP_BUILD_NUM = 26`**; **Review Claude NON RICHIESTA** (bundle ROUTINE read-only); **Deploy GIS-only PASS tecnico** — VPS HEAD **`d74cbb7`**, byte **2550551**, SHA-256 file **`2ec9a006b2362b5dec18cdadbcd7423e9aa039be97d1d18d5611811c8bfcb314`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=d74cbb7`; **QA operatore PASS** — «**QA MAJOR-5A1 PASS operatore**» (trigger auto-`finito` Regola H). **Runtime autorevole live VPS = `d74cbb7`**. OPSEC/fetch/proxy/sanitizer/import-export/tile/cache invariati per scope.
- **MAJOR-2BCD — Offline tile management combined program build 25 — CLOSED / PASS end-to-end:** runtime **`823bb73`** — build **`B5.5Z · build 25`**; blob git **`02a08d495671ba7e4a9684a5e7d613eb3c8bdb59`**; subject `feat(gis): harden offline tile errors and protected delete`; **programma unico 2B+2C+2D** (bundle **DELICATO** cache/storage/delete); **2B:** `classifyTileCacheError` + surfacing errori precache/cache (`formatPrecacheErrSummary`, `recordTileCacheSessionError`, `state._offlineAreaLastPrecacheErr`, warning quota in Diagnostica `diag.warn.quotaHigh`); **2C:** dialoghi scelta delete (`#offlineAreaDeleteDialog`, `#offlineAreaBatchDeleteDialog`) — metadata-only vs tile fisiche, copy i18n IT/EN/FR `offcache.area.deleteDialog*` / `offcache.sel.deleteDialog*`; **2D:** preview pre-conferma (`previewNamedAreaTileDelete`, `previewNamedAreasBatchTileDelete`, `populateOfflineArea*DeleteDialogPreview`), delete protetto (`deleteNamedAreaTilesRespectingOthers`, `deleteNamedAreasTilesRespectingOthersBatch`, `deleteNamedAreaMetadataOnlyById`), feedback post-delete (`showOfflineDeleteTilesFeedback`); batch toolbar allineata a delete fisico protetto; legacy map-rectangle delete rimosso/riallineato; **`APP_BUILD_NUM = 25`**; **Review pre-deploy PASS** (`REVIEW MAJOR-2BCD PASS — GO DEPLOY`, read-only diff `71ff501..823bb73`); **Deploy GIS-only PASS tecnico** — VPS HEAD **`823bb73`**, byte **2522536**, SHA-256 file **`7cd4c01b79f4f3392b8cebc1f5591e33a72654742778aa8d6683e4759e859dfa`**, CMP_OK, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=823bb73`; **QA operatore PASS** — «**QA MAJOR-2BCD PASS operatore**» (trigger auto-`finito` Regola H). **Runtime autorevole live VPS = `823bb73`**. OPSEC/fetch/proxy/sanitizer/import-export/`attachWheelZoom` invariati per scope review.
- **Priorità operativa post TRACK-CREATE-EDIT-UX-A:** **da scegliere da roadmap/backlog**; **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); estensioni MAJOR-3/4 import backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post TRACK-BRUSH-ANTIMERIDIAN:** landed **TRACK-CREATE-EDIT-UX-A** (`793f4cb` build 46); **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); estensioni MAJOR-3/4 import backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post TRACK-BRUSH-A:** landed **TRACK-BRUSH-ANTIMERIDIAN** (`9cc7937` build 44); **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); estensioni MAJOR-3/4 import backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post TRACK-STYLE-A:** landed **TRACK-BRUSH-A** (`d4f877a` build 42) e **TRACK-BRUSH-ANTIMERIDIAN** (`9cc7937`); **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); estensioni MAJOR-3/4 import backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post IMPORT-DROP-A:** landed **IMPORT-DROP-B** + **TRACK-MODAL-UX-A** (`1d28163`), **TRACK-STYLE-A** (`40c97b6`), **TRACK-BRUSH-A** (`d4f877a`) e **TRACK-BRUSH-ANTIMERIDIAN** (`9cc7937`); **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); estensioni MAJOR-3/4 import backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-4-a:** prossimo candidato **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); estensioni **MAJOR-3** (import unificato) backlog basso; **MAJOR-4** import/restore mission package backlog basso; programma pick Workbench **MAJOR-5A2** completo; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-3-a:** prossimo candidato **MAJOR-4** (landed **MAJOR-4-a**); **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); eventuali estensioni MAJOR-3 (import unificato) backlog basso; programma pick Workbench **MAJOR-5A2** completo; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-2E-a:** prossimo candidato **MAJOR-3/4** backlog basso; **OFFLINE-DOWNLOAD-CONTROLS** backlog (non ora); programma pick Workbench **MAJOR-5A2** completo; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-5A2-UX-BACKLOG:** prossimo candidato **MAJOR-2E-a** (landed) poi **MAJOR-3/4**; programma pick Workbench **MAJOR-5A2** completo; catena UX backlog 5A2b **chiusa**; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-5A2c:** prossimo candidato **MAJOR-5A2-UX-BACKLOG** (ROUTINE quando autorizzato) o **MAJOR-2E/3/4** backlog basso; programma pick Workbench **MAJOR-5A2** completo; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-5A2b:** prossimo candidato **MAJOR-5A2c** (pick mappa poligoni); **MAJOR-2E/3/4** backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-5A2a:** prossimo candidato **MAJOR-5A2b** (pick mappa WP+traccia); **MAJOR-2E/3/4** backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-5A1:** prossimo candidato **MAJOR-5A2** (selezione mappa↔lista + highlight); **MAJOR-2E/3/4** backlog basso; stop micro-UX salvo bug reale.
- **Priorità operativa post MAJOR-2BCD:** prossimo candidato **MAJOR-5A** (GIS Object Workbench); **MAJOR-2E/3/4** backlog basso; stop micro-UX salvo bug reale.
- **MAJOR-2A — Verificatore copertura offline read-only build 24 — CLOSED / PASS end-to-end:** runtime **`07ad4f4`** — build **`B5.5Z · build 24`**; finito docs **`28b2cc4`**; autosync **`ade8ac3`**; blob git **`b789538db128f4467e1e503b82d4e245c8de7591`**; subject `feat(gis): add offline coverage verifier`; **primo sottoblocco MAJOR-2** (read-only); helper `auditNamedAreaTileCoverage` + `runOfflineAreaCoverageAudit` + `runOfflineAreasVerifySelected`; scan tile attese vs IndexedDB (`collectNamedAreaTileKeys` + `getTileBlobByKey`); stato transiente `state._offlineAreaAudit` (session-only, non persistito); UI Mappe Offline — pulsante «Verifica» per riga + toolbar «Verifica selezionate»; feedback in-pannello + badge copertura; integrazione Diagnostica `offlineAreaAudits` (max 30 aree, solo numeri); limite 20k tile con conferma; yield async; i18n IT/EN/FR `offcache.audit.*`; **nessun** write/delete IDB; **nessun** fetch; OPSEC/fetch/proxy/geocoding/sanitizer/import-export/`attachWheelZoom` invariati; **`APP_BUILD_NUM = 24`**; **Review Claude NON RICHIESTA** (ROUTINE read-only); **Deploy GIS-only PASS tecnico** — VPS HEAD **`07ad4f4`**, byte **2502490**, SHA-256 file **`c8c5679c4c3dbe621ab0aaa2543bb184b4d9d85edfca92c4904f802c4436e1ea`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=07ad4f4`; **QA operatore PASS** — «**QA MAJOR-2A PASS operatore**» (trigger auto-`finito` Regola H). **Runtime autorevole live VPS = `07ad4f4`**.
- **Priorità operativa post MAJOR-2BCD (decisione backlog 2026-06-29):** programma **MAJOR-2BCD** **CLOSED**; prossimo candidato prioritario **MAJOR-5A** (GIS Object Workbench); **MAJOR-2E** (status persistito), **MAJOR-3** (import/export), **MAJOR-4** (mission package) backlog basso; **stop micro-UX** salvo bug reale.
- **MAJOR-1 — Diagnostica runtime / QA hardening build 23 — CLOSED / PASS end-to-end:** runtime **`9b359b7`** — blob git **`51f0781fc26f4226e5516f49bbe34aa44d25c2cf`**; subject `feat(gis): add runtime diagnostics panel`; **primo blocco programma major-feature** (read-only); pannello **Diagnostica** in Strumenti (`#sec-diagnostics`, `GIS_TOOL_SECTIONS.diagnostics`); metriche: build/runtime, OPSEC/offline/consensi, `navigator.storage.estimate()`, size `coordconv_v2`/`coordconv_ui_v1`, scan tile IDB read-only (totale + per layer), tabella `_netEvents` sessione, warning locali, Refresh, export JSON `goi-gis-diagnostics-*.json` (no geometrie/token); helper `diagCollectSnapshot`, `diagRefreshPanel`, `diagExportReport`; i18n IT/EN/FR `diag.*`; sanitizer/OPSEC gate/fetch tile/cache write/import-export/`state.mapWaypoints[]`/`attachWheelZoom` invariati; **`APP_BUILD_NUM = 23`**; display **`B5.5Z · build 23`**; **Review Claude NON RICHIESTA** (bundle ROUTINE read-only); **Deploy GIS-only PASS tecnico** — VPS HEAD **`9b359b7`**, byte **2485630**, SHA-256 file **`db61845988dee8a60ae4e679ee4ea5dd3e0392c926b20cfc7c61c1d6f4a299ea`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=9b359b7`; **QA operatore PASS** — «**QA MAJOR-1 PASS operatore**» (trigger auto-`finito` Regola H). Superseded per runtime live da **MAJOR-2A** (`07ad4f4`).
- **UX-NEXT-RUNTIME-BUNDLE-E — consolidamento UX GIS pannelli/mappa build 22 — CLOSED / PASS end-to-end:** runtime **`fb871b7`** — blob git **`3c5759048a8572b10e1271dd6d6759949dd1fc98`**; subject `feat(gis): consolidate map UI and panel UX`; **bundle ROUTINE** (coda `finito` pre-autorizzata; trigger auto da QA PASS Regola H); **18 interventi** (17 pieni + 1 parziale CSS cleanup): focus-visible toolbar/zoom/strati; zoom group `aria-label` + `tip.zoomLevel`; scale bar polish; HUD centro `gis.hud.centerFmt` + chip build `build 22` con tooltip `APP_BUILD_LABEL`; empty states Preferiti/Tracce/Poligoni/Waypoints/Offline/Range Rings con hint; micro-help Cerca/Misura/Layers/basemap; `tip.modalClosePanelEsc` su 6 pannelli GIS floating; mobile toolbar hit target 36px; scroll body pannelli; `attachWheelZoom` **non toccato**; sanitizer/OPSEC/rete/tile/proxy/cache/import-export/geometrie/`state.mapWaypoints[]` invariati; **`APP_BUILD_NUM = 22`**; display **`B5.5Z · build 22`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`fb871b7`**, byte **2455175**, SHA-256 file **`19bf6dc9c896d571d5babf564477a451990783cc05afa1773676907c84bd9c11** match, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled; **QA operatore PASS** — «**QA UX-NEXT-RUNTIME-BUNDLE-E PASS operatore**»; URL `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=fb871b7`. Superseded per runtime live da **MAJOR-1** (`9b359b7`).
- **UX-NEXT-RUNTIME-BUNDLE-D — HUD/resize polish + wheel zoom controllato build 19→21 — CLOSED / PASS end-to-end:** catena runtime **`ec86b62`** (bundle base build **19** — polish HUD `#gisMapHud` compact/chip/status/muted; focus-visible HUD e handle resize e/w; `GIS_PANEL_EW_RESIZE_KEYS`; `gisPanelResetEwWidth` doppio-clic ripristino larghezza; `formatHudCenterCompact` / `mapLayerHudLabelShort`; `tip.panelResizeEW` IT/EN/FR; CSS scroll orizzontale contenuto body pannelli; blob **`48d471377e299f5bb82d7e8dd2bd0b94b9a6dce1`**; deploy GIS-only **non chiuso separatamente** — assorbito in catena) + **`5fec693`** (**D-FIX1** build **20** — `attachWheelZoom` debounce 50 ms ±1 per burst; **QA operatore FAIL** — singolo scatto rotellina ancora 2–5 livelli su rotelline ad alta risoluzione) + **`19700b6`** (**D-FIX2** build **21** — `attachWheelZoom` wire-once su `#miniMap`, gesture idle **140 ms**, cooldown post-step **100 ms**, max ±1 per gesto fisico; blob **`65b7293ab229b2a37cb3f1ec03666f565900c73e`**); **Review Claude NON RICHIESTA** (bundle ROUTINE UX); **Deploy GIS-only PASS tecnico** — VPS HEAD **`19700b6`**, byte repo/servito **2446515**, SHA-256 **`7019c56e063659bd974e68a365a19bab2029bfef25dbd8285e78c253af90312a`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active (bind `100.114.7.53:8000`); URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=19700b6`; **QA operatore PASS** — attestazione «**QA UX-NEXT-RUNTIME-BUNDLE-D-FIX2 PASS operatore**» (trigger auto-`finito` Regola H); wheel max 1 livello per scatto; pan/±/resize/HUD OK; **`APP_BUILD_ID` `B5.5Z` invariato**. **Runtime autorevole live VPS = `19700b6`**. **Nessun pending BUNDLE-D.**
- **UX-NEXT-RUNTIME-BUNDLE-C — resize pannelli residui + HUD polish build 18 — CLOSED / PASS end-to-end:** runtime **`8f56566`** — blob git **`44a9ce09be9a977d304b41c152f35451d4110f8d`**; subject `feat(gis): extend residual panel resize and HUD polish`; **bundle ROUTINE** (coda `finito` pre-autorizzata; trigger auto da QA PASS Regola H); **9 item:** (1) resize e/w **`#rangeRingsPanel`**; (2) e/w **`#measurePanel`**; (3) e/w **`#helpOverlay`**; (4) e/w **`#waypointModal`** (4° pannello residuo sicuro); (5) affordance resize comune (handle/grip/hover/touch, `tip.panelResizeEW` IT/EN/FR); (6) HUD collision polish (offset angoli, mobile safe-area); (7) HUD tooltip/aria/i18n (`gis.hud.toolbar`, chip `data-hud-tip`); (8) HUD reset session-only (`↺` → tl + expanded, `gis.hud.reset`); (9) cleanup i18n orphan `gis.polygonPanel.renameLabel`/`renameSave`; CSS e/w esteso a 9 pannelli (inclusi A/B); sanitizer/OPSEC/rete/tile/proxy/cache/import-export/geometrie/`state.mapWaypoints[]` invariati; **`APP_BUILD_NUM = 18`**; display **`B5.5Z · build 18`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`8f56566`**, byte repo/servito **2440464**, SHA-256 file **`8ae47bdf76af3496ab2f1d295911e3fab7dca984e09a0606f1538704dab36d1e`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled (bind `100.114.7.53:8000`; warmup ~3s post-restart); URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=8f56566`; **QA operatore PASS** — attestazione «**QA UX-NEXT-RUNTIME-BUNDLE-C PASS operatore**». **`APP_BUILD_ID` `B5.5Z` invariato**. **Runtime autorevole live VPS = `8f56566`**.
- **UX-NEXT-RUNTIME-BUNDLE-B — resize multi-pannello + HUD avanzato build 17 — CLOSED / PASS end-to-end:** runtime **`584135e`** — blob git **`40f435685e473ebd2cd5e77995cc980b88c8a868`**; subject `feat(gis): expand panel resize and HUD controls`; **bundle ROUTINE** (coda `finito` pre-autorizzata; trigger auto da QA PASS Regola H); **8 item:** (1) resize laterale e/w **`#polygonPanel`**; (2) e/w **`#trackModal`**; (3) e/w **`#layersPanel`** (Mappe Offline); (4) e/w **`#searchPanel`** (4° pannello sicuro); (5) HUD angoli session-only `tl/tr/br/bl` (pulsante ghost `⊞`); (6) HUD compatto/collassabile (`−`/`+`); (7) chip extra centro mappa (`state.viewCenter`) + Seamarks se attivo; (8) polish layout/z-index/mobile + ignore drag resize su Search/Layers/Track; CSS e/w generalizzato da pilota Preferiti; i18n IT/EN/FR `gis.hud.cornerCycle`, `compact`, `expand`, `center`, `seamarks`; sanitizer/OPSEC/rete/tile/proxy/cache/import-export/geometrie/`state.mapWaypoints[]` invariati; **`APP_BUILD_NUM = 17`**; display **`B5.5Z · build 17`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`584135e`**, byte repo/servito **2434043**, SHA-256 file **`c303e69a9b3c74c1b1cffa87f30e50969cee1953f7b4643ee708920aa2a4479f`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled (bind `100.114.7.53:8000`); URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=584135e`; **QA operatore PASS** — attestazione «**QA UX-NEXT-RUNTIME-BUNDLE-B PASS operatore**». **`APP_BUILD_ID` `B5.5Z` invariato**. Superseded per runtime live da **BUNDLE-C** (`8f56566`).
- **UX-NEXT-RUNTIME-BUNDLE-A — resize pilota Preferiti + HUD GIS + cleanup rename bar build 16 — CLOSED / PASS end-to-end:** runtime **`61bcda5`** — blob **`5bf008d739ba5679e64605fd3e6f9a3fb9644836`**; subject `feat(gis): add UX runtime bundle for panel resize and HUD`; **bundle ROUTINE** (coda `finito` pre-autorizzata; trigger auto da QA PASS Regola H); **3 item:** (1) resize laterale pilota **`#favoritesPanel`** — handle `e`/`w` + estensione `gisPanelAttachResize` larghezza-only; (2) HUD leggero **`#gisMapHud`** — chip layer/zoom + warning `forceOffline`/`opsecStrict` (solo stato locale, nessuna rete/GPS/polling); (3) cleanup dead code **`#polygonPanelRenameBar`** / `polygonHideRenameBar` (rename inline invariato); i18n IT/EN/FR `tip.panelResizeEW`, `gis.hud.*`; sanitizer/OPSEC/rete/tile/proxy/cache/import-export/geometrie/`state.mapWaypoints[]` invariati; **`APP_BUILD_NUM = 16`**; display **`B5.5Z · build 16`**; **Review Claude NON RICHIESTA**; **Deploy GIS-only PASS tecnico** — VPS HEAD **`61bcda5`**, VPS blob match, byte repo/servito **2426256**, SHA-256 **`2684590171b5f260dd0ea7c5d04fdb189fabbc7411285a39effd72e2e88a5b5b`**, CMP_PASS, HTTP **200**, `goi-gis-app.service` active/enabled (bind `100.114.7.53:8000`); URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=61bcda5`; **QA operatore PASS** — attestazione «**QA UX-NEXT-RUNTIME-BUNDLE-A PASS operatore**» (resize Preferiti; HUD layer/zoom; poligoni rename inline/Modifica/ESC; Layers/pan-zoom OK). **`APP_BUILD_ID` `B5.5Z` invariato**. **Runtime autorevole live VPS = `61bcda5`**.
- **METHOD-QA-PASS-AUTO-FINITO — codifica trigger auto-finito dopo QA PASS (2026-06-28, docs/rules-only) — CLOSED / PASS:** commit task **`78ea6c9`** + autosync **`bacabef`**; regola «QA-PASS AUTO-INNESCA FINITO» (OM §4 Regola H): nei prompt bundle runtime la riga `QA <BLOCK-ID> PASS operatore` innesca automaticamente la coda `finito` pre-autorizzata; template coda in OM §4 + `docs/QA-CHECKLIST.md`; aggiornati `README.md`, `.cursor/rules/30-output-workflow.mdc`, `docs/HANDOFF.md`; elimina giro separato «QA PASS → ora lancia finito»; monolite invariato blob **`71e353ee85c15bf2713bc7998c72582f81723ec5`**; runtime VPS live **`7b8cf04`** build 15; QA operatore **n/a** (blocco metodo). **Metodo vivo:** METHOD-BUNDLING-DEFAULT + METHOD-QA-PASS-AUTO-FINITO.
- **docs/HANDOFF.md method A — handoff canonico repo — CLOSED / PASS docs-only (2026-06-27):** creato [`docs/HANDOFF.md`](HANDOFF.md) — seed operativo breve (regola stato, read-set, precedenza, ruoli, disciplina Cursor, gate Ramo A/B, deploy GIS-only, PASS, snapshot stato fresco, backlog UX/cleanup); puntatore aggiunto in README read-set; **non** sostituisce OM §7; monolite invariato; nessun deploy.
- **P-STYLE — stile poligoni (schema, export, editor) — CLOSED / PASS end-to-end:** catena runtime **`7ffee22`**/**`95c100d`** (P-STYLE-A+B — sanitizer polygon-scoped `fillColor`/`fillOpacity`/`lineWidth`/`lineOpacity` preserve-or-omit; `polygonResolveStyleProps`; rendering saved/edit; **review byte Claude PASS** su fix review **`95c100d`**, blob **`4a8463b1c6d71cde60d7bfe24a48049e6e3121ef`**) + **`efca0bf`** (P-STYLE-D — export GeoJSON whitelist + KML `ExtendedData` + stile visuale condizionale; legacy KML byte-invariato; **review byte Claude PASS**, blob **`ac8a7c30d3530ab3e92bd80e81a811449e935788`**) + **`0a51379`** (P-STYLE-C — UI editor stile working-copy `#polygonPanelEditStyleWrap`; `_polyEdit.styleOriginal/styleWorking/styleDirty`; preview live; salvataggio solo `polygonSaveEdit`→`gisFeatureUpdate`; **gate orchestratore PASS** — solo UI/working-copy; nessun sanitizer/export/import/create-path/`saveStore` diretto; nessun nuovo campo persistito; FR congelato; **review Claude NON RICHIESTA**); monolite finale blob **`8d13e41a36fe7cc0605dc8f315eff551725340ed`**. **P-STYLE-E deploy GIS-only PASS tecnico** (HEAD VPS **`0a51379`**, byte **2340941**, SHA-256 **`a822533215ebe5c48ea33ee4fe0fc9397c2f1d237de8a92a87535299a93fc937`**, cmp PASS, HTTP **200**, `goi-gis-app.service` active/enabled; bind Tailscale `100.114.7.53:8000`; Planet-Clone/Navionics proxy/Docker/n8n/Tailscale firewall **non toccati**). **QA operatore PASS** — attestazione «**QA P-STYLE PASS operatore**» (successiva al blocco README bootloader **`c409819`**, che registrava correttamente P-STYLE come pending in quel momento). Chiusura documentale docs-only separata dal blocco README. **Import file dedicato verso `state.gisPolygons[]` non implementato** (fuori scope catena). **`APP_BUILD_ID` `B5.5Z` invariato**. **Nessun pending P-STYLE.**
- **Governance i18n (decisione 2026-06-25, vincolante blocchi futuri):** italiano = lingua principale; inglese = seconda lingua mantenuta; francese = lingua legacy congelata — **non** aggiungere nuove stringhe FR; **non** modificare stringhe FR esistenti; nuovi sviluppi UI solo chiavi IT/EN; stringhe FR già presenti **non** cancellate in questo intervento; rimozione FR da selettore/dizionari/runtime = blocco separato approvato; nessuna chiave mancante che destabilizzi il runtime. Sostituisce per i **nuovi** blocchi il requisito operativo di aggiornare sempre IT/EN/FR. **Nessuna modifica retroattiva** al monolite in questa chiusura.
- **POLY-PARITY-P7-A — diagnosi read-only metadata/data poligono — completata:** mappa sanitizer condiviso Tracce/poligoni; fabbricazione `created_at`/`updated_at` in `gisSanitizeProperties`; gap bump/export; split **P7-B1** (dati) / **P7-B2** (UI) approvato; nessuna modifica runtime.
- **POLY-PARITY-P7-B1 — contratto metadata/data poligono legacy-safe — CLOSED / PASS end-to-end:** runtime **`57c6d39`**; review byte Claude **PASS**; deploy VPS GIS-only **PASS tecnico** (runtime servito `57c6d39`, byte/SHA match); **QA operatore P7-B1 PASS** — attestazione «**POLY-PARITY-P7-B1 PASS operatore**»; sanitizer poligoni preserva-o-ometti; Tracce invariate; bump `updated_at` su Salva dirty/rinomina; export GeoJSON `updated_at` valido; **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-PARITY-P7-B2 — UI date Creato/Aggiornato in Modifica — CLOSED / PASS end-to-end:** runtime **`47bb3f6`** — righe Creato/Aggiornato in `#polygonPanelEditSummary`; lettura `created_at`/`updated_at` dal feature canonico (`gisFeatureGet`); `polygonFormatPropertyTimestamp` (locale IT/EN/FR; fallback legacy-safe); contratto P7-B1 byte-invariato; nessun backfill; deploy VPS GIS-only **PASS tecnico** (runtime servito `47bb3f6`, blob `f90599e3eaf82071faf008b5c8b91c594091ca46`, byte **2279137**, SHA-256 **`4b8022887b50600df254344917e09e37b27706d09caada34feca405b7074db75** match, cmp PASS, HTTP **200**); **QA operatore PASS** — attestazione «**QA POLY-PARITY-P7-B2 PASS operatore**»: date locali in Modifica; `created_at` invariato dopo Salva; `updated_at` aggiornato alla riapertura; i18n IT/EN/FR; legacy senza errori; P2/A2/lista invariati; **`APP_BUILD_ID` `B5.5Z` invariato**. **Catena POLY-PARITY-P7 metadata/data completata end-to-end** (P7-A + B1 + B2).
- **POLY-UX-STABILITY-A-DIAG — diagnosi read-only handle/pannello — completata:** `renderAllMaps` indefinita (6 call site, 0 definizioni) silenzia refresh in `polygonRefreshEditUi`; auto-minimize draw ≠ close (A2 backlog); nessuna patch runtime.
- **POLY-UX-STABILITY-A1 — handle vertici visibili ingresso Modifica — CLOSED / PASS end-to-end:** runtime **`af87259`**; review byte Claude **PASS** (deviazione ratificata: piano autorizzava `refreshTileMapForTrackUi()` nel RAF; implementato `renderTileMap()` diretto con precedenza `viewCenter`→`lastPoint` — pan aggiorna `viewCenter`, `lastPoint` può essere marker precedente; evita salto mappa in ingresso Modifica; codice non riscritto; lezione A1: deviazione da vincolo approvato va segnalata prima dell'applicazione, non silenziosamente); deploy VPS GIS-only **PASS tecnico** (HEAD clone VPS `0e756d2`, runtime servito **`af87259`**, blob `57df10c8f654ba850be4044ea2400efddb3b74c0`, byte **2276652**, SHA-256 **`60f5fea9925350a9cbbbeabcc834f82939abc7caeb0f68ba6243c873dfd0c607** match, cmp PASS, `goi-gis-app` active/enabled, HTTP **200**; proxy/Planet-Clone/Docker/n8n non toccati); **QA operatore A1 PASS** — attestazione «**QA POLY-UX-STABILITY-A1 PASS operatore**»: handle immediati in Modifica; nessun pan/zoom per comparire; nessun salto mappa (pannato, lastPoint, stato fresco); drag vertice; nessun pan concorrente; info live; Annulla/Salva/riapertura Modifica OK; fix `polygonScheduleEditOverlayRefresh` (RAF+token+guardie), rimossa `renderAllMaps` da `polygonRefreshEditUi`; P2 drag/CTRL_SEL/P7-B1 invariati; **`APP_BUILD_ID` `B5.5Z` invariato**; **A2 non toccato**; 5 occorrenze `renderAllMaps` residue fuori scope A1.
- **POLY-UX-STABILITY-A2-B1 — toggle restore-first pannello Poligoni minimizzato — CLOSED / PASS end-to-end:** runtime **`db2f6ea`**; `isOpen()` nei due toggle esclude `gisPanelIsMinimized("polygonPanel")`; minimizzato → `openPolygonPanel()` restore-first; deploy VPS GIS-only **PASS tecnico** (runtime servito `db2f6ea`, byte **2276938**, SHA-256 **`d0f0e9a650b19825783f80ee6487714d30da6942af6c6ba92202ff5e2974903e** match, `goi-gis-app` active/enabled, HTTP **200**); **QA operatore A2-B1 PASS** — attestazione «**QA POLY-UX-STABILITY-A2-B1 PASS operatore**»; **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-UX-STABILITY-A2-B2 — chiusura edit-safe pannello Poligoni — storico PARTIAL FAIL, superseded da A2-B2-FIX:** runtime **`cb9f92f`** — `closePolygonPanel()` → `polygonEditCancelHandler()`; rollback logico PASS (`_polyEdit` nullo, nessun `gisFeatureUpdate`/`saveStore`); deploy VPS **PASS tecnico**; **QA operatore PARTIAL FAIL** — handle/geometria transitoria stale fino al pan (redraw canonico finale assente); difetto risolto e superseded da **A2-B2-FIX**.
- **POLY-UX-STABILITY-A2-B2-FIX — redraw post-close edit pannello Poligoni — CLOSED / PASS end-to-end:** runtime **`70ed7b3`** — dopo close con edit: cancel RAF A1 pendente + `renderTileMap` sincrono singolo (`viewCenter`→`lastPoint`, `mapZoom||11`); nessuna persistenza; deploy VPS GIS-only **PASS tecnico** (runtime servito `70ed7b3`, blob `bf7a78a99cb196442c41bfc7373b95e76d256a3b`, byte **2277805**, SHA-256 **`38bfe507270c688d4f34727724ca81ee6301adb6f3cdd1625a476bc534470ccc** match, cmp PASS, `goi-gis-app` active/enabled, HTTP **200**); **QA operatore PASS** — attestazione «**QA POLY-UX-STABILITY-A2-B2-FIX PASS operatore**»: chiusura X/toolbar durante edit dirty — handle e geometria transitoria rimossi immediatamente, canonico ripristinato senza pan/zoom; Salva→chiudi conserva geometria; A2-B1 restore-first invariato; riapertura Modifica pulita; **`APP_BUILD_ID` `B5.5Z` invariato**.
- **POLY-UX-STABILITY-A2-B3 — apertura pannello senza auto-arm disegno — CLOSED / PASS end-to-end:** runtime **`87cbe64`** — rimosso auto-`polygonStartDraw()` da `openPolygonPanel()`; apertura toolbar: pannello/lista visibili, nessun minimize/chip/drawing; disegno solo tramite `#polygonPanelNewBtn` (`Nuovo poligono` → `polygonStartDraw()`); minimizzazione/chip invariati su drawing esplicito; deploy VPS GIS-only **PASS tecnico** (runtime servito `87cbe64`, blob `0ab2498a8c80d3de0fefe3c3f302ced8969c3dcf`, byte **2277386**, SHA-256 **`680ec757381bf962436682be64112f66c42b59600b1b151ead65524aacc08b6f** match, cmp PASS, HTTP **200**); **QA operatore PASS** — attestazione «**QA POLY-UX-STABILITY-A2-B3 PASS operatore**»; A2-B1 restore-first, A1/P2 Modifica e A2-B2-FIX invariati; **`APP_BUILD_ID` `B5.5Z` invariato**. **Catena POLY-UX-STABILITY A2 completata end-to-end** (B1 + B2-FIX + B3; B2 storico superseded).
- **Standardizzazione modal trasversale (backlog WU-0006, rollout iniziato):** tutti i modal/pannelli operativi GIS devono occupare l’**altezza utile** della viewport (margini/safe-area app), con scroll interno del corpo — riferimento UX **`#measurePanel`** / Range & Bearing (`openMeasureFloatingPanelGis`, `_measurePanelLayoutOpts`); pattern full-height già presente su Range Rings B6.4a-2 (`defaultHeightFraction: 0.92`); header/azioni sempre raggiungibili; coerenza desktop/mobile/touch; compatibile minimizza/ripristina e modalità mappa; **non** include `alert`/`confirm` nativi; implementazione a blocchi per-modal. **Landed:** **MODAL-STD-SEARCH-B1** (`33c95ad`, build 10) — `#searchPanel`; **MODAL-STD-B2** (`06ed2a0`→`266b116`, build 11→13, catena FIX1/FIX2) — `#favoritesPanel` layout/scroll/close/ESC + ESC `#polygonPanel`; **UX-NEXT-RUNTIME-BUNDLE-A/B/C/D** (`61bcda5`/`584135e`/`8f56566`/`19700b6`, build 16→21) — resize laterale e/w + HUD + wheel zoom controllato. **Prossimi candidati:** micro-blocchi da audit (nessun obbligo fisso); titolo statico `<title>`. Dettaglio: roadmap §WU-0006 estensione backlog.
- **Measurement label collision avoidance — WU-0007 B4X1/B4X2 — CLOSED / PASS end-to-end:** B4X1 **`debd5b4`** — QA operatore **FAIL** storico (pill desincronizzato; offset eccessivo segmenti quasi nulli). **B4X2** **`d4b73bb`** — `layoutPill()` + tuning offset; deploy tecnico PASS HEAD **`f5854c3`**; smoke HTTP 200, byte/SHA match; **`APP_BUILD_ID` `B5.5Z`**. **QA operatore B4X2: PASS** — attestazione «QA WU-0007 B4X2 PASS operatore»: pill allineato al testo; offset corretto su segmenti lunghi e quasi nulli; linea/marker/freccia visibili; zoom/drag/Esc/Clear senza regressioni pertinenti; app utilizzabile. **B4 righello PASS** (`54d8586`) invariato; nessun fix aperto su questo tema.
- **WU-0007 T1 — unità distanza/velocità modal Traccia — CLOSED / PASS end-to-end:** runtime **`002624e`** — selettore persistente `state.trackDisplayUnit` (`km` | `nm` | `mi`, default `km`); helper display `formatTrackDistance` / `formatTrackSpeed`; distanza totale, segmenti, leg chiusura e colonna archivio `#savedTracksMount` disaccoppiati da `mapMeasureUnit`; etichette ETA cammino/auto con velocità di riferimento dinamica (`km/h` | `kn` | `mph`) — calcolo temporale ETA invariato (4/60 km/h fisici); dati canonici in metri invariati; normalizzazione UI visibile `NM` (Misura + scala + helper misura); deploy tecnico PASS HEAD **`d533e8b`**; smoke HTTP 200, byte **2 235 808** e SHA-256 match **`d8bc2b49e6bf1a90402c189995b53d630277fb7d8fd96b0dff1787fc218775f2**; **`APP_BUILD_ID` `B5.5Z` invariato**. **QA operatore T1: PASS** — attestazione «QA WU-0007 T1 PASS operatore»: selettore `km`/`NM`/`mi` funzionante; summary, segmenti, leg chiusura e archivio aggiornati; label ETA coerenti in `km/h`/`kn`/`mph`; tempo ETA invariato al cambio unità; persistenza dopo reload; funzionamento traccia regolare; UI `NM` corretta; nessuna regressione operativa pertinente. **Nessun fix aperto su T1.** Backlog futuro (non implementato): velocità di pianificazione ETA configurabile — vedi roadmap §WU-0007 T1.
- **WU-0007 T1-FLOAT — float Traccia allineato a `trackDisplayUnit` — CLOSED / PASS end-to-end:** runtime **`e92e301`** — `updateTrackMapFloatReadout()` e picker `[data-role="track-float-unit"]` usano `normalizeTrackDisplayUnit(state.trackDisplayUnit)` + `formatTrackDistance` (non più `mapMeasureUnit` / `formatMapMeasureDistance`); opzioni float **km / NM / mi** (rimossi `m` e `ft`); cambio unità nel float scrive `trackDisplayUnit`, `saveStore()` e aggiorna solo il readout float — **nessun** side-effect su Misura (`mapMeasureUnit`, `renderMapMeasureOverlay`, `updateMeasureReadouts`); float indipendente dal Measure box; float mostra **solo distanza** (nessuna velocità); metri canonici, geometria, ETA, import/export e storage invariati; coerenza con modal Traccia alla riapertura; modal Traccia, archivio e listener Misura invariati; deploy tecnico GIS-only **PASS** HEAD **`8995239`**; VPS allineata; blob Git monolite **`7c5350e0a1888317a0fc717e01f6c085ba579091`**; smoke HTTP **200**, byte **2 243 669**, SHA-256 file VPS = body HTTP **`2e4afcea5160f584fe11f8487854218941120dd6a55878cdeda5e2268e3dd362**; URL QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=e92e301`; **`APP_BUILD_ID` `B5.5Z` invariato**. **QA operatore T1-FLOAT: PASS** — attestazione «**QA WU-0007 T1-FLOAT PASS operatore**»: picker float `km`/`NM`/`mi` aggiorna readout distanza immediatamente; Measure box mantiene unità propria indipendente dal float; modal Traccia riaperta mostra stessa unità del float. **Nessun fix aperto su T1-FLOAT.**
- **UX toolbar laterale:** WU-0007 PASS — B1/B2 pulsanti più piccoli + Layers allineato (`e4c2be3`), B3 GPS label + colore qualità PASS (`c051ee1`), B4 distanza → righello PASS (`54d8586`), B5 waypoint/posiziona/torna in gruppo espandibile PASS (`7a02a7e`), B6 poligoni dentro Tracce (`e8395e9`), B7 MGRS dentro Layers PASS (`74d3f32`), B8 Range & Bearing dentro Tracce (`fa12567`). Estensione **B4X1** label misura (sopra) — non altera stato PASS B4. Restano fuori da WU-0007: standardizzazioni modal trasversali (WU-0006 backlog).
- **Layers UI grouped by map type:** PASS runtime (`a656541`) — menu Layers in tre sezioni (Topografica / Satellitare / Nautica), provider labels attive, selezione layer OK; test operatore senza regressioni su basemap, Navionics/SonarChart, MGRS e strumenti GIS. README bootloader già allineato (`b0a3de0`); nessun sync roadmap/README richiesto per questo blocco.
- **Basemap XYZ aperti:** WU-0008 PASS runtime (`cf6d796`) — aggiunto OSM HOT, normalizzato CARTO Voyager (`maxZoom: 20`, label), OpenTopoMap già presente mantenuto senza duplicazione; fix `resolveCatalogLayerId` per attivazione `osmHot`. I layer restano governati dai gate tile/rete esistenti (`tileFetchAllowed(layerId)`, `state.forceOffline`, `state.opsecStrict`) e dal flusso cache/export esistente. WU-0009 Google/Bing proxy resta filone separato e sensibile.
- **Motore tile tileScheme (WU-0008 8c-A):** PASS runtime (`cddc565`) — helper `normalizeTileScheme` / `formatTilePath` / `buildTileUrl`; layer `sat` su `urlBase` + `tileScheme: "zyx"`; fetch centralizzati; `makeTileKey` invariato.
- **Esri basemap catalogo (WU-0008 8c-B):** PASS runtime (`043b769`) — aggiunti `esriTopo`, `esriStreet`, `esriHillshade`, `esriRelief`, `esriOceanBase` via `urlBase` + `tileScheme: "zyx"`, `cacheable: false` (online-only); menu Layers Topografica/Nautica + i18n IT/EN/FR; `sat`/Navionics/SonarChart/seamarks invariati; endpoint verificati HTTP 200; `node --check` OK. Ocean Reference escluso. Browser QA operatore: da confermare se non già fatto.
- **Browse-cache guard `cacheable:false` (WU-0008 8d-B0):** PASS runtime (`23db6b4`) — `parseTileKeyLayerId` + guard fail-open in `cacheTileFromDisplay`: layer con `cacheable === false` (osmStandard, Esri 8c-B, futuri online-only) non persistono più in IndexedDB durante pan/zoom; layerId dalla `tkey` tile, non `state.mapLayer`; precache/export guard invariati; `node --check` OK. Browser QA IndexedDB before/after: PASS operatore (esriTopo/osmStandard 0→0, osmHot 0→96). Prerequisito per 8d-B (EOX).
- **Offline UX / cache per-layer / maxZoom (WU-0008 8d-B1):** **8d-B1-A PASS** (diagnosi) + **8d-B1-A-docs PASS** (roadmap). **8d-B1-B1 PASS** (`29ebf3a`) — badge «no offline», `OFFLINE_LAYER_IDS` catalogo-driven, pannello neutro, contatore cache nascosto su online-only, layer attivo verde. **8d-B1-B2 PASS** (`a0da9d1`) — stats IndexedDB reali tile/MB per-layer in `#pcLayerCacheStat`; scan O(n) on-demand. **8d-B1-B3 PASS** runtime (`finito` 2026-06-15) — `clampBasemapFitZoom` su `flyMapToTrackPoints` (×2) e `flyMiniMapToOfflineNamedAreaById`; maxZoom da `TILE_LAYERS[sanitizeMapLayer(state.mapLayer)]`; debito fit-area z18 **risolto**; `node --check` OK.
- **WU-0008 8d-B EOX:** **PASS** runtime (`2ca47b6`) + **Browser QA operatore PASS** — `eoxS2Cloudless` online-only (`cacheable:false`, `maxZoom:18`); gate fetch fail-closed allowlist; QA: localhost/tailnet `100.110.35.23` visibile+carica; `192.168.1.108` nascosto e no fetch se forzato; forced-offline/OPSEC strict/no offline bulk OK. Dettaglio: [`docs/work-units/WU-0005-0009-roadmap.md`](work-units/WU-0005-0009-roadmap.md) §8d-B.
- **Backlog metodo / adozione metodo (post-catena 8d):** catena **PASS** fino a **Fase F3** — Blocco 0/B + Fasi C–E + **F1** (spec + template), **F2** (collaudo report vivo), **F3** (obbligo `LAST_CURSOR_REPORT` GIS-only). Non WU tecnica runtime; dettaglio hash e cronologia: roadmap §Backlog metodo. `LAST_CURSOR_REPORT` obbligatorio post-push per task reale GIS-only; **non** per read-only, plan, review diff senza commit o micro-fix docs-only dove esplicitamente escluso — evidenza rolling post-push, **non** fonte viva primaria; **OM §7 e roadmap restano primari** (cfr. §4).
- **Governance online/offline:** WU-0005 B0/B1 documentata — regola: GIS online di default; `state.forceOffline` è interruttore volontario dell’operatore; `state.opsecStrict` resta gate superiore per chiamate di rete sensibili. Mappati i gate esistenti nel monolite: `isEffectivelyOnline()`, `tileFetchAllowed(layerId)`, `internetApiFetchAllowed()`, consenso proxy/Navionics-like (`ensureNavProxyConsent()`, `_navProxyConsentGranted`), geocoding (`geocodingAllowed()`, Nominatim/`reverseGeocode`), tile/layer fetch (`hydrateMapTiles`, `fetchAndStoreTile`), cache/offline maps, seamarks/SonarChart, tracking rete (`recordNetEvent`, `_netEvents`). Eventuali ambiguità UI/copy vanno trattate in micro-patch B2 separata.
- **Mappe offline UX (backlog):** micro-fix «Scarica selezionate» su aree COMPLETATA + copy IndexedDB vs file PC (`345d712`). Revisione UX futura: etichette cache vs export JPG, azione export da riga area, feedback fine job — candidato non-WU.
- **WU-0009B Google Satellite (`gsat`):** **PASS end-to-end** + **Browser QA operatore PASS (OPSEC strict, 2026-06-16)** — backend Planet-Clone `a7359e7` (`/gsat/`), frontend GIS `013b8cb`, deploy VPS verificato (`/status` OK, tile `200 image/jpeg`); browser operatore via Tailscale su GIS `100.114.7.53:8000` + proxy `:5000`; TEST 1–8 PASS (consenso Google ≠ Navionics/SonarChart; Annulla fail-closed; tile visibili post-consenso; placeholder su area non-cache senza consenso). Runtime: [`docs/runtime/VPS_DEPLOY_RUNTIME.md`](runtime/VPS_DEPLOY_RUNTIME.md). **Backlog WU-0009B residuo:** altre varianti Google, polish UI (B5), reboot-test VPS formale.
- **Nota anti-reflow §7 (mega-bullet WU-0009B):** il bullet combinato sotto (WU-0009B B4 → B5.1/B5.2/B5.3/B5.4/B5.4a-d → B6.1) va editato **solo** per clausola puntuale (es. cambio stato QA di una sotto-voce); **mai** riscrivere, rifloware o riformattare l'intero bullet; voci nuove, blocchi e backlog = bullet **separati** fuori dal mega-bullet; dopo edit §7, verificare blob parti non-target invariato (cfr. §4 passo 6 *published = verified*).
- **WU-0009B B4 Bing Satellite (`bsat`) — catena B4 PASS end-to-end (2026-06-17):** **B4.1A/B4.1A-VPS** smoke strategia C PASS; **B4.1B–B4.1C** proxy Planet-Clone **`1e8944d`** route `/bsat/` deploy VPS PASS; **B4.2 frontend GIS** runtime **`8d4deab`** — layer `bsat` (catalogo, gate OPSEC consenso `bing`, UI Layers, offline-eligible); **PASS tecnico statico**; deploy frontend GIS VPS **`fe6b289`** verificato; **B4.4 Browser QA OPSEC strict PASS operatore** (7/7 step); **B4.3A annullato** — `#setOpsecStrict` già presente (Guida → Geocoding → Impostazioni geocoding). **B5.1 polish UX (2026-06-17):** label `set.opsec.strict` ampliata (non solo geocoding); help-line `set.opsec.strictHelp` sotto toggle; **nessun secondo toggle**; gate/listener OPSEC invariati; hint Layers Satellitare **differito** (menu basemap ricostruito via `basemapLayersHtml`/`tlayerBasemapBtn` — rischio wipe se iniettato nel rebuild). **B5.1 Browser QA visuale:** PASS operatore. **B5.2 mobile viewport containment (2026-06-18):** PASS tecnico — meta `viewport-fit=cover`; CSS mobile-only `@media (max-width:768px)` toolbar scroll, header wrap, modal/drawer max-size, dialog OPSEC Conferma/Annulla sticky; nessuna logica GIS/OPSEC modificata. **B5.3 legenda scala multi-unità (2026-06-18):** PASS tecnico — `buildScaleBar` metrica (toggle m/km, poi rimosso in B5.3a); mi secondario, Nm, ratio 1:N. **B5.3a (2026-06-19):** PASS tecnico — toggle m/km rimosso; barre graduate 10 tacche CSS; **superata da B5.3b** per layout label. **B5.3b (2026-06-19):** PASS tecnico — fix overlap label centrale metrica vs caption `km · mi` (mid-label in flow + spacing box scala); runtime **`ad7d977`**. **QA visuale bundlata B5.1+B5.2+B5.3b (2026-06-19):** **PASS operatore** — attestazione **operatore** post-deploy VPS **`fec53ca`** (smoke HTTP `Content-Length: 2107749`; URL tailnet `:8000/coordinate_converter%20Claude.html?v=fec53ca`); copre B5.1 OPSEC label/help, B5.2 mobile viewport, B5.3b scala finale; **non** attestata da AI. **Backlog UX residuo:** hint statico Layers (B5.x). **B5.4 export JPEG scala opzionale (2026-06-19):** PASS tecnico — checkbox «Includi scala» nel dialog export JPG; scala disegnata su canvas 2D (`drawJpgExportScale`, riuso `computeMapScaleModel`); nessun `foreignObject`/raster HTML `.tile-scale`; export senza scala invariato (default off). **B5.4a (2026-06-19):** PASS tecnico — box scala JPG bianco pieno/opaco (`#ffffff`), testo/bordi ad alto contrasto; nessun cambio calcoli/export base. **B5.4b (2026-06-19):** PASS tecnico — fix layout canvas `drawJpgExportScale` (`textBaseline: top`, boxH/gap/font/barH aumentati; degradazione Nm se canvas troppo basso); box bianco opaco preservato; calcoli/export base invariati. **B5.4c (2026-06-19):** PASS tecnico — ratio 1:N sempre renderizzata e leggibile nel JPG export (riga dedicata, `gapBeforeRatio`, font 700 ≥11px, Nm omessa prima della ratio se spazio insufficiente); box bianco opaco preservato. **B5.4d (2026-06-19):** PASS tecnico — layout export a due colonne: ratio 1:N a sinistra (centrata verticalmente), tabella km·mi/barre/Nm a destra; box bianco opaco preservato; **QA operatore PASS (2026-06-20) — output JPG scaricato:** app VPS/tailnet `:8000`, runtime **`97406ab`** (build **B6.6B**), deploy **`63084dd`**, QA `:8000/coordinate_converter%20Claude.html?v=97406ab&force=b66b`; nel file JPG scaricato scala/legenda nel posto corretto, layout export valido; **PASS limitato al JPG verificato** (non copre scala in-app né export avanzato). **B6.1 fix Range Rings creazione manuale (2026-06-19):** PASS tecnico — `#rrCreateBtn` non resta nascosto quando centro risolvibile; primary unico; default `NM` + distanze `1, 5, 10` su reset; nessun rewrite parser/overlay; **QA operatore N/A — SUPERATO da B6.2** (B6.2 ha rimosso «Crea anelli», flusso che B6.1 correggeva).
- **Range Rings B6.2 pick-first UX (2026-06-19):** PASS tecnico — runtime **`d38c253`**; `#rrCreateBtn`/`Crea anelli` rimosso; **`Punta e crea`** unico primary; pannello minimizzato al pick (`gisMinimizePanel`/dock); default **`NM`** + distanze **`1, 5, 10`**; preset chips unit-aware.
- **Range Rings B6.3/B6.3a stili e label (2026-06-19):** PASS tecnico — runtime B6.3 **`d69cacd`**, B6.3a **`22f19f1`**; colore/spessore/tipo linea cerchi; colore label distanza; sfondo/badge label; label a bearing offset dalle guide radiali legacy.
- **Range Rings B6.3b/B6.3c edit UX (2026-06-19):** PASS tecnico — runtime B6.3b **`50b0a86`**, B6.3c **`20d2141`**; B6.3b edit style parity (load/save tutti gli stili in modifica); B6.3c click **`Modifica`** centra/fit mappa sul set selezionato.
- **Range Rings B6.4 radial/bearing spokes (2026-06-19):** PASS tecnico — runtime **`d0a4a0a`**; deploy VPS byte-match **`2f7ee52`**; toggle linee radiali; count **`{2,3,4,8,16}`** (default **`3`** = bearing legacy **`0/90/270`**); colore/spessore/tipo linea radiale; spokes centro→raggio max (non più guide ripetute per ogni anello); `sanitizeRangeRingSet` retrocompat (**set vecchi → `spokesEnabled=true`**, count default **`3`**); **QA operatore non più pending — COPERTA da regressione B6.6B (2026-06-20):** durante QA B6.6B operatore ha attestato spokes/radiali B6.4 invariati PASS; in uso da più deploy successivi.
- **Range Rings B6.4a-2 panel full-height (2026-06-20):** PASS tecnico + **PASS operatore post-deploy VPS** — runtime **`656dd13`**; HEAD/deploy **`7dd1a41`**; deploy GIS-only; smoke **`200`**, Content-Length **`2142705`**; build label **`B6.4a-2`**; QA tailnet `:8000/coordinate_converter%20Claude.html?v=656dd13`; **`node --check`** OK. **Attestazione operatore:** «tutto perfetto» (2026-06-20).
- **Range Rings B6.5 center drag (2026-06-20):** runtime **`f943675`**; deploy VPS **`2cfd553`**; handle `g.rr-center-handle` + controller `mapRrCenterDocDrag`; live redraw; click-to-place invariato. **Browser QA operatore: FAIL** — handle centro non visibile/afferrabile (overlay rings z-index 2 sotto pin `tile-marker` z-index 8; dot r=6 col colore set poco contrastato). → **B6.5B-1**.
- **Range Rings B6.5B-1 center handle visibility (2026-06-20):** PASS tecnico + **PASS operatore post-deploy VPS** — runtime **`3963c76`**; HEAD/deploy **`e694c0f`**; deploy GIS-only (Planet-Clone/proxy **non toccato**); smoke **`200`**, Content-Length **`2151292`**; build label **`B6.5B-1`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=3963c76`; fix z-index confinato `.range-rings-overlay.rr-move-center-active{z-index:12}` + handle **target/crosshair** alto contrasto; handle visibile/afferrabile in Modifica → «Sposta centro sulla mappa»; drag live cerchi/spokes/label; click-to-place e pan fuori handle OK; stili B6.3 e spokes B6.4 non regressi; pannello full-height B6.4a-2 OK; tile tailnet OK. **Nota UX accettata:** drag attivo solo dopo «Sposta centro sulla mappa» (non always-on sul pin centrale). **`node --check`** OK.
- **Range Rings B6.6B edit-mode center handle affordance (2026-06-20):** PASS tecnico + **PASS operatore post-deploy VPS** — runtime **`97406ab`**; HEAD/deploy **`63084dd`**; deploy GIS-only (Planet-Clone/proxy **non toccato**); smoke **`200`**, Content-Length **`2152189`**; build label **`B6.6B`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=97406ab` (cache browser risolta con `&force=b66b`); handle subito visibile in Modifica (`_rrEditingSetId`); centro invariato all'ingresso; drag live cerchi/spokes/label; tap mappa in Modifica semplice inerte; «Sposta centro sulla mappa» + click-to-place OK; pan fuori handle OK; B6.3/B6.4/B6.4a-2/B6.5B-1 non regressi. **`node --check`** OK.
- **B QA OPSEC/proxy/offline — PASS operatore post-catena Range Rings B6.1→B6.6B (2026-06-20):** app deployata VPS/tailnet **`:8000`**; QA `http://100.114.7.53:8000/coordinate_converter%20Claude.html?v=97406ab&force=b66b`; runtime **`97406ab`** (build **`B6.6B`**); HEAD deploy **`63084dd`**; memoria pre-aggiornamento **`b9d58b9`**. **Pre-flight (attestato operatore):** `goi-nav-proxy` + `goi-gis-app` active; proxy `/status` con **`gsat`/`bsat`** disponibili; app `:8000` HTTP **`200`** / build **`B6.6B`**. **OPSEC strict + consenso provider:** strict verificato; Annulla fail-closed; tile via proxy dopo Conferma; consenso per-provider separato; Navionics/SonarChart indipendente se incluso nel test. **Forced-offline:** verificato — area non cache → placeholder/nessuna rete; cache IndexedDB dove disponibile; online ripristinato a disattivazione. **Proxy routing:** **`gsat`** via `/gsat/...` e **`bsat`** via `/bsat/...` su proxy **`:5000`**; tile **`200`** / proxy operativo. **Coesistenza Range Rings:** anelli/spokes/handle B6.6B coesistono con layer esterni; nessuna interferenza osservata con consenso provider né forced-offline/cache. **Limiti:** sotto-check senza evidenza autonoma separata — esito basato su attestazione operatore sintetica «tutto ok»; **non** attestato da AI.
- **B5.4eB scala in-app allineata a export JPG (2026-06-20):** PASS tecnico + **PASS operatore post-deploy VPS** — runtime **`0edf503`**; HEAD/deploy **`f904279`**; deploy GIS-only (Planet-Clone/proxy **non toccati**); smoke **`200`**, Content-Length **`2151652`**; build label **`B5.4eB`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=0edf503` (`&force=b54eb` cache); scala in-app box opaco, layout due colonne (ratio **`1:N`** sx, tabella km/mi + barre + Nm dx); narrow: Nm omessa/degradata senza overlap; export JPG B5.4d invariato/PASS; mappa interattiva; Range Rings B6.6B non regressi; attestazione operatore «tutto a posto»; **`node --check`** OK.
- **B5.4f — etichette graduate scala valore per-tacca (backlog):** mostrare il valore di una tacca (es. «1 km» / «0.5 Nm»); valore per-tacca tondo per costruzione (`pickM` / `pickNm` = 1/2/5 × 10ⁿ); opzionale anche valore a metà (`metricMidLabel` / `nmMidLabel`, già nel modello); solo unità primaria km/Nm — **non** mi per valore per-tacca (miglio non divide tondo); parità visiva su entrambi i renderer: `buildScaleBar` in-app e `drawJpgExportScale` export; export B5.4d è PASS e verrebbe ritoccato → B5.4f richiede nuova ri-QA export; **PLAN-FIRST**, non patch isolata in-app; runtime non modificato in blocco docs corrente.
- **B5.5C export JPG — selezione granulare overlay + label waypoint (2026-06-21):** PASS tecnico remoto + **PASS operatore post-deploy VPS** — runtime **`5a10a48`**; HEAD/deploy **`4da28f5`**; deploy GIS-only (`goi-gis-app` **active**; Planet-Clone/proxy **non toccati**); smoke **`200`**, Content-Length/wc **`2161529`** — byte-match **PASS**; SHA-256 file/body **`0489f73e6e9e6998af912884230f2d5382824bf95478b1724d0227151ebb7208**; build **`B5.5C`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=5a10a48`; dialog `#jpgExportDialog`: master overlay + 5 categorie granulari transienti (tracce, waypoint, etichette waypoint, poligoni/aree, Range Rings) default tutte ON; master overlay ON/OFF; tracce ON/OFF; waypoint ON/OFF; label waypoint indipendenti dai pin; poligoni/aree ON/OFF; Range Rings ON/OFF; scala indipendente; qualità fissa **3×** invariata; fedeltà stili B5.5B-1 OK; export multiplo OK; app stabile post-export; tile raster interpolate = **atteso, non bug**; B5.5Z fuori scope; proxy/cache/IndexedDB fuori scope; attestazione operatore «tutto pass» (2026-06-21); **`node --check`** OK. **B5.5C chiuso end-to-end.**
- **B5.5D export JPG — riquadro coordinate punto/waypoint (2026-06-21):** PASS tecnico remoto + **PASS operatore post-deploy VPS** — runtime **`5551622`**; HEAD/deploy **`e99f60f`**; deploy GIS-only (`goi-gis-app` **active**; Planet-Clone/proxy **non toccati**); smoke **`200`**, Content-Length/wc **`2179062`** — byte-match **PASS**; SHA-256 file/body **`67f927f8fab1ba60e518e169b25aafbaa01cb90837969e5591e31e4a01e6035f**; build **`B5.5D`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=5551622`; dialog `#jpgExportDialog`: «Includi coordinate» fail-closed (**OFF** ad ogni apertura); sorgenti centro mappa / ultimo punto / waypoint (`wp.id`); formati primary, DD, DDM, DMS, UTM, MGRS (precisione **5**), DD+MGRS; snapshot pre-`await`; `drawJpgExportCoords` top-left; indipendente da overlay B5.5C e da scala; qualità fissa **3×** / cap **8192** invariati; nessun GPS silenzioso; nessuna rete/cache/proxy aggiunta; tile raster interpolate = **atteso, non bug**; attestazione operatore «QA B5.5D PASS operatore» (2026-06-21); **`node --check`** OK. **B5.5D chiuso end-to-end.**
- **B5.5Z-FIX0 offline-export JPG — `layer` undefined (2026-06-21):** PASS tecnico remoto + **PASS operatore post-deploy VPS** — bug **preesistente** (non feature B5.5Z) in `exportOfflineAreaAsJpg()`: `layer` letto senza dichiarazione locale in strict mode; fix runtime **`3751e19`**: `const layer = getOfflineTileLayer(layerId)`; accessor canonico; **nessuna** modifica a cache, rete, OPSEC, progress, placeholder o download; **`node --check`** OK; deploy GIS-only HEAD **`ff904dd`**; `goi-gis-app` **active** su `100.114.7.53:8000`; smoke **`200`**, byte-match **`2179108`**; SHA-256 file/body **`9c5e766709774a725440f35406936a577ce988abcb5090b26795fd627b273cc4**; build servita **`B5.5D`**; Planet-Clone/proxy **non toccati**; QA tailnet `:8000/coordinate_converter%20Claude.html?v=3751e19`; export JPG Mappe Offline completato, JPG apribile, nessun errore `layer is not defined`; attestazione operatore PASS (2026-06-21). **B5.5Z-FIX0 chiuso end-to-end.** Prerequisito tecnico catena **B5.5Z**.
- **B5.5Z-1 quick export — snapshot viewport e zoom dinamico (2026-06-21):** PASS tecnico statico — infrastruttura read-only (+116 righe); `getQuickExportViewportSnapshot()` fotografa bbox/zoom/`layerId`/dimensioni viewport visibile (math `renderTileMap`, area `.tile-map` come `exportMapAsJpg`); antimeridiano fail-closed nello spazio pixel mondiale (`originX`/`endX` vs `worldPx`) + difesa `w > e`; `computeQuickExportZoomLevels()` puro: zoom interi crescenti dal corrente al primo non valido via `computeOfflineJpgGrid` + `validateOfflineJpgExportSize` (limiti esistenti 576 tile / 8192 px / 32M px); **nessun call site**; **nessun** cambiamento UI/dialog/listener/exporter/HTML/i18n/build/cache/rete/OPSEC; **`node --check`** OK. Assorbito in **B5.5Z CLOSED**.
- **B5.5Z-2A offline-export — core mosaico geografico (2026-06-21):** PASS tecnico statico — `renderGeographicJpgMosaic(options)` estrae canvas+loop tile da `exportOfflineAreaAsJpg()`; Mappe Offline delega con comportamento intenzionalmente invariato; **`node --check`** OK. Regressione Mappe Offline **PASS** in QA B5.5Z-DELTA-A1. Assorbito in **B5.5Z CLOSED**.
- **B5.5Z-3 export JPG rapido pulsante superiore (2026-06-22):** PASS tecnico statico + review diff PASS — `#jpgExportDialog` esteso (layer read-only, zoom export, hint area vista); snapshot viewport all'apertura (`getQuickExportViewportSnapshot` → `_jpgQuickExportSnapshot`); layer automatico = `state.mapLayer` visualizzato; zoom selezionabili dal corrente al massimo esportabile singolo (`computeQuickExportZoomLevels`); overlay granulari, scala e coordinate disponibili; `exportQuickViewportAsJpg()` + crop bbox visibile + `renderGeographicJpgMosaic`; consenso OPSEC/proxy condiviso con Mappe Offline; dialog non chiudibile durante `_jpgQuickExportBusy`; `exportMapAsJpg()` legacy/fallback; Mappe Offline invariato; **`node --check`** OK; percorso entro-cap **invariato** e **PASS** in QA operatore post-DELTA-A1. Assorbito in **B5.5Z CLOSED**.
- **B5.5Z-DELTA-A1 export alto-zoom segmentato tile-only (2026-06-22):** PASS tecnico statico + review byte-level PASS + fix rilascio memoria deterministico PASS + **PASS operatore post-deploy VPS** — runtime **`1099655`**; HEAD/deploy **`e15e772`**; deploy GIS-only (`af336a2..e15e772` fast-forward); `goi-gis-app` **active** su `100.114.7.53:8000`; `goi-nav-proxy` e Planet-Clone **non toccati**; smoke **`200`**, byte-match **`2228069`**; SHA-256 file/body **`263ef4603a6ea1053f696631787901dc5b48145b0363b1d464c10e0832bab386**; QA tailnet `:8000/coordinate_converter%20Claude.html?v=1099655`; select zoom **tutti** i livelli interi dal corrente a `layer.maxZoom`; entro-cap singolo JPG B5.5Z-3 invariato (overlay/scala/coordinate); oltre-cap export segmentato tile-only sequenziale (`computeQuickExportSegmentPlan`, naming `_z<zoom>_r<row>c<col>.jpg`, download «Scarica segmento e continua»); stima preventiva (dimensioni virtuali, tile, immagini, richieste rete max); soglia egress alta (`>5760` tile o `>10` immagini) + seconda conferma; hard-stop (`>23040` tile o `>64` immagini); overlay/scala/coordinate disabilitati nel percorso segmentato con spiegazione; OPSEC/proxy/force-offline una volta per job; cleanup canvas/blob deterministico in success/cancel/fail; Mappe Offline JPG regressione **PASS**; attestazione operatore «QA B5.5Z-DELTA-A1 PASS operatore» (2026-06-22); **`node --check`** OK. **B5.5Z-DELTA-A1 CLOSED / PASS end-to-end.**
- **B5.5Z export JPG rapido zoom reale — catena FIX0→1→2A→3→DELTA-A1 (2026-06-22):** **CLOSED / PASS end-to-end** — runtime distribuito VPS **`1099655`** @ HEAD deploy **`e15e772`**; quick export geografico viewport con zoom fino a `layer.maxZoom`; entro-cap completo B5.5Z-3; oltre-cap segmentato tile-only DELTA-A1; Mappe Offline invariato. **Backlog opzionale non bloccante:** overlay geografici sui segmenti oltre-cap.
- **B5.5Z-BUILD label runtime visibile (2026-06-22):** PASS tecnico statico + deploy VPS GIS-only + smoke etichetta + **PASS operatore verifica minima** — runtime **`3fa6212`**; HEAD/deploy **`053ac18`**; **`APP_BUILD_ID`** **`B5.5D` → `B5.5Z`**; **`APP_BUILD_DETAIL`** *Quick geographic JPG export and segmented high-zoom tiles*; placeholder footer/about allineati; modifica **esclusivamente identificativa** (5 righe monolite, nessuna logica export/tile/cache/OPSEC/provider/GPS/i18n); commento storico `(B5.5D)` su `drawJpgExportCoords` invariato; deploy GIS-only (`e15e772..053ac18` fast-forward); `goi-gis-app` **active** su `100.114.7.53:8000`; `goi-nav-proxy` e Planet-Clone **non toccati**; smoke **`200`**, byte-match **`2228096`**; SHA-256 file/body **`a99a24f16fe125dc672b06afe1bbb68bcbffba2009a33f6bafa457fb12ea60ab**; QA tailnet `:8000/coordinate_converter%20Claude.html?v=3fa6212`; footer e About **`B5.5Z`**; detail corretto; app avviata normalmente; attestazione operatore «QA B5.5Z-BUILD PASS operatore» (2026-06-22); **`node --check`** OK; **`coordinate_converter Claude.html`** = ultima versione funzionante verificata su VPS. **B5.5Z-BUILD CLOSED / PASS end-to-end** — build visibile allineata al runtime B5.5Z.
- **B5.5E-2 export JPG — qualità fissa 3× senza selettore (2026-06-21):** PASS tecnico remoto + **PASS operatore post-deploy VPS** — runtime **`25555c2`**; HEAD/deploy **`2d505af`**; deploy GIS-only (Planet-Clone/proxy **non toccati**); smoke **`200`**, Content-Length/wc **`2155320`** — byte-match **PASS**; build **`B5.5E-2`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=25555c2`; selettore 1×/2×/3× **assente**; checkbox overlay/scala OK; export JPG OK; griglia/overlay/marker/label/scala nitidi; no fill nero/alterazione label; overlay ON/OFF OK; scala ON/OFF OK; export multi-elemento OK; fedeltà stili B5.5B-1 OK; B6.6C non regressi; app stabile post-export; tile raster interpolate = **atteso, non bug**; attestazione operatore «QA B5.5E-2 PASS operatore». **`node --check`** OK. **Catena B5.5E chiusa** (configurazione finale).
- **B5.5E-1 export JPG — default qualità massima 3× (2026-06-21):** PASS tecnico statico + **QA operatore parziale** (radio 3× preselezionato) — build **`B5.5E-1`**; `_jpgExportScale` default **3** → **superato da B5.5E-2** (selettore rimosso); deploy VPS byte-match **2158230** @ `0cc28d5`.
- **B5.5E export JPG — supersampling 1×/2×/3× (2026-06-21):** PASS tecnico statico — build **`B5.5E`**; dialog `#jpgExportDialog`: radio risoluzione 1×/2×/3× (`_jpgExportScale`, default **1** → **B5.5E-1** default **3**); `exportMapAsJpg({ includeScale, includeOverlays, scale })`; canvas `W*S/H*S` + `ctx.scale(S,S)`; cap memoria fail-safe **8192** px; `rasterizeSvgOntoCanvas(..., rasterScale=S)` su griglia/overlay/marker (overlay mantiene `inlineStyles=true`); viewBox fallback griglia MGRS; tile raster solo interpolate; `drawJpgExportScale` invariato; no fetch/zoom reale (B5.5Z). **`node --check`** OK. **QA operatore post-deploy VPS: pending** (supersampling base; default 3× in B5.5E-1).
- **B5.5B-1 export JPG — overlay style fidelity (2026-06-21):** PASS tecnico + **PASS operatore post-deploy VPS** — runtime **`6524183`**; HEAD/deploy **`30849de`**; deploy GIS-only (Planet-Clone/proxy **non toccati**); smoke **`200`**, Content-Length/wc **`2154397`** — byte-match **PASS**; build **`B5.5B-1`** servita; QA `:8000/coordinate_converter%20Claude.html?v=6524183`; fix inline computed styles overlay-only (`rasterizeSvgOntoCanvas` + `inlineStyles=true`); label/overlay JPG coerenti con app live (stile/alone OK); tracce/aree **non** nere piene; overlay ON/OFF OK; cursore escluso; scala opzionale OK; B6.6C panel restore OK; attestazione «tutto ok». **`node --check`** OK.
- **B5.5B export JPG avanzato — overlay mappa base (2026-06-20):** PASS tecnico statico + **deploy VPS **`4b75e22`** smoke **`200`**, Content-Length/wc **`2153290`** byte-match **PASS**; runtime **`e6b28db`**; build **`B5.5B`**; dialog `#jpgExportIncludeOverlays` default true; 4 overlay SVG WYSIWYG via `rasterizeSvgOntoCanvas`; cursore escluso; scala B5.4d/B5.4eB invariata. **QA operatore FAIL parziale:** labeling leggermente diverso; traccia/area poligono nero pieno in JPG → corretto in **B5.5B-1**. **`node --check`** OK.
- **B5.5A-1 export JPG avanzato — PASS piano/diagnosi (2026-06-20):** diagnosi monolite read-only — `exportMapAsJpg(opts)` (~L19719) = DOM-to-canvas **1×** su `.tile-map`; `rasterizeSvgOntoCanvas` (~L19691) rasterizza SVG inline, scalabile via `dw/dh`; dialog `#jpgExportDialog` (~L9890) ha **solo** `#jpgExportIncludeScale`, confirm passa `{ includeScale }`; scala via `computeMapScaleModel` + `drawJpgExportScale` (B5.4d/B5.4eB, **da preservare**). **Oggi il JPG include:** sfondo, `img.tile` caricate, `.tile-grid` MGRS, `.tile-marker svg`, scala opzionale. **NON include** overlay SVG separati appesi a `.tile-layer`: `.saved-tracks-overlay`, `.waypoints-overlay`, `.range-rings-overlay`, `.gis-polygons-overlay` (+ draft track/polygon/GPS/coverage/bbox); `.tile-readout` (PUNTO+CURSORE) fuori export — **cursore escluso per costruzione**. Stati canonici: `state.mapWaypoints[]`/`savedTracks[]`/`track`/`gisPolygons[]`/`rangeRingSets[]`/`lastPoint|viewCenter`/`primary`/`mapZoom`/`mapSize`. **Decisione tecnica:** WYSIWYG SVG capture degli overlay live via `rasterizeSvgOntoCanvas`; **no** re-render geometria-per-geometria in fase 1; **no** fetch rete / **no** tile cache/offline/proxy per B5.5B-E; zoom reale isolato in **B5.5Z** (può richiedere tile diverse → OPSEC strict/forced-offline/cache/proxy opt-in). **UX:** dialog gruppo «Includi nell'immagine» (tracce, waypoint, label wpt, poligoni, Range Rings, griglia MGRS, scala, tab coordinate punto/wpt) + sezione qualità 1×/2×/3×; zoom in sezione avanzata solo post-B5.5Z; cursore mai opzione. **Scomposizione:** B5.5B scaffolding dialog + config object + cattura overlay SVG base (no zoom); B5.5C selezione granulare per-overlay + label wpt; B5.5D tab coordinate punto/wpt su canvas (cursore escluso); B5.5E risoluzione 1×/2×/3× (supersampling overlay/testo, tile upscalati, no fetch); B5.5Z studio zoom reale separato, solo se sicuro. **Rischi:** regressione B5.4d/B5.4eB; mismatch in-app vs export; overlay DOM-dipendenti/nascosti; performance/memoria canvas 3×; rete imprevista solo se zoom reale. **Raccomandazione:** primo blocco runtime **B5.5B** (risolve il gap principale a rischio minimo, riuso SVG live, no rete, scala intatta). Test runtime futuri: `node --check`, assenza `<script src>`/`type="module"`, deploy VPS GIS-only + byte-match, QA operatore `?v=<hash runtime>` confronto in-app vs JPG per overlay + cursore escluso + regressioni scala/export e B6.6C. **Non** parte del PASS B5.4d.
- **Range Rings B6.6C panel restore after create (2026-06-20):** PASS tecnico + **PASS operatore post-deploy VPS** — runtime **`41f180b`**; HEAD/deploy **`69fa6cf`**; deploy GIS-only (Planet-Clone/proxy **non toccati**); smoke **`200`**, Content-Length **`2151776`**; wc -c file VPS **`2151776`** — byte-match **PASS**; build label **`B6.6C`** servita; QA tailnet `:8000/coordinate_converter%20Claude.html?v=41f180b`; «Punta e crea» + click mappa → pannello ripristinato/ingrandito auto; distanze vuote/non valide → pannello riaperto + messaggio validazione visibile; B6.6B handle/edit mode non regressi; export JPG/scala non regressi; attestazione operatore «tutto ok»; **`node --check`** OK.
- **WU-0007 B6.7a Range Rings titolo opzionale + Stili comprimibile (2026-06-22):** **CLOSED / PASS end-to-end** — runtime **`b2d828f`**; deploy HEAD **`d3122e4`**; deploy tecnico GIS-only **PASS** (`goi-gis-app` only); smoke **`200`**, file/body **`2237896`** byte, SHA-256 **`5690ca15f6f320ec0276af2c7904a880fd75380d31a9b702c6cf61c8699a122a`** byte-match **PASS**; build **`B5.5Z`** invariata; QA tailnet `:8000/coordinate_converter%20Claude.html?v=b2d828f`; attestazione **«QA WU-0007 B6.7a PASS operatore»** — `showTitle` per-ring (fallback legacy `labelMode`); `#rrStyleDetails` (`<details>` chiuso); titolo mappa indipendente da etichette distanza; proxy/Planet-Clone/n8n/Docker **non toccati**; **nessun fix aperto B6.7a**.
- **WU-0007 B6.7b Range Rings memoria ultimo stile persistente (2026-06-22):** **CLOSED / PASS end-to-end** — runtime **`0ba6cdc`**; deploy HEAD **`230eb6e`**; deploy tecnico GIS-only **PASS** (`goi-gis-app` only); smoke **`200`**, file/body **`2243940`** byte, SHA-256 **`9130ef55392309ecc073cd18d3104490aee7575e39e31353e728844f4be1dbb2`** byte-match **PASS**; blob **`def83a9`**; build **`B5.5Z`** invariata; QA tailnet `:8000/coordinate_converter%20Claude.html?v=0ba6cdc`; attestazione **«QA WU-0007 B6.7b PASS operatore»** — preferenza `settings.rangeRingsLastStyle` in `coordconv_v2` / `state.rangeRingsLastStyle`; `sanitizeRangeRingsLastStyle`; nuovo ring da ultimo stile confermato (`rrGetPreferredNewRingFormStyle` / `rrResetRangeRingsFormToNew`); aggiornamento solo su create/save riusciti; cancel/reset bozza/import non alterano preferenza; nome/centro/distanze/unità/geometria/metadata esclusi; ring esistenti invariati; `opacity` + `showTitle` inclusi; proxy/Planet-Clone/n8n/Docker **non toccati**; **nessun fix aperto B6.7b**. **WU-0007 B6.7a–B6.7b — CLOSED / PASS end-to-end**.
- **WU-0009 — provider basemap esterni (Navionics, Google, Bing):** la valutazione di ammissibilità all'uso è stata effettuata e chiusa dall'operatore. **Decisione: PROCEDERE.** Non riaprire la questione nei blocchi successivi. Le considerazioni su licenze/condizioni d'uso dei provider sono fuori scope per il lavoro tecnico: non vanno sollevate come gate, STOP o rischio nei piani futuri. Il lavoro su Bing (B4) e su eventuali provider successivi procede sul piano tecnico (proxy, quadkey, versione, consenso OPSEC, deploy). Questa chiusura riguarda esclusivamente l'ammissibilità d'uso dei provider; **non** sospende la segnalazione di rischi tecnici (stabilità versione/discovery, egress, consenso OPSEC, cache, drift cross-repo), che restano parte normale dei piani.
- **Infra VPS runtime (docs):** `docs/INFRA_VPS.md` (`e390fc7`) — inventario host/deploy; complementare a `docs/runtime/VPS_DEPLOY_RUNTIME.md`. Planet-Clone stub rimandato a repo separato.

**Escluso da questa OM:** porte raw tailnet, open proxy, B2/Tailscale Serve, reboot-test systemd, ACL/firewall, n8n, control-plane, Planet-Clone operativo.

---

## 7b. Workspace operativo unico

- Lavorare **solo** in `GitHub\cursor-coordinate-converter`, allineato a `origin/main`.
- **NON** usare `Tools\CesiumTest` per il GIS Tool.
- `Tools\CesiumTest` è il clone di Planet-Clone / proxy Navionics: progetto diverso.
- Se un task coinvolge Planet-Clone o proxy Navionics, dichiararlo esplicitamente come lavoro **separato** dal GIS monolite.
- **Runtime/deploy VPS GOI** (post WU-0009 `gsat`): supporto operativo in [`docs/runtime/VPS_DEPLOY_RUNTIME.md`](runtime/VPS_DEPLOY_RUNTIME.md) — Planet-Clone runtime separato dal GIS; proxy `goi-nav-proxy.service` su tailnet `100.114.7.53:5000`; dettagli deploy/smoke/cache/boot in quel documento. Inventario host esteso: [`docs/INFRA_VPS.md`](INFRA_VPS.md). §7 resta stato vivo; il doc runtime non lo sostituisce.

---

## 8. Work unit

| WU | Stato | Scopo |
| --- | --- | --- |
| WU-0001 | PASS | OPSEC strict cycle |
| WU-0002 | PASS | Memory standardization (CLOSED) |
| WU-0003 | CLOSED | SonarChart overlay |
| WU-0004 | CLOSED | Basemap / SonarChart indipendenti; B2 rimosso per decisione |

---

## Pattern nomi inbox orchestratore

- **Pattern ufficiale:** `AAAA-MM-GG_HHMM_<tipo>_<slug>.md`
- **Tipi comuni:** `plan`, `riepilogo`, `handoff`, `qa`
- **Regole pratiche:**
  - non usare doppio underscore;
  - includere sempre `HHMM`;
  - includere sempre il segmento `<tipo>`;
  - usare slug descrittivo in kebab-case.
- **Esempio valido:** `2026-06-14_0102_riepilogo_memory-standardization-final-autosync.md`
