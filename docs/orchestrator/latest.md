> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-05 — **`finito` Regola H** MAP-BOX-ZOOM-A (+ FIX1). Commit task docs **`e3cf395`** (`docs: finito MAP-BOX-ZOOM-A-FIX1 after Regola H QA PASS`). Runtime tip **`8e3cee4`** / build **117** (già in `origin/main` dal fix commit; **incluso** nel ciclo `finito` come tip live, non nel commit docs). ROUTINE; QA FAIL A → FIX1; deploy GIS-only PASS; QA PASS operatore. Working tree post-task/pre-autosync: **pulito**. Monolite **non** nel commit docs. Inbox: `docs/orchestrator/inbox/2026-08-05_1313_riepilogo_finito-sessione.md`.

2026-08-05 — **docs-only backlog** `MAP-BOX-ZOOM-A` + `CARTO-INDEX-FEDERATED-A` registrati (poi MAP-BOX chiuso end-to-end). Commit task **`b737d5c`**.

2026-08-03 — **`finito` Regola H** ROUTING-ANELLO-A (+ FIX1). Commit task docs **`30063e0`**. Runtime tip storico **`f718582`** / build **115** (superseded live).
