# P-UNITS-FIX — isolamento unità Poligoni da Misura GIS

**Data:** 2026-06-26  
**Tipo:** micro-fix runtime + finito sessione + autosync orchestratore

## Commit task (runtime fix)

- **SHA:** `8be2845453f10deb6cbf319c3b9a07e55f6b6e26`
- **Subject:** `fix(gis): isolate polygon metric units from measure state (P-UNITS-FIX)`
- **File:** `coordinate_converter Claude.html` (+2/−22)
- **Blob monolite:** `d51d210b378afdbc2a781ed878f919a9ab530456`

## Commit docs

- **SHA:** `df0c50a52d1e2514756549a574e4773e07b0da1e`

## Problema

`polygonSyncLengthUnitToMeasure()` accoppiava `polygonMetricLengthUnit` a `state.mapMeasureUnit`, aggiornando selettori/overlay/readout Misura GIS.

## Fix

- Rimossa `polygonSyncLengthUnitToMeasure` e relativa chiamata in `polygonApplyUnitsChange`.
- `polygonApplyUnitsChange` aggiorna solo `renderPolygonDraftInfo` / `renderPolygonEditInfo`.
- Rimosso `aria-labelledby` autoreferenziale su `#polygonPanelLengthUnit` e `#polygonPanelAreaUnit`; restano `<label for>`.

## Verifiche

- Harness isolamento (`C:\tmp\p-units-fix-isolation-test.js`): PASS (casi A/B/C)
- `node --check`: PASS
- `git diff --check`: PASS

## Non eseguito

- Deploy / QA operatore / Review Claude

## Stato

- **P-UNITS + P-UNITS-FIX:** runtime pushato; **non CLOSED end-to-end**
- **P-VERTEX-MODAL / P-STYLE:** non iniziati
