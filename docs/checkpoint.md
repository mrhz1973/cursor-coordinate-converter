# Checkpoint progetto — GOI GIS Tool

**Data snapshot:** 2026-06-13
**File canonico app:** `coordinate_converter Claude.html` (HTML single-file)

**Ultimo cambio:** 2026-06-13 — **Blocco 4 documentale + pivot i18n «proxy tailnet»:** deploy VPS tailnet stabilizzato (systemd `goi-nav-proxy.service` + `goi-gis-app.service`); causa-radice timeout Windows → **ACL Tailscale restrittiva** (non firewall host); grant additivo applicato manualmente in admin console il 2026-06-13 `{ "src": ["autogroup:member"], "dst": ["100.114.7.53/32"], "ip": ["tcp:8000", "tcp:5000"] }`; smoke test diretto da browser tailnet PASS (app, Navionics `/tiles/`, seamarks); tunnel SSH usato solo come workaround temporaneo, poi dismesso; Planet-Clone **`5e57c7f`** deployato su VPS con SonarChart proxy `/sonar/` (layer 1, overlay) — **endpoint disponibile dal proxy, non ancora integrato nel monolite GIS** (l'app usa solo `/tiles/`); patch i18n IT/EN/FR «proxy locale» → «proxy tailnet»; reboot-test rinviato (VPS condiviso con n8n). **Prossimo blocco:** audit OPSEC (porte raw tailnet 5000/8000, SonarChart lato proxy, valutazione B2 `tailscale serve` + rebind loopback + URL relative). Commit i18n in **`fb4dcb0`**; docs operative in commit dedicato.

**Ultimo cambio (precedente):** 2026-05-27 — **Browser PASS — T1.6 minimal GeoJSON export classification marking.** Commit testato **`c59d2de`**. Test manuale: waypoint export GeoJSON; `metadata.classification = "UNCLASSIFIED"`, `metadata.kind = "waypoints"`. Nessun codice modificato in questa sessione (solo docs).

**Ultimo cambio (precedente):** 2026-05-27 — **T1.6 minimal export classification (GeoJSON):** helper `exportGeoJsonMetadata()` adds default `classification: "UNCLASSIFIED"` to all GeoJSON FeatureCollection `metadata` builders (`buildGeoJSON`, `buildGeoJSONRoute`, `spatialBuildFeatureCollectionFromAppState`, `gisAllAsFeatureCollection`, `savedTrackToFeatureCollection`, waypoint modal GeoJSON). No UI, no storage, no GPX/KML/CSV change. Trigger: `docs/requests/2026-05-27-classification-markings-export-request.md`. **`docs/roadmap.md` non modificato.** Browser QA: NOT EXECUTED (superseded by browser PASS entry above).

**Ultimo cambio (precedente):** 2026-05-02 — **Chiusura sessione (`finito`):** **Pass 6 Step 6F.3** in `coordinate_converter Claude.html` — **Elimina selezionati** waypoint: rimosso **`window.confirm`** da **`waypointsDeleteSelectedBulk`**; conferma interna **`#wpDeleteSelectedBar`** + stato transiente **`waypointPendingDeleteSelectedIds`**, **`wpShowDeleteSelectedConfirmBar`** / **`waypointsDeleteSelectedExecute`** / **`wpHideDeleteSelectedBar`** / **`wpWireDeleteSelectedBarOnce`**; **Esc** nel modal Waypoint chiude prima la barra bulk (senza cancellare dati); mutua esclusione con **`#wpDeleteOneBar`** e **`#wpRemoveFavBar`**; **`waypointModalUnsavedCloseRisk`** e **`closeWaypointModal`** aggiornati; chiusura barra su **Deseleziona** / **Seleziona tutti** e all’avvio di **`waypointModalAddSelectedRowsToFavorites`**; nessuna selezione → feedback **`waypointModal.noSelection`**; i18n **`waypointModal.deleteSelectedConfirmTitle`**, **`deleteSelectedConfirmMsg`** (`{count}`), **`deleteSelectedConfirm`**, **`deleteSelectedDone`**. Memoria orchestratore 6F.3 era già su `main` (**`6ec3f99`** + **`8082d1a`**); questo **`finito`** **versiona il monolite** + checkpoint sessione; riconciliazione orchestratore in commit dedicato post-push. **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-02 — **Chiusura sessione (`finito`):** **Pass 6 Step 6F.2 + 6F.2a** in `coordinate_converter Claude.html` — **6F.2:** selezione multipla waypoint nel modal (`waypointModalSelectedRowIds` `Set` transiente, ripristino checkbox in `renderWpModalList`, barra **`#wpBatchFavRow`** con contatore / Seleziona tutti / Deseleziona / **Aggiungi selezionati ai Preferiti**; `pushFavoriteEntrySilent(..., { deferPersist: true })` + un solo `renderFavorites`/`saveStore` a fine batch; `waypointCoordinateHasMatchingFavorite` per skip; feedback `showWpModalListFavFeedback`; i18n `waypointModal.selectionCount` ecc.; `wpClearListFavFeedbackQuiet` su barre confirm delete/rimuovi-preferito; **nessuna** rimozione multipla Preferiti; delete multipla da lista restava con `confirm` fino al **`finito`** **6F.3** (barra **`#wpDeleteSelectedBar`**). **6F.2a:** in `renderFavorites`, pulsante **`data-fav-waypoint`** — icona **📍** al posto di **★** (tooltip `tip.favAddWaypoint` invariato). Memoria orchestratore per 6F.2/6F.2a era già su `main` **senza** monolite; questo **`finito`** **versiona il monolite** + checkpoint sessione; riconciliazione orchestratore in commit dedicato post-push. **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-02 — **Chiusura sessione (`finito`):** **Pass 6 Step 6F.1 + 6F.1a** in `coordinate_converter Claude.html` — **6F.1:** Converti → waypoint da risultato (`#btnAddResultToWaypoint`, `waypointAddFromConvertedPoint`, `#convertWaypointFeedback`, `syncConvertResultWaypointBtn` in `openConvertModal`); Waypoint lista — stella su coords già in **Preferiti** → barra conferma interna **`#wpRemoveFavBar`** (un solo favorito match, più recente in `state.favorites` via `unshift`), **`removeFavoriteExecute`**, niente `alert`/`window.confirm` su questi flussi; i18n `convert.*` / `waypointModal.removeFavorite*` IT/EN/FR. **6F.1a:** riga primaria waypoint/preferiti + feedback **sopra** `#results` nel Converti GIS (`.convert-cm-primary-actions`) così i pulsanti restano in prima vista con body scrollabile. Memoria orchestratore già pubblicata in inbox **`2026-05-02_1258_…6F1…`**, **`1327_…6F1a…`** (commit memoria **non** includevano il monolite). Questo **`finito`** **versiona il monolite** + checkpoint sessione; riconciliazione orchestratore in commit dedicato post-push. **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-02 — **Chiusura sessione (`finito`):** **Pass 6 Step 6E.2** in `coordinate_converter Claude.html` — minimizza **`#favoritesPanel`**, **`#layersPanel`**, **`#astroPanel`**, **`#rangeRingsPanel`** (stesso pattern Traccia/Waypoint + notice); guard senza chiusura automatica dei sub-dialog (preferiti **`#favInlineConfirmBar`**, Mappe **`#offlineDraftWarnDialog`**, Astro picker waypoint/favoriti, RR picker/conferma delete/batch bar); Esc non chiude il floating se il pannello è minimizzato; **`trackSyncPickModeUi`** / **`renderTileMap`** — pulsante Waypoint sulla mappa resta **active** con **`#waypointModal`** aperto in GIS anche minimizzato. Precedente **`finito`** stesso giorno aveva già versionato il batch **6E.1d**. Aggiornati **`docs/checkpoint.md`** e **`docs/session-geolocalizzazione-e-mappa.md`**. Riconciliazione orchestratore: commit dedicato post-push `finito`. **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-02 — **Chiusura sessione (`finito`):** **Pass 6 (batch monolite cumulativo)** in `coordinate_converter Claude.html` — da stato **Pass 5 Step E.3** (`9a90fbc`) a **Pass 6 Step 6E.1d**: standardizzazione liste **Preferiti / Waypoint** (6A–6A.2), **Mappe offline** partial-offscreen e azioni tabella (6B), **Traccia** partial-offscreen e polish contestuale/GPS/doppio click (6C–6C.4), pilot **minimizza** Traccia/Waypoint con dock `#gisMinimizedDock` (6E.1), correttivi smoke post-pilot (6E.1a), polish dock/GPS/Esc mappa (6E.1b), **Converti** partial-offscreen + offset dock (6E.1c), **coesistenza Astro + Traccia** senza chiusura incrociata da Strumenti/tile Astro (6E.1d). **Pass 6 Step 6E.2** in quel commit era ancora **fuori** (implementato nel **`finito`** successivo). Dettaglio operativo anche in **`docs/orchestrator/inbox/`**. **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-01 — **Chiusura sessione (`finito`):** **Pass 5 Step E.3 — partial-offscreen Waypoint/Favoriti** in `coordinate_converter Claude.html`: `partialMinVisible: 72` in **`_waypointPanelLayoutOpts`** e **`_favoritesPanelLayoutOpts`**; **`clampWaypointModalRect`** e **`clampFavoritesPanelRect`** usano **`gisPanelClampRectPartialVisible`** (strip minima recuperabile, stile RR/Astro). Nella stessa sessione era già stato pubblicato su `main` il **piano Pass 6** (liste modali GIS, azioni compatte, rename UI IT Preferiti) in **`13a7a48`** (`docs/orchestrator/inbox/2026-05-01_1709_plan_pass6-modal-actions-standardization.md`). Aggiornati **`docs/checkpoint.md`** e append **`docs/session-geolocalizzazione-e-mappa.md`**. Riconciliazione orchestratore: commit dedicato post-push `finito`. **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-01 — **Chiusura sessione (`finito`):** **Pass 5 Step B→E.2 — Astro GIS** in `coordinate_converter Claude.html`: **`#astroPanel`** floating (drag/resize/clamp, sorgenti result/mapCenter, i18n Step B); **Step C** — sorgente **`mapPick`**, disarm concorrenti, Esc vs Range Rings (`stopImmediatePropagation` / `astroClearPickCenterMode`); **Step D / D.1 / D.2** — **`#astroWaypointPicker`** (ricerca, max 50, colonne opzionali, sort, tooltip); **Step E / E.1** — **`#astroFavoritePicker`**, Meta display filtrata, pickers ridimensionabili, layout sessione **`gPanelLayouts`** (`astroWpPicker` / `astroFavPicker`, fuori `UI_PANEL_KEYS`); **Step E.2** — modal **Favoriti** senza summary duplicato nel panel, **Centra** su righe favoriti/waypoint (**`gisMapCenterOnLatLon`** → `viewCenter` + **`renderTileMap`**, no `lastResult` / `lastPoint`), chiavi **`common.center`**, **`tip.common.centerMapPoint`**, **`map.noticeCenteredOnPoint`**, corpi pannello GIS leggermente trasparenti. **`docs/roadmap.md` non modificato.** Riconciliazione orchestratore: commit dedicato post-push `finito`.

**Ultimo cambio (precedente):** 2026-05-01 — **Chiusura sessione (`finito`):** **Pass 5 Step A — Astro state** in `coordinate_converter Claude.html`: **`state.astro`** transient (source, lat, lon, label, origin, pickMode, error; non persistito in `saveStore`); flag **`astroPanelOpen`** / **`astroPickCenterMode`** (stub Step B+); **`runAstroUI`** usa coordinate da `state.astro` se finite, altrimenti da **`state.lastResult`**; intestazione tabella Astro tramite i18n **`astro.col.utcLmt`** (IT/EN/FR). In chat: **convenzione prompt operativi** — bundle per richiesta operativa: implementazione, verifiche automatiche, test browser se possibile, pubblicazione memoria orchestratore, RIEPILOGO con esito (secondo prompt solo per output mancante/contraddittorio, test fallito, rischio reale pre-commit, dichiarazione impossibilità controlli). **`docs/roadmap.md` non modificato.** Riconciliazione orchestratore: commit dedicato dopo push step `finito`.

**Ultimo cambio (precedente):** 2026-05-01 — **Chiusura sessione (`finito`):** **Pass 4B Tier 1 — SunCalc** in `coordinate_converter Claude.html`: secondo `<script>` inline **vendored** (header MIT + subset `getTimes` / `getMoonTimes` / `getMoonIllumination`) **prima** del core; blocco duplicato rimosso dal mega-script; **`window.SunCalc = SunCalc`** dopo l’IIFE; **`runAstroUI`** usa fallback `typeof SunCalc !== "undefined" ? SunCalc : window.SunCalc` per le tre API (fix Astro post-split). **Regole:** `.cursor/rules/30-output-workflow.mdc` — pubblicazione memoria orchestratore **obbligatoria** dopo ogni intervento operativo **anche** se il monolite resta solo locale / non nell’autosync, con checklist inbox e eccezione solo su richiesta esplicita utente (commit **`555f6c5`** già su `main`). **Memoria orchestratore** (pre-`finito`): inbox Pass4b fix + hash (`fc52438`, `b3cb726`). **`docs/roadmap.md` non modificato.**

**Ultimo cambio (precedente):** 2026-05-01 — **Chiusura sessione (`finito`):** **Favoriti da Waypoint Modal + Convertitore** in `coordinate_converter Claude.html`: helper **`pushFavoriteEntrySilent`** (stesso payload favoriti esistente, rispetta checkbox convenzione nome; niente `prompt` sui nuovi flussi); lista modal waypoint — pulsante riga **★** (`data-role="wp-fav"`) + **`#wpListFavFeedback`**; risultati conversione — **`#btnAddResultToFavorites`** allineato a *Aggiungi a waypoint* (`syncConvertResultWaypointBtn`); feedback **`#pasteStatus`** / messaggi i18n IT/EN/FR (`fav.addFromResult`, `fav.convertedPointName`, salvataggi, `fav.noValidPoint`, tip). Memoria orchestratore già aggiornata in **`a0b48e3`** (solo docs pre-monolite); questa chiusura **versiona il monolite** + checkpoint sessione. **`docs/roadmap.md` non modificato.** Dettaglio: `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-05-01 — Favoriti waypoint + convertitore (Finito)*.

**Ultimo cambio (precedente):** 2026-05-01 — **Chiusura sessione (`finito`):** **Range Rings Blocco 1 UI/UX** in `coordinate_converter Claude.html`: pulsante mappa **toggle** open/close (`gisToolButtonToggle`), icona **◎** senza label testuale; rimossi ingressi RR da **Altri strumenti** / tool grid (resta `GIS_TOOL_SECTIONS` per fallback codice); creazione semplificata (**Punta e crea** primary, pick map / crea separati nascosti); default distanze **`1, 5, 10 km`**; lista colonna **Data**, azioni riga icone (✎ / ⌖ / ✕), niente export GeoJSON per riga; notifica **`rangeRings.notice.renamePending`** durante conferma rename; **clamp drag/resize parziale fuori viewport solo per RR** (`gisPanelClampRectPartialVisible`, `partialMinVisible`), **`gisPanelClampRect` globale invariato**. Memoria orchestratore: inbox implementazione + **riconciliazione `finito`** (commit dedicato). **`docs/roadmap.md` non modificato.** Dettaglio: `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-05-01 — Range Rings UI Blocco 1 + orchestratore (Finito)*.

**Ultimo cambio (precedente):** 2026-04-30 — **Chiusura sessione (`finito`):** **Range Rings + mappa tile** in `coordinate_converter Claude.html`: fix ricorsione **`rrCancelPendingRename`** (re-render lista solo con rename pendente); **hydrate** con **`_mapTileGen`**, stale check post-`await`, **`AbortController`** su fetch tile, **`syncOfflineDeltaViewportHints`** parallelo con `tileGen`; **rimossa strumentazione debug** (ingest localhost, `RR_DEBUG_PERF`, regioni bf0d51). Memoria orchestratore già aggiornata in commit separato **`ccb26bd`**. Dettaglio: `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-30 — Range Rings loop + mappa tile hydrate + pulizia debug (Finito)*.

**Ultimo cambio (precedente):** 2026-04-29 — **Chiusura sessione (`finito`):** **Misura GIS (M1 + M3 leggero)** in `coordinate_converter Claude.html`: stato transient vs preferenze persistite (`saveStore.settings`: unità/tipo geometria/poligono chiuso/`gisMeasureFlow` con **`sanitizeGisMeasureFlow`** e lettura al load); geometrie misura **non** nel payload; **`#measOperativeNotices`** + helper (`measClearMsgs`, `measShowInfo`/`measShowError`, `measSyncOperativeInfo`), messaggi Esc vertici azzerati, errori input **direct**, **`gisRefreshI18n`** se tab Misura attivo; **`gisExitMeasureTabPartial`** non resetta più **`gisMeasureFlow`**; rimossi **`saveStore`** ridondanti (direct/contextmenu handle); fix sequenza **`measSyncOperativeInfo`** dopo Esc. **Orchestratore:** alias **`aggio`** ≡ **«aggiornati»** (ChatGPT lettura remota / Cursor pubblicazione memoria) in `docs/orchestrator/chatgpt-checkpoint.md`, `docs/orchestrator/README.md`, `.cursor/rules/30-output-workflow.mdc` + inbox dedicato; **`coordinate_converter Claude.html` non toccato** in quel commit documentale. **`docs/roadmap.md` non modificato.** Dettaglio: `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-29 — Misura M1+M3 + alias aggio orchestratore (finito)*.

**Ultimo cambio (precedente):** 2026-04-29 — **Chiusura sessione (`Finito`):** monolite **modal Traccia (GIS)**: sostituito `window.prompt` salvataggio con **`#trackSaveNameDialog`**, CTA **`.track-save-cta`**, chiusura con bozza non salvata tramite **`#trackUnsavedCloseDialog`** (Salva/Scarta/Annulla; niente `window.confirm` sui percorsi `closeTrackModal` e `trackAbandonOrFinishDraftUi`); **“Elimina traccia corrente”** con **`#trackClearCurrentDialog`** (Elimina/Annulla) al posto del confirm nativo; rifinita **gerarchia toolbar** (primary **Nuova traccia**, add convertito/undo/import·export/undo per peso visivo) senza toccare Waypoint, CoT, mappe offline, reset globale, geocoding, OPSEC, GPS live, `watchPosition`, Favoriti, Converti, Search. Documentazione allineata. Dettaglio: `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-29 — Track modal: dialog interni e toolbar (Finito)*.

**Ultimo cambio (precedente):** 2026-04-29 — **Waypoint modal + CoT XML (file-only) — rifinitura (pre-Track):** hint **CoT export** visibile e dedup import; favoriti/convertito/waypoint; nessun tocco mappe offline / reset / CoT live. Vedi *Checkpoint 2026-04-29 — Waypoint modal + CoT XML rifinitura (Finito)* in session doc.

**Ultimo cambio (precedente):** 2026-04-28 — **Documentazione — reset totale locale registrato:** solo `docs/`: aggiornati `docs/checkpoint.md`, `docs/session-geolocalizzazione-e-mappa.md` (append *Checkpoint 2026-04-28 — Reset totale dati locali implementato*), `docs/PROJECT_notes.md` (stato feature + backlog). Si attesta che **Impostazioni → Cancella tutti i dati locali** è **implementata** nel monolite (zona danger, dialog `#appFullResetDialog`, conferma digitando **CANCELLA**, `#btnClearStore` Guida allineato, `performAppFullLocalReset()`, wipe `coordconv_v2` / `coordconv_ui_v1` / legacy `coordconv_v1`, IndexedDB `CoordConvMapTiles` store `tiles`, gestione errori senza reload silenzioso, nessun `saveStore()` dopo wipe, `location.reload()` a reset riuscito; *Svuota tutta la cache tile* / `performOfflineGlobalReset()` non toccati da questa feature). **Test browser utente:** esito positivo («funziona tutto»). **Prossimo blocco consigliato:** **CoT XML** import/export **file-only**. Dettaglio: `docs/session-geolocalizzazione-e-mappa.md` → sezione omonima in fondo.

**Ultimo cambio (precedente):** 2026-04-28 — **Chiusura sessione (`Finito`):** commit unico su working tree: documentazione **backlog strategico** + convenzione «mettilo nel backlog» (`docs/session-geolocalizzazione-e-mappa.md`, `docs/checkpoint.md`, `docs/PROJECT_notes.md`); monolite **Mappe Offline / GIS UI** (pannello Layers dimensioni default, scroll tabella aree, gruppo azioni riga, **X delete** `[data-offline-del]` allineata a Waypoint con `min-height` anti-compressione, override visivi senza toccare logica cache/dialog tre scelte). In chat/plan: **piano tecnico** per *Impostazioni → Cancella tutti i dati locali* (reset totale vs *Svuota cache tile*), non implementato in questo commit. Dettaglio chiusura: `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-28 — Chiusura sessione (Finito)*.

**Ultimo cambio (precedente):** 2026-04-28 — **Documentazione — backlog strategico + convenzione «mettilo nel backlog»:** solo `docs/`: append in `docs/session-geolocalizzazione-e-mappa.md` della sezione *Checkpoint 2026-04-28 — Backlog strategico: Tactical Tools, Cartografia avanzata, Core/Field/Net* (stato UI Mappe Offline validato; prossimo blocco consigliato **Impostazioni → Cancella tutti i dati locali**; macro-backlog Tactical on-map, cartografia/geo-analisi, TAK/CoT/Mission Package, report template, simbologia, sicurezza locale; proposta **Core/Field/Net**; convenzione **backlog ≠** issue GitHub / commit / modifica roadmap·rules senza approvazione). Aggiornati `docs/checkpoint.md` (indice) e `docs/PROJECT_notes.md` (sintesi). **Non** toccati `coordinate_converter Claude.html`, `docs/roadmap.md`, `.cursor/rules/*.mdc`. Dettaglio esteso: `docs/session-geolocalizzazione-e-mappa.md` → sezione omonima in fondo.

**Ultimo cambio (precedente):** 2026-04-28 — **Mappe Offline — rifinitura (toolbar / progress batch / delta / nomi / tinta cache):** rimossi dalla toolbar batch i pulsanti duplicati **Modifica** e **Centra** (restano per-riga); `Scarica selezionate` riusa `#pcBar`/`#pcProgress`/`#pcStatus` (`useProgressBar: true` nel batch); prima versione prudente di **delta copertura** solo viewport + zoom corrente con cap (classi `tile-delta-cached` / `tile-delta-missing`, stato `state._offlineDeltaAreaId` transiente); pulizia `#pcAreaName` su nuova selezione/viewport fuori modifica; blocco **nome duplicato** con `offcache.nameAlreadyUsed` in salvataggio/precache nuova area; tinta generica **tile-mission** in copertura resa meno invasiva. Solo `coordinate_converter Claude.html` + doc sessione. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-28 — Mappe Offline rifinitura*.

**Ultimo cambio (precedente):** 2026-04-28 — **Post-pack 17 UI/UX controllata:** stabilizzazione patch 17 e avanzamento locale sul monolite: rename inline più coerente (**Enter** salva via blur/change, **Esc** ripristina senza chiudere pannelli), stile uniforme input rename (`.offa-name-inp`, `.saved-name-inp`, `.wp-row-name-inp`, `.fav-name-inp`), resize colonna **Nome** esteso a Tracce salvate e Waypoint (solo sessione, no localStorage, handle desktop), clamp robusti su resize Mappe Offline, bottone **Ripristina layout pannelli** nel menu impostazioni (reset solo UI: `coordconv_ui_v1`, `gPanelLayouts`, larghezze colonne sessione; nessun dato operativo cancellato), i18n IT/EN/FR per reset layout e handle colonne, `#btnBboxPick[hidden]` reso anche non interattivo. QA: `git diff --check` OK e `node --check` JS inline OK. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-28 — Post-pack 17 UI/UX controllata*.

**Ultimo cambio (precedente):** 2026-04-28 — **MEGA-PATCH UI/UX (monolite + checkpoint sessione):** verifica `#layersPanelBody` senza padding-top anomalo (Mappe Offline compatte; `.offline-area-tip-portal` invariato); mitigazione clip controlli mappa/Layers (`.gis-map-mount` `overflow:visible`, `overflow-clip-margin` su `.tile-map` GIS); `#btnBboxPick` nascosto (`hidden`/`aria-hidden`/`tabindex=-1`) mantenendo logica bbox e **Usa viewport**; X float `.mb-close` / `.toff-close` allineate a chip neutri tipo pannelli GIS; **rename con conferma** (tracce salvate, aree offline tabella, waypoint lista, favoriti) + chiavi i18n IT/EN/FR (`track.renameConfirm`, `offcache.renameConfirm`, `waypoint.renameConfirm`, `fav.renameConfirm`); tabella Mappe Offline: input nome metadata-only, **resize colonna Nome** desktop (`--offa-name-col`, handle, niente localStorage); audit export senza refactor builder. Regola `.cursor/rules/30-output-workflow.mdc` già presente nel working tree committata insieme. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-28 — MEGA-PATCH UI/UX controllata*.

**Ultimo cambio (precedente):** 2026-04-27 — **Tracce salvate (tabella + toolbar) + workflow riepilogo:** selezione multipla e batch visibilità (Mostra/Nascondi selezionate, Mostra/Nascondi tutte); stato transient `_savedTracksSort` e helper sort (dove presenti nel monolite); funzione `applySavedTracksVisibilityOnlySelected` lasciata nel codice; **rimosso dalla UI** il pulsante batch ridondante «Isola selezionate» / `btnSavedTracksShowOnlySelected` (listener + `syncSavedTracksSelectionActions`); **anti-sfarfallio** hover su checkbox/Vis./riga selezionata (CSS mirato `#savedTracksMount`, tooltip senza anim/backdrop pesanti, soppressione pseudo-tooltip col. 1–2, `aria-label` post-render, larghezze minime, `tr.is-selected:hover` stabile). **`.cursor/rules/30-output-workflow.mdc`:** RIEPILOGO con contenuto numerato; file copiabile obbligatorio `/tmp/NN-goi-gis-riepilogo.md` (primo `NN` libero 01–99), deprecato il solo `/tmp/goi-gis-riepilogo.md`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Tracce salvate saved-tracks UI + riepilogo workflow*.

**Ultimo cambio (precedente):** 2026-04-27 — **Traccia — post-crash Blocco 1 + UX + bugfix:** workflow **modifica traccia salvata** (`trackIsEditingSavedLibrary`, i18n Salva/Aggiorna/conferme/notifiche), CTA unico in testata “Punti” (`#btnTrackSaveLibrary` sempre nascosto), **Aggiorna** senza `window.prompt` sul nome, chiusura post-save senza riaprire prompt; UX: “Elimina traccia corrente”, **+** su Nuova traccia (badge), lista punti **collassata** di default, rimossi “Chiudi traccia” e “Termina traccia” in modifica salvata; bugfix: niente **traccia orfana** alla chiusura modal se edit salvata senza delta (`_closeTrackModalCore` + reset), **overlay** salvate senza duplicare l’id in editing, **ID punti** preservati in load/save, snapshot **name+meta** in `trackCurrentPointsMatchSavedSnapshot`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Track saved-edit workflow + UX + bugfix*.

**Ultimo cambio (precedente):** 2026-04-27 — **Traccia — cleanup comandi archivio + gating “Salva” (UI):** rimosso dalla UI il comando pericoloso **“Elimina ultima salvata”** (`#btnTrackDeleteLastSaved`) lasciando solo **“Elimina selezionate”** (visibile solo con selezione); eliminata la ridondanza del salvataggio quando il prompt “Traccia completata” è visibile; introdotta `syncTrackSaveLibraryActionsState()` per **nascondere** i comandi “Salva traccia in archivio…” quando la traccia corrente ha <2 punti e per mantenere l’anti-doppione: `#btnTrackSaveLibrary` resta nascosto anche quando `state._trackPromptOpen` è true, mentre il bottone nel prompt (`data-role="track-prompt-save"`) è visibile solo con ≥2 punti. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Track archive UI cleanup + save gating*.

**Ultimo cambio (precedente):** 2026-04-27 — **Waypoint Import dialog (step 1):** toolbar Waypoint — entrypoint unico **`#wpImportBtn`** con testo **Importa / Import / Importer** + tooltip i18n; nuova **`<dialog id="waypointImportDialog">`** (`showModal`) con descrizione, area **drag & drop** locale (`#waypointImportDropZone`), pulsante **Sfoglia…** che riusa **`#wpImportFile`** (`accept` `.geojson`/`.json` + MIME); estensioni non ammesse → messaggio i18n senza chiamare import; **`waypointsImportGeoJSON(file, opts)`** accetta opzionale **`onDone(ok,msg)`** per chiudere la dialog solo su import con **almeno un punto aggiunto** (`ok`), lasciandola aperta su errori/parse/cap/annulla; Esc globale chiude prima import dialog poi export; **`closeWaypointModal`** chiude anche import dialog. Nessun DnD su mappa, nessun GPX/KML/KMZ/CSV waypoint, nessun tocco ad altre modal o builder. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Waypoint Import dialog (step 1)*.

**Ultimo cambio (precedente):** 2026-04-27 — **Modal Waypoint — pulizia lista e UX:** nascosto il residuo legacy **`#map-waypoints`** in contesto `#sec-waypoints` (CSS `display:none !important`) così non compete più con la tabella **`#wp-list`**; eliminato il **doppio messaggio vuoto** (coesistenza `.wp-list:empty::before` e `.track-points-list:empty::after` su `#wp-list` → soppresso solo `::after` in modal); in lista operativa nascosto per ora il dettaglio testuale **Simbolo / icona** (solo rendering riga, dati e editor invariati); **`#wpDeleteSelectedBtn`** con `hidden` di default e visibile solo con ≥1 checkbox selezionata (`syncWpSelectAllState` + fix dopo “Seleziona tutte”); **Elimina selezionati** spostato **dentro** `#wp-listWrap` come toolbar in fondo al blocco lista. Nessun tocco a `state.mapWaypoints[]`, export dialog, builder, altre modal. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Waypoint modal lista duplicata + empty/selezione*.

**Ultimo cambio (precedente):** 2026-04-27 — **Pattern Export dialog — step 1 (solo Waypoint):** toolbar Waypoint sostituisce i cinque pulsanti export con un solo **`#wpExportOpenBtn`** (“Esporta” IT / “Export” EN / “Exporter” FR); nuova **`<dialog id="waypointExportDialog">`** (`showModal`) con elenco GeoJSON / GPX / KML / KMZ / CSV che invoca **`waypointsExportGeoJSON()`** e **`waypointsExport(kind)`** senza modificare builder né `state.mapWaypoints[]`; stato vuoto con messaggio i18n e formati disabilitati; Esc e chiusura waypoint modal chiudono anche la export dialog; focus su formato o su close, ripristino focus su entrypoint. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Waypoint Export dialog (step 1)*.

**Ultimo cambio (precedente):** 2026-04-27 — **Fase 3B — modal Waypoint (CSS/HTML + i18n, senza logica):** uniformazione al pilota Convert (shell `#waypointModal` / pannello `#waypointModalPanel`, close neutro, toolbar a card, tipografia campi/tabella, contatore pill, bottoni per ruolo); rimozione UI comando **Elimina tutto** (`#wpClearBtn` nascosto, binding invariato); **Elimina selezionati** spostato sotto la lista (`.wp-list-actions`); tooltip tabella meno clip (overflow lista + z-index tooltip scoped); blocco **Nome sulla mappa** in basso + `data-i18n-tip` su «Sempre visibile» / «Solo nel tooltip» (`tip.waypointModal.mapNameAlwaysTip`, `tip.waypointModal.mapNameTooltipModeTip`); etichetta bulk rinominata (`waypointModal.clearAll` → Elimina tutto / Delete all / Tout supprimer). Nessun JS operativo waypoint, nessun tocco ad altre modal, Theme-1, `coordconv_ui_v1`, OPSEC, geocoding, IDB tile, track/misura/parser. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-27 — Fase 3B Waypoint + micro rifiniture*.

**Ultimo cambio (precedente):** 2026-04-26 — **Fase 3A — Convert modal (solo CSS/HTML):** completata la patch parziale post-crash: shell scoped `#convertModal` / `#convertModalBody` (token locali, close neutro, card rhythm), azioni risultati (`convert-cm-actions-row`) + footer opzioni (`convert-cm-footer-options`); regole `.convert-cm-footer-options` / `.persist-toggle` **solo** sotto `#convertModalBody .results-col` (rimossa duplicazione globale su `.results-col`); rimosso selettore orfano `#convertModalBody .gis-coord` (nessun markup/JS). Nessun cambio a parser, geocoding, OPSEC, IDB tile, waypoint/track/misura, Theme-1, `coordconv_ui_v1`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Fase 3A Convert modal (chiusura patch)*.

**Ultimo cambio (precedente):** 2026-04-26 — **Traccia — guard salvataggio (min 2 punti) + UX avviso:** `saveCurrentTrackToLibrary` richiede almeno 2 punti (`track.saveMinTwoPoints` IT/EN/FR), `return false` senza `saveStore`; `notifyTrackSaveMinPointsBlocked` / `clearTrackSaveMinPointsNotice` mostrano il messaggio in badge, `#trackSaveGuardMsg`, prompt completamento (`#trackPromptSaveErr`) e “Salva e nuova” (`#trackNewPromptSaveErr`); “Salva e nuova” e salvataggio da prompt non chiudono/resetano se fallisce; `renderTrackSummary` pulisce l’avviso con ≥2 punti. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Track save min 2 punti*.

**Ultimo cambio (accessibilità):** 2026-04-26 — **Theme-1 — select tema:** su `#themeSelect` aggiunto `data-i18n-aria="settings.themeAria"` così `applyLanguage` imposta `aria-label` dai dizionari IT/EN/FR già presenti; `aria-labelledby="themeSettingsLbl"` mantenuto. Nessun cambio a logica tema / JS / stringhe i18n. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Theme-1 select aria*.

**Ultimo grande cambio:** 2026-04-26 — **Theme-1: tema automatico locale + select tre modalità**. Nel monolite: `state.theme` persistito come preferenza `light` | `dark` | `auto`; risoluzione `auto` solo con `new Date()` e ora locale (07:00–18:59 chiaro, 19:00–06:59 scuro); DOM sempre `data-theme="light"|"dark"`; rimosso default `matchMedia(prefers-color-scheme)`; funzioni `resolveThemeFromLocalClock`, `getEffectiveTheme`, `applyThemePreference`, `syncThemeDomFromState`, `scheduleThemeAutoRefresh` / `clearThemeAutoRefresh`, listener `visibilitychange`; menu impostazioni: `#themeSelect` al posto del toggle binario; i18n IT/EN/FR (`theme.opt*`, `tip.theme`, `settings.themeAria`); import sessione JSON accetta `theme: "auto"`. Nessun tocco a `coordconv_ui_v1`, OPSEC, geocoding, IDB tile, waypoint/track. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Theme-1 tema automatico locale*.

**Cambio precedente:** 2026-04-26 — **Monolite: region markers + Persistent UI (Fase 1) + micro UX**. Nel file canonico: marker folding **CSS** (`/* #region CSS — … */`, 15 blocchi) e **JS** (`// #region JS — …`, 11 macro-regioni dopo consolidamento R3); persistenza dedicata **`coordconv_ui_v1`** (solo geometria pannelli floating GIS whitelist `track|waypoint|convert|search|favorites|layers` con `left/top/w/h/touched`), funzioni `captureUiState` / `sanitizeUiState` / `loadUiState` / `saveUiState` / `applyUiState` / `scheduleSaveUiState`, salvataggio a fine drag/resize con debounce, `applyUiState()` dopo `gisInit()`, `clearStore()` rimuove anche la chiave UI; tooltip `tip.offlineHints` più esplicito; CSS `.bbox-move` a `inset:0` per trascinamento su bbox piccoli. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Region markers, persistent UI Fase 1, micro UX*.

**Cambio precedente:** 2026-04-26 — **Cursor.code-workspace: ottimizzazioni memoria/editor per monolite**. Impostazioni workspace: `editor.largeFileOptimizations`, tetto `typescript.tsserver.maxTsServerMemory` 2048 MB, `typescript.disableAutomaticTypeAcquisition`, `typescript`/`javascript.suggest.autoImports` disattivati, blocco `[html]` con `quickSuggestions` off e `wordBasedSuggestions` off (suggerimenti su trigger), `files.watcherExclude` / `search.exclude` per repomix, backup e oggetti git; `files.exclude` solo per `repomix-output*.xml`. **Nessuna esclusione** di `docs/` né dei file `@docs` principali. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Cursor workspace memoria/editor*.

**Cambio precedente:** 2026-04-26 — **Pannello Mappa/Offline: tabella aree salvate + batch metadata-only**. Nel monolite: sezione `#offlineAreasSection` con tabella ordinabile aree offline (selezione, visibilità, nome, stato, data, zoom, strato, tile, spazio, azioni leggere), selezione multipla transient per id (`state._offlineAreasSelectedIds`, shift range e checkbox select-all), toolbar batch esterna (mostra/nascondi selezionate, elimina solo metadata, «Usa area» / «Centra» solo su selezione singola), rendering via `renderOfflineAreasList()` / `updateOfflineAreasToolbarUI()` / `offlineAreaStatusLabel()`; blocco `#pcBboxFeedback` per messaggi aria-live su area selezionata / pronta al download. Nessuna modifica a IndexedDB/download core/cache tile; eliminazione aree non cancella tile. Chiavi i18n IT/EN/FR sotto `offcache.list.*`, `offcache.sel.*`, `offcache.area.*`, `offcache.bboxFeedback*`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-26 — Offline areas O1-O4c*.

**Cambio precedente:** 2026-04-25 — **GIS floating panels e UI polish** (commit `b2546e0`). Stabilizzati i pannelli GIS sopra la mappa: drag/resize condivisi per Track, Waypoint, Convert, Search, Favorites e Layers; Search/Favorites/Layers ora hanno dialog flottanti dedicati; Track ha prompt di completamento/salvataggio, nuova traccia, pausa/riprendi inserimento e lista punti collassabile; header/settings e controlli basemap sono stati ripuliti per ridurre overlay invasivi. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-25 — GIS floating panels e UI polish*.

**Cambio precedente:** 2026-04-25 — **Waypoint Manager: export multi-formato (GPX / KML / KMZ / CSV)**. Aggiunti `waypointsExport(kind)` (`kind` ∈ gpx, kml, kmz, csv) che mappa `state.mapWaypoints` in `points` con `desc` dalle `meta.notes` e riusa i builder esistenti (`buildGPX`, `buildKML` + `buildKmz(…, "doc.kml")`, `buildCsv`); quattro pulsanti in toolbar accanto a «Esporta GeoJSON» (`wpExportGpx` … `wpExportCsv`), binding in `bindUI` dopo `#wpExportBtn`, chiavi i18n IT/EN/FR `waypointModal.export*` e `tip.waypointModal.export*`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-25 — Waypoint Manager export*.

**Cambio precedente (grande):** 2026-04-24 — **GIS Phase 1: fondazione GeoJSON invisibile** (piano `.cursor/plans/gis_phase_1_5bb51510.plan.md`). Aggiunti nel monolite, senza nuova UI: `state.gisTracks[]` (cap 50), `state.gisPolygons[]` (cap 50), `state.gisLayers[]` (cap 20 con seed Waypoints/Tracks/Polygons), `state.gisSelection` + `undoStack/redoStack` transient, helper CRUD `gisFeature*` / `gisLayer*`, sanitize/clamp load/save, scaffold `gisActionRecord()` e read-lens `gisAllAsFeatureCollection()` che produce un GeoJSON FeatureCollection unificando waypoint esistenti + nuovi store track/polygon. `state.mapWaypoints[]` resta fonte canonica dei waypoint; nessuna migrazione a store unificato in questa fase. Dettagli completi in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-24 — GIS Phase 1*.

**Cambio precedente:** 2026-04-24 — **GIS Tool cleanup pre-phase** (piano `.cursor/plans/gis_tool_cleanup_pre-phase_0ce7df27.plan.md`). Pulizia di superficie prima di iniziare il GIS progressive plan: rename app `Convertitore Coordinate` → **GOI GIS Tool**, rimozione Radius/LOS tools, Live Tracking e DTG, store bump `coordconv_v1` → `coordconv_v2`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-24 — Cleanup pre-GIS*.

**Ultimo cambio UI:** 2026-04-24 — **UI tokens semantici + glass sweep (solo CSS)** (secondo passaggio del piano `.cursor/plans/css_glass_ui_+_tokens_24558147.plan.md`). Sopra il restyle glass precedente, rifattorizzata la cima di `<style>` in due livelli: (1) token semantici (`--surface-page/raised/glass/glass-hover/field`, `--text-primary/muted`, `--border-hairline/strong/glow`, `--radius-card/control/chip`, `--shadow-soft/elevated`, `--map-control-bg/border`, `--tooltip-bg/fg`) separati per light/dark; (2) alias legacy (`--bg`, `--panel`, `--panel2`, `--text`, `--muted`, `--border`, `--shadow`, `--shadow-lg`) ora derivati dai semantici. Sweep sui componenti (card, form, btn, modal/drawer/topbar, controlli mappa, menu basemap, tooltip) per sostituire valori hard-coded con token. HTML/JS invariati. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-24 — UI tokens semantici*.

**Ultimo cambio feature (precedente al 2026-04-25):** 2026-04-24 — **WaypointModal manager split** (piano `.cursor/plans/waypointmodal_manager_split_4a8b4ca8.plan.md`). Waypoint finalmente staccati dal track builder: nuova `<details id="sec-waypoints">` + `<dialog id="waypointModal" class="app-modal">` che hostna manager completo (list + editor inline con `autoDetect` + import/export GeoJSON dedicato + cap 200). Nuovo pattern **tab-as-modal** (`GIS_TABS_AS_MODAL`), `GIS_TAB_SECTIONS.waypoints` ora punta a `sec-waypoints` (non più alias di `sec-track`). `state.mapWaypoints` resta single source of truth; modal riparenta via `gisMoveSectionTo` senza clone. Sanitize load esteso per `meta.icon/color/notes`. Dettagli in `docs/session-geolocalizzazione-e-mappa.md` → *Checkpoint 2026-04-24 — WaypointModal*.

**Convenzione:** quando chiedi in chat **«Checkpoint md»**, si aggiornano **sempre** questo file **e** `docs/session-geolocalizzazione-e-mappa.md` (append in fondo), salvo che specifichi «solo session» o «solo checkpoint.md».

## Documentazione lunga (autoritativa per feature / decisioni)

- `docs/session-geolocalizzazione-e-mappa.md` — consuntivo completo, checkpoint storici, backlog. **I checkpoint “ufficiali” di sessione vanno aggiornati lì** (sezione in fondo); questo file è solo un indice corto per `@` in chat.
- `docs/PROJECT_notes.md` — living document tecnico: overview repo, mappa sezioni del monolite, stack incorporato, stato feature, roadmap sintetica, comando Repomix.
- `docs/roadmap.md` — **rev. 2** (2026-04-23). Strategic reference: vision, §3 Distribution Strategy, §4 Architectural Principles, §8 Out of Scope. Contiene la "Notice to AI Assistants" mirrorata in `.cursor/rules/00-project-core.mdc`.
- `docs/cursor-workflow.md` — companion operativo della roadmap: quando @-menzionarla, come combinarla con le rules, pattern di uso in sessione.

## Cursor — Project Rules (`.cursor/rules/`)

Struttura attuale (efficienza token: core sempre attivo, HTML auto-attached, dominio on-demand):

| File | Ruolo |
|------|--------|
| `00-project-core.mdc` | **Always** — vincoli assoluti, stile risposta (IT), patch non file interi; non modificare file fuori `.cursor/rules/` senza permesso esplicito. **Contiene Mirror notice + Rejected patterns (ref) + Disagreement protocol** allineati a `docs/roadmap.md` §Notice. |
| `10-html-architecture.mdc` | **Auto** `*.html` — stato `state`, `coordconv_v2`, IDB, GPS opt-in (single-shot), offline tile, GIS Phase 1 hybrid stores (`gisTracks`/`gisPolygons`/`gisLayers` + transient undo/selection), `skipHistory`, i18n `data-i18n` vs `data-i18n-html`, idempotenza/cleanup. |
| `30-output-workflow.mdc` | **Always** — dopo ogni patch: RIEPILOGO numerato in chat + stesso testo in `/tmp/NN-goi-gis-riepilogo.md` (primo `NN` 01–99 libero); `/tmp/goi-gis-riepilogo.md` non è più il percorso standard. **Autosync:** `latest.md` + `inbox/` + commit/push selettivo memoria **anche** se il monolite non è committato; eccezione solo se l’utente chiede esplicitamente di non pubblicare. Comando memoria: **`aggio`** / **«aggiornati»** (stesso significato; vedi `docs/orchestrator/chatgpt-checkpoint.md`). |
| `20-domain-knowledge.mdc` | **Agent Requested** — CRS/datum extra, geocoding Nominatim/OPSEC, tile pack/coverage, track, export, misura on-map. |
| `99-known-state.mdc` | **Manual** (`@99-known-state`) — solo invarianti “non rompere queste cose”. |

**Rimosso:** `regole.mdc` (contenuto ripartito nei file sopra, per evitare duplicazione `alwaysApply`).

## Relazione mirror roadmap ↔ rules

`docs/roadmap.md` (sezione "Notice to AI Assistants") e `.cursor/rules/00-project-core.mdc` (sezioni Mirror notice / Rejected patterns / Disagreement protocol) contengono **contenuto duplicato per design**. Quando si modifica uno, aggiornare l'altro nella stessa operazione. La roadmap resta la sorgente autoritativa per la lista Rejected patterns estesa; le rules contengono il principio + esempi principali.

## Invarianti da non rompere (sintesi)

- Nessun GPS silenzioso all’avvio; contesto sicuro per Geolocation.
- Geolocation **solo single-shot** (`getCurrentPosition` via `btnMyLocation`); il live tracking via `watchPosition` è stato rimosso nel cleanup pre-GIS e non va reintrodotto senza decisione esplicita.
- Offline: tile da IDB + placeholder; niente fallback “online forzato” che violi scelta utente/OPSEC.
- Geocoding: `opsecStrict` blocca rete; niente invio automatico di paste non-coordinate senza conferma.
- i18n: `data-i18n` sicuro (`textContent`); `data-i18n-html` solo dove esplicitamente previsto.
- Persistenza: sanitizzare/clampare al load tutto ciò che arriva da `localStorage`.
- GIS Phase 1: `mapWaypoints[]` resta canonico per waypoint; `gisTracks[]`/`gisPolygons[]`/`gisLayers[]` sono store GeoJSON-native additivi e persistiti; `gisSelection`/`undoStack`/`redoStack` restano transient.
- **GIS-first**: le sezioni classiche (paste, manual, geocoding, results, tools) restano nel DOM ma sono nascoste in home (`body.gis-mode > main > …`); vengono **spostate** con `appendChild` via `GIS_HOME_SLOTS`, **mai clonate**. `gisInit()` deve girare prima di `initMiniMapOnStartup()` per far rendere la mappa direttamente in `#gisMapMount`.
- Il pulsante on-map `[data-role="offline-panel-open"]` in GIS mode attiva la tab `layers` (capture-phase intercept): non bypassare con nuovi handler.

## Prossimo passo suggerito (solo se serve)

- Aggiornare **questo** `checkpoint.md` a fine sessione (1 minuto) oppure aggiungere un paragrafo in coda a `session-geolocalizzazione-e-mappa.md` per checkpoint “lunghi”.
