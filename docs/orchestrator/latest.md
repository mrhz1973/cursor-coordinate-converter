> **AUTOSYNC NON AUTORITATIVO.**  
> Questo file resta un riepilogo autosync, ma non è la memoria corrente del GIS.  
> Dopo il flip rules di Fase 3, per lo stato corrente leggere `README.md`, [`docs/OPERATING_MEMORY.md`](../OPERATING_MEMORY.md) e la WU corrente.  
> Non usare questo file come current-state primario.

# Stato operativo (sintetico)

Ingresso breve per **ChatGPT**; i dettagli in **`docs/orchestrator/inbox/`**. **Mantenerlo corto**.

## Ultimo aggiornamento

2026-08-12 — **`D-FLIGHT-F IMPLEMENTED — REVIEW REQUIRED — NOT DEPLOYED`**. Runtime task `5270342` / build **161** / `APP_BUILD_ID=D-FLIGHT-F`. Session-only helper client + OPSEC gate + CTA pannello. **Nessun** deploy GIS, **nessuna** edit CORS VPS, **nessun** restart helper, **nessun** `POST /refresh` reale, **nessun** `finito`, **nessuna** Browser QA live. Dettaglio: [`docs/orchestrator/inbox/2026-08-12_0704_riepilogo_d-flight-f-implemented-predeploy.md`](inbox/2026-08-12_0704_riepilogo_d-flight-f-implemented-predeploy.md). NEXT: review GPT-sostitutiva sul FULL SHA → poi CORS/config + deploy (separati).
